# Open-Ended Note Scanner Negative Fixture

This fixture intentionally contains exactly one unresolved open-ended note —
no bead id, no artifact path, no false-positive context — so the scanner must
flag it and the report must surface a "lacks linked bead/artifact" error.

## Drift example (must fail validation)

- We should add more cases here someday.
