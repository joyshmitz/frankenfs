# Open-Ended Note Scanner Negative Fixture

This fixture intentionally contains two unresolved open-ended notes — no bead
id, no artifact path, no false-positive context — so the scanner must flag them
and the report must surface "lacks linked bead/artifact" errors.

## Drift example (must fail validation)

- We should add more cases here someday.
- A fake delay can hide incomplete production behavior.
