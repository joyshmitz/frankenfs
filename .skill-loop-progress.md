# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankenfs performance campaign
# Total Passes: 5
# Started: 2026-06-01T19:21:38-04:00

## Status: IN PROGRESS - Pass 3 of 5 queued

Completed bead: `bd-pyxaz` (`[perf] inline little-endian parser reads in ffs-types`)

Completed bead: `bd-l4lxw` (`[perf] batch fsck/scrub 4 KiB block reads`)

Profile evidence:
- Checked-in `profiles/flamegraph_cli_inspect.svg` and `profiles/flamegraph_fuse_read.svg`
  show repeated `ffs_types::read_le_u16`, `ffs_types::read_le_u32`, and
  `ffs_types::ensure_slice` frames on parser/read-path workloads.

Baseline:
- `rch exec -- cargo bench --profile release-perf -p ffs-harness --bench metadata_parse -- metadata_parse`
- `metadata_parse`: `[140.82 ns, 150.14 ns, 158.98 ns]`

Outcome:
- No code kept. `bd-pyxaz` was closed as a rejected/no-change attempt.
- Candidate 1 (cold error helpers + fixed-byte-copy path): `metadata_parse`
  measured `[407.55 ns, 408.88 ns, 410.41 ns]`, worse than original baseline.
- Candidate 2 (cold error helpers only): `metadata_parse` measured
  `[447.13 ns, 448.98 ns, 451.16 ns]`.
- The tiny helper effect is below the noise of unpinned `rch` worker selection:
  the original baseline ran on `vmi1149989`; the candidate runs selected `ts2`.
  Treat tiny-helper optimization as inconclusive unless the campaign has a
  same-worker or same-invocation comparison protocol.

## Missions
1. Completed: perf bead and benchmark target selection. No ready `[perf]` beads
   were available initially; fallback bead `bd-pyxaz` was attempted and closed
   with no code kept. Ready cc-filed perf beads are now available.
2. Completed for pass 1: baseline/profile/opportunity matrix. Remote criterion
   baseline captured; checked-in flamegraphs provided the hotspot target.
3. Completed for pass 1: single-lever optimization attempted twice, both
   rejected by measurement.
4. Completed for pass 1: behavior proof used `ffs-types` tests plus listed
   golden sha256 manifests. `scripts/verify_golden.sh --checksums-only` has a
   pre-existing nested-fixture coverage failure, filed as `bd-3vink`.
5. Completed for pass 2: `bd-l4lxw` resumed the current in-flight optimization pass,
   replacing the previously blocked parser pass because `br ready --json`
   reports it as assigned to PlumFern and it owns the compiler blocker that
   stopped the earlier run. Focused remote `rch` check, clippy, and unit tests
   pass for the `ffs-block`/`ffs-repair` batch-read lever.
   - Baseline: p50=16.0 ms, p95=18.3 ms, p99=18.9 ms; strace=2051 pread64,
     2048 of them 4096-byte block reads.
   - After: p50=14.52 ms, p95=16.45 ms, p99=16.84 ms; strace=32 preadv plus
     3 pread64 metadata reads.
   - Score: Impact 4 x Confidence 4 / Effort 2 = 8.0, so the lever is kept.
   - Isomorphism: ordering preserved (same ascending block range), tie-breaking
     N/A, floating-point N/A, RNG N/A. Golden proof: `sha256sum -c` passed for
     `conformance/golden/checksums.sha256` and `tests/fixtures/golden/checksums.txt`;
     ext4 inspect output matched the checked-in `ext4_8mb_reference.json`
     metadata subset with `diff`.
   - Caveat: `rch exec` warns and falls through for non-compilation `hyperfine`;
     the timing was run against the release-perf binary built and retrieved by
     `RCH_FORCE_REMOTE=true rch exec -- cargo build -p ffs-cli --profile release-perf`.
6. Blocked for pass 2 before this resume: `bd-r8zw8` was claimed, baseline was established, and a
   single `ffs-ondisk` group-descriptor decode lever was attempted. The
   candidate was reverted because the required post-change `rch` benchmark
   could not run while peer-owned `bd-l4lxw` edits in `crates/ffs-block/src/lib.rs`
   fail to compile on `std::os::unix::fs::FileExt::read_vectored_at`
   (`E0658`, `unix_file_vectored_at`). `bd-r8zw8` is back to `open` with a
   blocker note.

## Completed Passes
1. `bd-pyxaz`: rejected/no-change helper-level parser read attempt.
2. `bd-l4lxw`: kept `ffs-block` contiguous vectored reads plus `ffs-repair`
   scrub batching. Result: pread-family hot calls collapsed from 2051 to 35,
   p95 improved from 18.3 ms to 16.45 ms on `fsck -f --json`.

## Blocked Passes
2. `bd-r8zw8`: no code kept. Baseline before candidate:
   `ext4_inode_parse` `[27.642 ns, 28.375 ns, 29.305 ns]`,
   `ext4_group_desc_32byte` `[7.1102 ns, 7.2404 ns, 7.3826 ns]`,
   `ext4_group_desc_64byte` `[7.6581 ns, 7.7427 ns, 7.8383 ns]`,
   `btrfs_superblock_parse_region` `[44.129 ns, 45.489 ns, 46.913 ns]`
   on `vmi1152480`. Focused tests/check/clippy passed for the candidate, but
   post-change benchmark failed before measurement due the unrelated
   `ffs-block` compile error above.
