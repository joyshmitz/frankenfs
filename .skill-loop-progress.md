# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankenfs [perf] beads
# Total Passes: 5+
# Started: 2026-06-03T11:22:55-04:00

## Status: COMPLETE - 5 of 5 passes executed; next target must be a deeper profiled primitive

## Missions
1. GF(2^8) LRC region multiply: validate and land `bd-itovh` portable-SIMD split-table vector-shuffle lever in `ffs-repair`, with scalar-vs-SIMD criterion A/B and byte-for-byte isomorphism.
2. Post-LRC reprofile: re-run the `ffs-repair` perf surface after pass 1 and pick the next dominant repair/erasure hot path; reject any target without profile or same-binary A/B evidence.
3. Block-cache or extent hotspot: use the next ready [perf] bead or measured criterion hotspot in `ffs-block`/`ffs-extent`; require one data-structure or memory-layout lever only.
4. MVCC/journal synchronization hotspot: use contention/tail evidence to select one synchronization-path lever; prove ordering and conflict semantics unchanged.
5. Final progressive deepening pass: re-run the no-gaps ready set, apply alien-graveyard matching to the highest residual hotspot, and land only if Score >= 2.0 with golden sha256 proof.

## Completed Passes
1. `bd-xmh5g.30`: rejected/no code kept. The 16-bit halfword zero-run summary
   table regressed the profile-backed `largest_free_run/fragmented_mixed_bytes`
   target on rch Criterion worker `vmi1149989`: prior bead baseline 835.63 ns
   mean, candidate production row 1.8059 us mean, same-binary A/B
   `old_mixed_word` 1.0263 us vs `halfword_summary` 2.1270 us. Source and
   temporary bench scaffolding were restored to HEAD. Next pass is using a fresh
   remote reprofile and must target a different primitive, not another larger
   lookup table.
2. `bd-xmh5g.32`: rejected/no code kept. The fixed-depth broadword
   run-existence ladder for `longest_zero_run_in_mixed_word` passed focused rch
   correctness proof after an initial property-test catch, but failed the
   performance gate. Fresh rch Criterion baseline on `vmi1227854`:
   `largest_free_run/fragmented_mixed_bytes` 985.92 ns mean. Candidate
   production rebench on the same worker: 2.1271 us mean. Same-binary A/B:
   `old_loop` 1.1697 us vs `fixed_depth_broadword` 1.7855 us. Source and
   temporary bench scaffold were restored to HEAD; next pass must avoid the
   largest-free-run micro-variant family and attack a different profiled
   primitive.
3. `bd-xmh5g.33`: rejected/no code kept. The mixed-word byte-summary
   composition lever preserved behavior but failed the performance gate.
   Fresh rch Criterion baseline on `vmi1153651`:
   `largest_free_run/fragmented_mixed_bytes` 2.6338 us mean. Candidate
   production rebench on `ts2`: 5.1617 us mean. Same-binary A/B:
   `old_mixed_word_loop` 1.9835 us vs `byte_table_word_chunks` 5.1641 us.
   Focused rch behavior proof passed 6 largest-run tests/proptests, and the
   marker-bounded golden report hash stayed
   `4d3300c701fc62e241ad715f89ccf4701ecb6b72ced820ca0922cc8a18dbf9b0`.
   Source and temporary bench scaffold were restored to HEAD; next pass must
   move off largest-free-run mixed-word variants and reprofile a different
   primitive.
4. `bd-xmh5g.34`: rejected/no code kept. The data-byte-major LRC global
   parity scheduling lever targeted the profiled `ffs-repair`
   `lrc_encode_global_64blocks_8parity` row after a fresh remote `scrub_codec`
   sweep. Baseline on rch worker `ts2`: 1.1328 ms mean. Focused behavior proof
   passed 46 LRC tests under rch. Candidate rebench on `ts2`: 2.4884 ms mean,
   a regression. Source was restored to HEAD. Next pass must avoid both the
   rejected GF(256) swizzle/SWAR region-multiply family and this byte-major
   parity scheduling family; attack a different profiled repair/codec
   primitive or a newly filed [perf] bead.
5. `bd-e59rn`: rejected/no code kept. The scrub-range batch `BlockNumber`
   allocation-removal lever targeted the profiled `scrub_clean_256blocks` and
   `scrub_corrupted_256blocks` rows. Baseline on rch worker `ts2`:
   clean 110.05 us mean, corrupted 108.47 us mean. Focused rch behavior proof
   passed 4 `scrub_range` tests, and the scrub report snapshot hash stayed
   `266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
   Candidate rebench on `ts2`: clean 109.26 us mean, corrupted 108.41 us mean,
   below the Score>=2.0 gate. Source was restored to HEAD. The next attack
   should move to a fundamentally different profiled primitive: RaptorQ decoder
   constraint solving with a proven source-symbol transform, RaptorQ encoder
   construction amortization, or a newly filed [perf] bead outside the
   `ffs-block` lane currently owned by another agent.

## Campaign 2026-06-04: RaptorQ Decode Row-Reduction

Status: IN PROGRESS - pass 7 completed; pass 8 must reprofile because the LRC
global-parity bottleneck shifted after row-parallel execution and RaptorQ encode
source-I/O batching failed the Score gate.

Profile-backed target: `ffs-repair` RaptorQ decode Criterion rows on rch worker
`ts2`:
- `raptorq_decode_group_16blocks`: 967.49 us mean
- `raptorq_decode_group_owned_symbols_16blocks`: 966.36 us mean
- `raptorq_decode_group_no_corruption_16blocks`: 148.05 ns mean

Golden artifact before pass 1:
`crates/ffs-repair/src/snapshots/ffs_repair__scrub__tests__scrub_report_human_output.snap`
= `266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.

Pass 1 lever: attempt a bounded RaptorQ repair-symbol row set sized to the
corruption cardinality, then append the remaining repair equations and retry
only if the bounded solve fails. This targets decoder equation count and repair
symbol cloning/moves, not the rejected transform-cache family.

Pass 1 result: rejected/no code kept. Focused rch proof on worker
`vmi1149989` passed `decode_bounded_repair_budget_matches_full_symbol_output`,
and the golden scrub report snapshot hash stayed
`266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Same-worker rch Criterion on `ts2` rejected the lever: baseline
`raptorq_decode_group_16blocks` 967.49 us mean vs candidate 1.0029 ms mean
(borrowed public path regression); baseline
`raptorq_decode_group_owned_symbols_16blocks` 966.36 us mean vs candidate
914.23 us mean (owned path win, insufficient because borrowed regressed);
no-corruption stayed flat at 148.05 ns vs 147.21 ns. Source was restored to
HEAD. Next pass should attack RaptorQ source-symbol construction by batching
contiguous intact block spans with `read_contiguous_blocks`, preserving source
symbol order and scalar error-order fallback.

Pass 2 result: rejected/no code kept. Alien-graveyard matching pointed at
batching/communication-avoidance, so the pass tested RaptorQ source-symbol
batching over contiguous intact spans. A temporary benchmark harness adjustment
made the in-memory `scrub_codec` device advertise real vectored reads and added
a same-binary scalar-vs-contiguous A/B row. Same-worker rch Criterion on `ts2`
rejected the lever: the production borrowed row changed from the clean baseline
966.61 us mean to 980.99 us mean, the owned row changed from 966.36 us mean to
974.66 us mean, and no-corruption stayed flat at 148.18 ns. The isolated A/B
only showed 980.39 us mean for scalar reads vs 965.15 us mean for contiguous
reads, a small read-path win that is below Score>=2.0 and does not survive the
public production row. Source and temporary benchmark scaffolding were restored
to HEAD. Next pass must avoid source I/O micro-levers and attack RaptorQ
decode-kernel work or encoder construction amortization.

Pass 3 result: rejected/no code kept. This pass narrowed the pass-1 repair-row
idea to the owned-symbol recovery path only, leaving borrowed decode on the
full original repair-row set. Focused rch proof
`decode_owned_bounded_budget_matches_borrowed_full_output` passed on
`vmi1149989`, but the same-worker `ts2` Criterion owned row failed to repeat
the prior apparent win: original production baseline
`raptorq_decode_group_owned_symbols_16blocks` 966.36 us mean vs candidate
969.59 us mean. Source was restored to HEAD. The next pass must stop iterating
bounded repair-row variants and move to a different primitive: decoder
kernel/data-structure work inside the RaptorQ solve, or encoder construction
amortization with same-binary A/B proof.

Pass 4 result: confirmed strategic blocker/no code kept for `bd-416sl`.
Profile context was the closed `bd-itovh` GF(256) split-table target, where the
expected pshufb-class primitive is gated by `std::simd::Simd<u8, N>::swizzle_dyn`.
Current local toolchain is `rustc 1.98.0-nightly (14210df0e 2026-05-31)`,
LLVM 22.1.6. The local and rch codegen probes compiled a safe
`#![forbid(unsafe_code)]` `swizzle_dyn` kernel with `-O -C
target-cpu=x86-64-v3`; `rustc --print cfg` confirmed `target_feature="ssse3"`
and `target_feature="avx2"`, but the emitted assembly still contained scalar
`vpextrb`/`vpinsrb` lane extraction/insertion and no `pshufb`/`vpshufb`.
Remote evidence came from rch worker `vmi1153651`. Isomorphism proof is N/A
because no production code changed; ordering, tie-breaking, floating-point, and
RNG behavior are unchanged by construction, and the scrub golden snapshot hash
remains
`266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Conclusion: do not re-attempt safe-Rust shuffle/gather SIMD kernels by merely
raising `target-cpu`; attack a complexity-class or memory-layout primitive next.

Pass 5 result: rejected/no code kept. Refreshed rch Criterion on worker `ts1`
ranked the local `ffs-repair` surface as LRC global parity 1.1088 ms mean,
RaptorQ owned decode 1.0788 ms mean, and RaptorQ borrowed decode 978.10 us
mean. Alien-graveyard matching pointed at memory-layout kernels, so the pass
tested a nibble-sliced GF(256) multiply table for `encode_global`: 8 KiB of
`coeff * low_nibble` and `coeff * high_nibble` tables instead of the incumbent
64 KiB byte table. Algebraic isomorphism was proven by an exhaustive remote
test over all 256 coefficients and all 256 input bytes, and 47 LRC tests
including the golden parity report passed on rch worker `ts2`. Performance
failed: same-worker `ts2` baseline for `lrc_encode_global_64blocks_8parity`
was 1.1328 ms mean, while the candidate measured 1.6926 ms mean. Source was
restored to HEAD. Next pass must stop iterating GF table shape variants and
attack a different primitive, such as parity-row parallelism with deterministic
ordering proof or a higher-level repair scheduling/batching target with a
fresh rch profile.

Pass 6 result: accepted. The profiled target was
`lrc_encode_global_64blocks_8parity`: fresh surface baseline on rch worker
`ts1` was 1.1088 ms mean, and same-worker-family baseline on `ts2` was
1.1328 ms mean. The lever parallelizes independent global parity row
construction with Rayon while preserving the indexed `j` row order through
ordered range collection. Candidate rebench on `ts2`: 456.74 us mean
([431.71 us, 483.26 us]), a 2.48x win versus the `ts2` baseline and above the
Score>=2.0 gate. Behavior proof: `RCH_FORCE_REMOTE=true` `cargo test -p
ffs-repair lrc -- --nocapture` passed 46 LRC tests on `ts2`; `cargo check -p
ffs-repair --all-targets` passed on `ts2`; `cargo clippy -p ffs-repair
--all-targets -- -D warnings` passed on `ts2`; `cargo fmt --check --package
ffs-repair` passed locally. Golden scrub snapshot sha256 stayed
`266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Isomorphism: ordering preserved by indexed range collect; tie-breaking N/A;
floating-point N/A; RNG unchanged/N/A; each parity row is independent and writes
only its owned output buffer. Pass 7 should reprofile `ffs-repair` before
choosing the next primitive.

Pass 7 result: rejected/no code kept. After the accepted LRC row-parallel pass
shifted the repair profile, this pass tested a different RaptorQ encode-source
I/O primitive: use `read_contiguous_blocks` for `encode_group` only when the
device advertises contiguous reads, preserving the existing scalar loop for
non-vectored devices and invalid ranges. Fresh forced-remote baseline for
`raptorq_encode_group_16blocks` on rch worker `vmi1293453` was 266.54 us mean
([253.20 us, 286.27 us]). Focused forced-remote proof
`encode_contiguous_read_path_matches_scalar_symbols` passed on worker
`vmi1156319`, proving byte-for-byte repair-symbol equivalence between scalar
and contiguous source-symbol construction. Candidate same-binary Criterion on
rch worker `vmi1149989` rejected the lever: contiguous target row 357.37 us
mean ([299.87 us, 403.58 us]) vs scalar-device A/B row 335.48 us mean
([307.07 us, 364.97 us]). Ordering preserved, tie-breaking N/A,
floating-point N/A, RNG unchanged/N/A, and the scrub golden snapshot hash
remains
`266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Source and temporary benchmark scaffolding were restored to HEAD. Next pass
must reprofile the post-LRC surface and avoid RaptorQ source-I/O batching.

Pass 8 result: rejected/no code kept (`bd-xmh5g.42`). Fresh remote profile on
`ts2` kept RaptorQ decode as the dominant `ffs-repair` surface:
`raptorq_decode_group_16blocks` 962.84 us mean,
`raptorq_decode_group_owned_symbols_16blocks` 994.39 us mean, and
`lrc_encode_global_64blocks_8parity` 395.58 us mean. Alien-graveyard mapping
matched RaptorQ §1.1 sparse GF(256) inactivation decode; the tested one-lever
candidate set `RQ_DECODE_WAVEFRONT_BATCH` from 4 to 0 (sequential/single-batch
mode). Focused forced-remote behavior proof passed 15 decode tests, including
the encode/decode proptest, on worker `vmi1153651`; the scrub golden snapshot
hash stayed
`266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Candidate Criterion on worker `vmi1167313` failed the performance gate:
borrowed decode 1.2059 ms mean, owned decode 1.1644 ms mean, and
no-corruption 180.46 ns mean. Source was restored to HEAD. Next pass must
avoid wavefront batch-width tuning and attack a deeper decode-kernel primitive:
upstream asupersync sparse/dense solve data layout, GF(256) addmul block kernel,
or a new profile-backed non-RaptorQ target.

Pass 9 result: rejected/no code kept (`bd-xmh5g.43`). Profile-backed target
remained RaptorQ decode after the pass-8 `ts2` baseline: borrowed decode
962.84 us mean, owned decode 994.39 us mean, and no-corruption 152.72 ns mean.
Alien-artifact rationale: `asupersync`'s `InactivationDecoder` owns a
dense-factor artifact cache and its upstream tests prove second-run cache hits,
but FrankenFS constructed a fresh decoder per `decode_group` call. The tested
one-lever candidate cached `InactivationDecoder` instances by
`(K, symbol_size, seed)` so repeated group recovery shapes could retain dense
factor artifacts. Forced-remote proof passed `cargo check -p ffs-repair
--all-targets` on `ts2`; focused decode proof passed 15 tests, including the
encode/decode proptest, on `ts2`; the scrub golden snapshot hash stayed
`266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Candidate Criterion on `ts2` failed the keep gate: borrowed decode 962.97 us
mean ([959.57 us, 966.81 us]), owned decode 974.69 us mean
([968.65 us, 980.86 us]), and no-corruption 148.54 ns mean
([148.08 ns, 149.06 ns]). The dominant borrowed row was unchanged, and the
owned-symbol win was too small/borderline for a global cache. Source was
restored to HEAD. Next pass must stop wrapper-level RaptorQ batch/cache tuning
and either attack the upstream GF(256) dense-solve kernel as a separate
coordinated dependency primitive or reprofile a non-RaptorQ surface not owned by
the LRC bead.

Pass 10 result: rejected/no code kept (`bd-xmh5g.41`). The in-flight LRC bead
tested a single safe-Rust unroll of `gf256_mul_xor_into` over 8-byte chunks,
preserving the existing GF(256) coefficient lookup and bytewise XOR result.
Fresh rch baseline for `lrc_encode_global_64blocks_8parity` before the lever
was 777.84 us mean on `vmi1156319`; the first candidate run on `vmi1227854`
looked promising at 185.92 us mean but was cross-worker. A pinned
`RCH_WORKER=vmi1227854` A/B rejected the lever: baseline 381.45 us mean
([365.75 us, 399.46 us]) vs candidate confirmation 246.28 us mean
([232.79 us, 262.15 us]), only ~1.55x and below the Score>=2.0 keep gate.
Focused remote proof passed 46 LRC tests on `vmi1156319`; local
`cargo fmt --check --package ffs-repair` passed; the scrub golden snapshot hash
stayed `266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`.
Isomorphism: ordering preserved because each byte keeps the same table lookup
and XOR result; tie-breaking N/A; floating-point N/A; RNG unchanged/N/A.
Source was restored to HEAD. Next pass should not continue LRC byte-loop
micro-unrolling; attack a different profiled primitive, preferably upstream
asupersync GF(256) dense-solve kernel work or a newly reprofiled non-RaptorQ
surface.

Pass 11 result: accepted (`bd-xmh5g.44`). No ready perf child bead existed, so
the pass reprofiled a non-overlapping `ffs-alloc` surface and targeted
`alloc_blocks_batch_persist`. Fresh same-worker baseline on rch worker `ts2`
for `cargo bench -p ffs-alloc --profile release-perf --bench batch_alloc --
--noplot`: `alloc_20_blocks/batch_20` 2.6632 us mean and
`alloc_100_blocks/batch_100` 4.0969 us mean. The kept lever replaces repeated
per-block `bitmap_find_free` calls inside `try_alloc_batch_in_group` with a
single cyclic streaming bitmap scan that sets selected free bits in place and
emits the same one-block `BlockAlloc` sequence. Candidate same-worker rebench
on `ts2`: `batch_20` 2.5220 us mean and `batch_100` 2.9094 us mean, a 1.41x
win on the profiled `batch_100` target with high confidence and low effort
(Score >= 2.0). Behavior proof: remote `cargo check -p ffs-alloc
--all-targets` passed; remote `cargo test -p ffs-alloc batch -- --nocapture`
passed 15 batch allocation tests including batch-vs-single proptest; remote
`bitmap_take_free_bits_cyclic_golden_report` passed and produced golden rows
whose sha256 is
`63fe7e8032b595c84a9c3e83a1d06b809a790c63acac240b27922e460aba11dd`. Golden
artifact:
`tests/artifacts/perf/2026-06-04_batch_alloc_stream/golden_batch_take_rows.tsv`.
Remote `cargo clippy -p ffs-alloc --all-targets -- -D warnings` passed on
`ts2`; local `cargo fmt --check --package ffs-alloc` passed because `rch`
refuses non-compilation `cargo fmt` commands.
Isomorphism: ordering preserved by cyclic first-free order; tie-breaking
unchanged because equal candidates remain lowest bit in cyclic order from the
goal; floating-point N/A; RNG unchanged/N/A.

Pass 12 result: rejected/no code kept (`bd-xmh5g.45`). Post-pass-11 remote
profile of `ffs-alloc` `bitmap_ops` showed `succinct_build` as a distinct
residual construction target (`vmi1149989` full-profile row 1.3347 us mean;
focused baseline 1.5338 us mean [1.4406 us, 1.6294 us]). Alien-graveyard
mapping pointed at succinct/rank-select construction and cache-local
streaming, so the pass tested a single streaming rank-table builder for
`SuccinctBitmap::build`: copy the bitmap once, then count aligned 256-bit
blocks from the owned data instead of calling `popcount_range` per block.
Focused rch behavior proof `build_index_golden_report` passed on
`vmi1293453`, covering empty, partial-byte, superblock-crossing, and full
32K-bit index construction. Candidate Criterion failed the keep gate on rch
worker `ts2`: `succinct_build` 3.7196 us mean ([3.7187 us, 3.7205 us]), a
clear regression versus the baseline family. Source was restored to HEAD and
local `cargo fmt --check --package ffs-alloc` passed. Next pass must avoid
succinct-build range/count micro-layout variants and attack a different
profiled primitive, such as allocator/Bw-tree chain consolidation or a fresh
`ffs-repair` decode-kernel/data-layout target with same-worker evidence.

Pass 13 result: accepted (`bd-xmh5g.47`). Profile-backed non-overlapping target
was the `ffs-btree` mixed 8-thread Bw-tree delta-chain residual after pass 12,
while `ffs-repair` LRC and MVCC flush lanes were peer-owned. The one lever makes
`DEFAULT_CONSOLIDATION_THRESHOLD` active in `MappingTable::append_delta`,
preconsolidating an equivalent base state before retrying the pending mutation
instead of waiting for `MAX_CHAIN_DEPTH`. Same-binary rch Criterion on worker
`vmi1264463`: `old_deferred_consolidation_8` 4.9455 s mean
([4.6494 s, 5.2365 s]) -> `new_default_preconsolidation_8` 2.1811 s mean
([1.6495 s, 2.7749 s]), a 2.27x win and Score>=2.0. Behavior proof: remote
`cargo check -p ffs-btree --all-targets` passed on `ts2`; remote
`cargo clippy -p ffs-btree --all-targets -- -D warnings` passed on `ts2`;
remote golden test
`append_preconsolidation_matches_deferred_golden_report` passed; local
`cargo fmt -p ffs-btree --check` passed. Golden artifact:
`tests/artifacts/perf/2026-06-04_bwtree_preconsolidation/bwtree_preconsolidation_golden.tsv`
with sha256 `354ce4886e5de4ef335c9826d11f11db4d5a7d57d47ec62886d04f6e49a210fb`.
Isomorphism: ordering preserved by CAS linearization; tie-breaking unchanged
because newest delta shadowing and `BTreeMap` order are unchanged;
floating-point N/A; RNG unchanged. Next pass should reprofile and avoid
occupied `ffs-repair` LRC (`CrimsonDove`) and peer-owned MVCC flush lanes.

Pass 14 result: accepted (`bd-xmh5g.48`). Fresh rch profile after pass 13 showed
the remaining Bw-tree write path dominated by repeated full-base
materialization: `write_heavy/bwtree/8` 436.65 ms mean versus locked 26.134 ms,
and `mixed/bwtree/8` 283.48 ms versus locked 26.146 ms. Alien-graveyard mapping
picked a B-epsilon-tree message-buffer primitive combined with the existing
Bw-tree delta chain. The one lever adds an insert/delete `MessageBuffer` head
delta that coalesces newest per-key mutations over the same successor chain and
falls back to flushing when the bounded buffer is full. Same-binary rch
Criterion on worker `vmi1149989`: `old_individual_preconsolidation_8` 541.55 ms
mean ([512.15 ms, 571.45 ms]) -> `new_message_buffer_8` 149.65 ms mean
([142.16 ms, 156.83 ms]), a 3.62x win. Keep score:
`3.62 * 0.90 / 1.50 = 2.17`, clearing the Score>=2.0 gate. Behavior proof:
remote `cargo check -p ffs-btree --all-targets` passed on `ts1`; remote golden
test `message_buffer_matches_individual_preconsolidation_golden_report` passed
on `ts1`; remote `cargo test -p ffs-btree -- --nocapture` passed on `ts1`;
remote `cargo clippy -p ffs-btree --all-targets -- -D warnings` passed on
`ts2`; local `cargo fmt --package ffs-btree --check` and `git diff --check`
passed. Golden artifact:
`tests/artifacts/perf/2026-06-04_bwtree_message_buffer/bwtree_message_buffer_golden.tsv`
with sha256 `4f2958281a766531e9a890d31147aa1f7102c136cab39f85207ed35e09a2f9f6`.
Isomorphism: ordering preserved by CAS head publication over the same successor
chain; tie-breaking unchanged because newest same-key mutation shadows older
mutations and different-key effects commute in the materialized `BTreeMap`;
floating-point N/A; RNG unchanged. Next pass must reprofile because the
write-path bottleneck has shifted after buffering.
