# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankenfs [perf] beads
# Total Passes: 5
# Started: 2026-06-03T11:22:55-04:00

## Status: IN PROGRESS - Pass 5 of 5 pending next profile-backed perf target

## Missions
1. GF(2^8) LRC region multiply: validate and land `bd-itovh` portable-SIMD split-table vector-shuffle lever in `ffs-repair`, with scalar-vs-SIMD criterion A/B and byte-for-byte isomorphism.
2. Post-LRC reprofile: re-run the `ffs-repair` perf surface after pass 1 and pick the next dominant repair/erasure hot path; reject any target without profile or same-binary A/B evidence.
3. Block-cache or extent hotspot: use the next ready [perf] bead or measured criterion hotspot in `ffs-block`/`ffs-extent`; require one data-structure or memory-layout lever only.
4. MVCC/journal synchronization hotspot: use contention/tail evidence to select one synchronization-path lever; prove ordering and conflict semantics unchanged.
5. Final progressive deepening pass: re-run the no-gaps ready set, apply alien-graveyard matching to the highest residual hotspot, and land only if Score >= 2.0 with golden sha256 proof.

## Completed Passes
1. `bd-xmh5g.30`: rejected/no code kept. The 16-bit halfword zero-run summary
   table regressed the profile-backed `largest_free_run/fragmented_mixed_bytes`
   target on rch Criterion worker `vmi1149989`: prior bead baseline 835.63 ns
   mean, candidate production row 1.8059 us mean, same-binary A/B
   `old_mixed_word` 1.0263 us vs `halfword_summary` 2.1270 us. Source and
   temporary bench scaffolding were restored to HEAD. Next pass is using a fresh
   remote reprofile and must target a different primitive, not another larger
   lookup table.
2. `bd-xmh5g.32`: rejected/no code kept. The fixed-depth broadword
   run-existence ladder for `longest_zero_run_in_mixed_word` passed focused rch
   correctness proof after an initial property-test catch, but failed the
   performance gate. Fresh rch Criterion baseline on `vmi1227854`:
   `largest_free_run/fragmented_mixed_bytes` 985.92 ns mean. Candidate
   production rebench on the same worker: 2.1271 us mean. Same-binary A/B:
   `old_loop` 1.1697 us vs `fixed_depth_broadword` 1.7855 us. Source and
   temporary bench scaffold were restored to HEAD; next pass must avoid the
   largest-free-run micro-variant family and attack a different profiled
   primitive.
3. `bd-xmh5g.33`: rejected/no code kept. The mixed-word byte-summary
   composition lever preserved behavior but failed the performance gate.
   Fresh rch Criterion baseline on `vmi1153651`:
   `largest_free_run/fragmented_mixed_bytes` 2.6338 us mean. Candidate
   production rebench on `ts2`: 5.1617 us mean. Same-binary A/B:
   `old_mixed_word_loop` 1.9835 us vs `byte_table_word_chunks` 5.1641 us.
   Focused rch behavior proof passed 6 largest-run tests/proptests, and the
   marker-bounded golden report hash stayed
   `4d3300c701fc62e241ad715f89ccf4701ecb6b72ced820ca0922cc8a18dbf9b0`.
   Source and temporary bench scaffold were restored to HEAD; next pass must
   move off largest-free-run mixed-word variants and reprofile a different
   primitive.
4. `bd-xmh5g.34`: rejected/no code kept. The data-byte-major LRC global
   parity scheduling lever targeted the profiled `ffs-repair`
   `lrc_encode_global_64blocks_8parity` row after a fresh remote `scrub_codec`
   sweep. Baseline on rch worker `ts2`: 1.1328 ms mean. Focused behavior proof
   passed 46 LRC tests under rch. Candidate rebench on `ts2`: 2.4884 ms mean,
   a regression. Source was restored to HEAD. Next pass must avoid both the
   rejected GF(256) swizzle/SWAR region-multiply family and this byte-major
   parity scheduling family; attack a different profiled repair/codec
   primitive or a newly filed [perf] bead.
