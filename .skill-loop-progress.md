# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: FrankenFS perf campaign
# Total Passes: 5
# Started: 2026-06-04T22:32:47-04:00

## Status: COMPLETED - bd-xmh5g.63 allocator clone lever

## Missions
1. Finish in-flight ownership: resume `bd-xmh5g.63`, reserve only `ffs-alloc`, and leave peer dirty `ffs-btree`/`ffs-extent`/`ffs-btrfs`/`ffs-core` surfaces untouched.
2. Baseline and proof surface: use the bead's fresh RCH baseline for `single_20x1`/`single_100x1`, and bind proof to existing allocation-order plus rollback-byte tests.
3. Single allocator lever: remove the extra 4 KiB rollback bitmap clone in `try_alloc_safe` by keeping the read `BlockBuf` as rollback source.
4. Verification and rebench: run crate-scoped RCH behavior proof, golden sha256, fmt/diff checks, and same-surface Criterion rebench.
5. Commit, close, and reprofile: commit only if Score>=2.0, close `bd-xmh5g.63`, push `main` and `main:master`, then re-triage for shifted perf bottlenecks.

## Completed Passes
1. Finish in-flight ownership: resolved current identity as `PlumFern`; `bd-xmh5g.63` is assigned to `PlumFern`; reserved `crates/ffs-alloc/src/lib.rs` and `.skill-loop-progress.md`; confirmed other dirty files belong to other active perf/parity surfaces.
2. Baseline and proof surface: bead baseline is RCH `batch_alloc` on `vmi1153651`, `single_20x1` [66.975, 68.933, 71.192] us and `single_100x1` [322.71, 328.49, 336.00] us; existing `alloc_persist_gdt_write_failure_restores_bitmap_and_group_stats` proves exact rollback bytes.
3. Single allocator lever: worktree changes only `try_alloc_safe`, replacing `original_bitmap` + clone with one mutable bitmap Vec and `bitmap_buf.as_slice()` for rollback.
4. Verification and rebench: `cargo fmt --check -p ffs-alloc`, `git diff --check`, RCH `cargo clippy -p ffs-alloc --all-targets -- -D warnings` on `ts1`, direct RCH `cargo test -p ffs-alloc batch_alloc -- --nocapture` on `ts2`, rollback test, and golden sha256 `4d3300c701fc62e241ad715f89ccf4701ecb6b72ced820ca0922cc8a18dbf9b0` passed.
5. Commit, close, and reprofile: RCH rebench on `vmi1149989` measured `single_20x1` [24.562, 25.556, 26.707] us and `single_100x1` [128.83, 132.64, 137.78] us; Score=2.4 despite baseline/rebench worker mismatch; `bd-xmh5g.63` closed with evidence.
