# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: reprofile for pass 6 profile-backed hotspot
# Total Passes: 5+
# Started: 2026-06-08T21:14:00Z

## Status: PASS 54 REJECTED - bd-xmh5g.181 production COW insert/update batching

## Missions
1. Reprofile restored source with a crate-scoped RCH benchmark before editing.
2. Select a top `[perf]` bead if available, otherwise file one for the
   profiler-evident hotspot.
3. Attack a different primitive family than the rejected S3-FIFO split,
   feedback, hot-hit counter, and contiguous write-back batching routes.
4. For any lever: prove behavior unchanged, rebench same-worker or same-binary,
   and keep only Score>=2.0.
5. Commit/close/push main and main:master after the pass decision.

## Latest Pass
54. `bd-xmh5g.181` btrfs production write-path final fs_tree insert + inode
   update batching: REJECTED, no runtime code kept. Local override proof was
   used because remote RCH workers were offline/blocked. Existing primitive A/B
   still showed the COW-tree opportunity in isolation:
   `sequential_insert_then_update` `[2.5275, 2.6425, 2.8092] ms` versus
   `batched_insert_then_update` `[1.3919, 1.4358, 1.4761] ms`. A narrowed
   production candidate was limited to the pure append/no-overlap branch and
   preserved candidate behavior in focused probes, but the actual production
   harness row did not improve. Detached old-code baseline at `0e9f0c1e` for
   `btrfs_write_path/btrfs_sequential_write_1m_4k` was
   `[17.716, 18.910, 20.147] ms`; candidate was
   `[19.195, 20.092, 20.896] ms` with Criterion p=0.16 and no significant
   improvement. Candidate behavior probes passed before rejection:
   `insert_then_update_matches_sequential_and_preserves_order_bd_hfkty`,
   `bd_xmh5g_18`, overwrite preservation, sector-boundary btrfs-check, and
   datasum csum capture. Score 0.0 because measured production impact was
   non-positive. Route next pass away from this final `fs_tree` insert plus
   inode update batching lever.

53. `bd-xmh5g.179` ffs-alloc bitmap largest-free-run table/SWAR route:
   REJECTED before source change. Same-worker RCH Criterion on `vmi1227854`
   established `largest_free_run/fragmented_mixed_bytes` baseline
   `[1.5637, 1.6700, 1.7562] us`. Existing same-binary A/B then rejected the
   obvious table route: `largest_free_run_ab/old_halfword_table`
   `[8.0206, 8.5817, 9.2963] us` versus current
   `largest_free_run_ab/word_run_detector` `[1.2499, 1.4080, 1.5947] us`.
   No runtime code was changed; behavior proof is N/A because no candidate
   shipped. Route next pass to a different hotspot family instead of
   micro-tuning this already-fast mixed-word detector.

52. `bd-h3087` btrfs send-stream parent-chain path/depth memoization: KEPT.
   Same-worker RCH Criterion on `vmi1227854`
   `btrfs_send_stream_deep_paths/generate_send_stream` improved from
   `[30.123, 30.754, 31.333] ms` to `[24.021, 24.530, 25.322] ms`
   (1.25x midpoint, 1.19x conservative lower/upper). Lever: cache
   `generate_send_stream` primary PATH construction and directory depth by
   inode while preserving first-link selection and `(depth, ino)` directory
   ordering. Isomorphism: emit ordering preserved by identical `emit_order`
   construction, tie-breaking unchanged via `links.first()` and inode sort key,
   floating point N/A, RNG N/A, and raw send-stream SHA-256 goldens preserved
   for flat, deep, renamed-parent, hardlink, missing-parent, root self-ref, and
   non-root self-loop cases:
   `bab19e3cc9e0639ff53470abc6ed5e596a9f9b9868a46d76061e31c3cf237431`,
   `482fd169b19c71bdb36a9bb4e70ad0bf7d4df087632b0ad9539062785089b480`,
   `064c7895f53ab4745c18cdceb536b56c197f969d46fc1297c6145bf5bed93af0`,
   `e23470feba03c5cb6acd9391ce357d8b85c31a416c0929526e12e9b09e92aeb7`,
   `5cc7d8d4ee9ef3d0c6c37ed721630551c420e9d3365bef194955af8eb50fa54e`,
   `d272ea2d17b469310a9c24b0e937ce5baad73e9ee173ae3a85f61c7afec97a92`,
   `b2e688204e610da9a4566562676611e1cd597231ae62e5fb0f87da0981e2584c`.
   Gates: `cargo fmt --check -p ffs-btrfs`, `git diff --check`,
   RCH `cargo test -j 1 -p ffs-btrfs generate_send_stream --lib --
   --nocapture --test-threads=1`, RCH `cargo check -j 1 -p ffs-btrfs
   --all-targets`, and RCH `cargo clippy -q -j 1 -p ffs-btrfs --all-targets
   -- -D warnings`. Score 6.1 (Impact 3.2 x Confidence 0.95 / Effort 0.5).

## Completed Passes
1. `bd-xmh5g.137` fixed 20/80 large-cache S3-FIFO split: REJECTED, no
   runtime code kept. The committed candidate at `c2ad1f7c` did not carry a
   valid same-worker timing proof, and the fresh RCH deterministic report was
   too small to justify keeping it: prior profile/workload baseline
   `compile_like hits=2160 misses=12688 hit_rate=0.145474`, current RCH
   `vmi1227854` report `hits=2195 misses=12653 hit_rate=0.147831`. The source
   is restored to the pre-split policy.
2. `bd-xmh5g.139` online S3-FIFO split/admission controller: REJECTED, no
   runtime code kept. Fresh restored-source baseline for
   `block_cache_s3fifo_compile_like` was RCH same-command local fallback
   `[17.426, 18.388, 19.518] ms`. Candidate workload report worsened from
   `hits=2160 misses=12688 hit_rate=0.145474` to
   `hits=2072 misses=12776 hit_rate=0.139547`; focused RCH timing also
   regressed to `[22.143, 22.776, 23.129] ms`.
3. `bd-xmh5g.140` standard S3-FIFO concurrent hot-read counter striping:
   REJECTED, no runtime code kept. Golden sha256 was preserved, but same-worker
   `ovh-b` timing regressed from `[8.0215, 8.3599, 8.7510] ms` to
   `[33.773, 34.620, 35.393] ms`.
4. `bd-xmh5g.141` contiguous write-back sync batching: REJECTED, no runtime
   code kept. Focused restored-source baseline on `ovh-a` for
   `writeback_sync_100x4k` was `[121.27, 122.21, 123.42] us`; candidate
   regressed to `[124.22, 125.48, 127.15] us`.
5. `bd-xmh5g.142` adaptive S3-FIFO scan-resistant split/admission: REJECTED,
   no runtime code kept. Clean baseline report on `vmi1227854`:
   `compile_like hits=2160 misses=12688 hit_rate=0.145474`,
   `mixed_seq70_hot30 hits=7829 misses=16171 hit_rate=0.326208`,
   `database_like hits=32977 misses=5903 hit_rate=0.848174`. Candidate report
   on `ovh-b` worsened all target rows:
   `compile_like hits=2140 misses=12708 hit_rate=0.144127`,
   `mixed_seq70_hot30 hits=7591 misses=16409 hit_rate=0.316292`,
   `database_like hits=32923 misses=5957 hit_rate=0.846785`. Score<2.0; the
   source was restored exactly to pre-lever state. Shipped-state isomorphism is
   exact: returned bytes, S3-FIFO FIFO/main/ghost ordering, eviction victim
   tie-breaking, floating-point behavior (N/A), and RNG seeds are unchanged.
6. `bd-xmh5g.143` ffs-repair RaptorQ small-erasure direct decode: KEPT.
   Same-worker RCH Criterion on `ovh-a` improved
   `raptorq_decode_group_16blocks` from 5.6875 ms to 3.2752 ms (1.74x
   midpoint, 1.63x conservative lower/upper) and
   `raptorq_decode_group_owned_symbols_16blocks` from 5.7001 ms to 3.2986 ms
   (1.73x midpoint, 1.60x conservative lower/upper). The original refreshed
   baseline and an independent clean baseline on `vmi1227854` both showed the
   same hotspot scale (`raptorq_decode_group_16blocks` 5.5540 ms and
   5.4811 ms midpoints, respectively). The lever adds a safe-Rust GF(256)
   direct solve for one or two unique corrupt source blocks and falls back to
   the existing inactivation decoder unless all supplied repair rows validate.
   Isomorphism: requested corrupt-block ordering and duplicates preserved,
   tie-breaking uses the first full-rank repair row set in input order and
   validates all repair rows before return, floating point N/A, RNG seed
   unchanged via `repair_seed`, and golden SHA checks passed
   (`scrub_report_human_output.snap` sha256
   `266a75f96df99a397f78bff54632acc9b5b025577392366d2cd90ee3f5ad4b42`).
   Score 6.9 (Impact 4.6 x Confidence 0.9 / Effort 0.6).
7. `bd-xmh5g.144` ffs-repair fused direct-erasure known+basis encoder:
   KEPT. Same RCH local-fallback path improved
   `raptorq_decode_group_16blocks` from `[4.2856,4.8516,5.1633] ms` to
   `[2.8445,2.9338,3.0647] ms` (1.65x midpoint, 1.40x conservative) and
   `raptorq_decode_group_owned_symbols_16blocks` from
   `[3.8677,3.9906,4.1454] ms` to `[2.8359,2.8765,2.9222] ms` (1.39x
   midpoint, 1.32x conservative). The lever fuses the previous known-source
   and one-byte missing-basis encoder solves into one GF(256) product-space
   encoder with `block_size + missing_count` lanes. Isomorphism: corrupt output
   ordering and duplicates preserved, full-rank repair-row scan order
   unchanged, floating point N/A, RNG seed unchanged, full repair-row
   validation unchanged, and golden SHA checks passed for the scrub snapshot,
   codec corruption fixture, and recovery evidence JSONL. Score 4.6 (Impact
   4.1 x Confidence 0.9 / Effort 0.8).
8. `bd-xmh5g.145` ffs-repair source-domain syndrome direct decode:
   KEPT. Same RCH local-fallback path improved
   `raptorq_decode_group_16blocks` from `[2.8986,2.9594,3.0035] ms` to
   `[551.69,562.51,581.12] us` (5.26x midpoint, 4.99x conservative) and
   `raptorq_decode_group_owned_symbols_16blocks` from
   `[2.9056,2.9954,3.1448] ms` to `[546.73,553.77,563.39] us` (5.41x
   midpoint, 5.16x conservative). The lever replaces the widened
   `block_size + missing_count` direct encoder with a K-byte source-domain
   coefficient encoder plus cached repair residual syndromes. Isomorphism:
   corrupt output ordering and duplicates preserved, full-rank repair-row scan
   order unchanged, floating point N/A, RNG seed unchanged, invalid repair ESI
   fallback preserved via `InactivationDecoder::repair_equation`, source-domain
   projection is proven against the prior fused known+basis oracle, and golden
   SHA checks passed for the scrub snapshot, codec corruption fixture, and
   recovery evidence JSONL. Score 8.9 (Impact 4.7 x Confidence 0.95 / Effort
   0.5).
9. `bd-xmh5g.146` ffs-repair LRC source-major global parity:
   REJECTED, no runtime code kept. RCH local-fallback baseline from the pass-9
   profile was `lrc_encode_global_64blocks_8parity`
   `[559.77,583.58,617.72] us`; the one-lever candidate changed
   `encode_global` from parity-major Rayon projection to source-major
   multi-parity projection and regressed to `[973.84,989.83,1003.3] us`.
   Candidate behavior proof was clean before rejection: LRC global golden
   sha256 stayed
   `9968a12c3b5c77999bd21b612756d65b0f40841deb23de7801525c561c5f53d0`,
   parity block ordering `j=0..p` was unchanged, each parity block still
   accumulated data blocks in increasing `i` order, tie-breaking N/A,
   floating point N/A, RNG N/A, and zero-parity mismatch-validation behavior
   was preserved by restoring the original path. Gates run for the candidate:
   `cargo fmt --package ffs-repair --check`, RCH local-fallback
   `cargo check -p ffs-repair --all-targets`, and RCH remote
   `cargo clippy -p ffs-repair --all-targets -- -D warnings`. Score 0.0
   because the measured impact was negative.
10. `bd-xmh5g.147` ffs-repair RaptorQ source-domain encode projection:
   KEPT. Same RCH local-fallback benchmark command improved
   `raptorq_encode_group_16blocks` from `[3.0663,3.1779,3.3353] ms` to
   `[534.56,538.42,542.39] us` (5.90x midpoint, 5.65x conservative). The
   lever replaces the encode-side full `block_size`-wide RaptorQ
   `SystematicEncoder` solve with a K-byte source-coefficient encoder and
   GF(256) projection over the real source blocks. Isomorphism: repair ESI
   ordering remains `K..K+repair_count-1`, `is_source=false` is unchanged,
   degree metadata is reconstructed from the same RFC tuple parameters, repair
   bytes match the old full encoder, tie-breaking N/A, floating point N/A, and
   RNG seed derivation is unchanged via `repair_seed`. Golden SHA checks
   matched exactly between the old full-encoder reference and optimized
   `encode_group`:
   `c862675bccc157b71044085c51d2a4c1d0d41fe9edf95fac861d79142d81e759`.
   Gates: `cargo fmt --package ffs-repair --check`, RCH local-fallback
   `cargo test -p ffs-repair raptorq_reference_encode_golden_report -- --nocapture`,
   RCH local-fallback
   `cargo test -p ffs-repair raptorq_encode_golden_report -- --nocapture`,
   RCH local-fallback
   `cargo test -p ffs-repair encode_group_projected_symbols_match_full_encoder_reference -- --nocapture`,
   RCH local-fallback `cargo check -p ffs-repair --all-targets`, and RCH
   local-fallback `cargo clippy -p ffs-repair --all-targets -- -D warnings`.
   Score 11.2 (Impact 5.9 x Confidence 0.95 / Effort 0.5).
11. `bd-xmh5g.148` ffs-repair RaptorQ direct-decode source-major residual
    projection: KEPT. Same-worker RCH remote `vmi1227854` improved
    `raptorq_decode_group_16blocks` from `[656.26,689.01,724.55] us` to
    `[567.48,591.48,616.40] us` (1.165x midpoint, 1.065x conservative).
    The lever keeps direct-decode row selection identical, caches each
    selected row's source-domain coefficients, and projects known source
    contributions source-major across rows. Isomorphism: corrupt output
    ordering and duplicates preserved by unchanged `recover_from_direct_solution`,
    full-rank repair-row tie-breaking unchanged by unchanged
    `select_full_rank_pair` and repair-symbol input order, per-row GF
    accumulation over known source indices remains ascending, floating point
    N/A, RNG seed unchanged via `repair_seed`, and decode golden stdout sha256
    was `5e11bf11a354338c346ceffaa88d681e03a0cd794bd4ffb280f31a14dc58608f`.
    Gates: `rustfmt --edition 2024 --check crates/ffs-repair/src/codec.rs`,
    RCH remote `vmi1227854` `cargo test -p ffs-repair decode -- --nocapture`,
    RCH remote `vmi1227854` `cargo check -p ffs-repair --all-targets`, and
    RCH remote `vmi1227854`
    `cargo clippy -p ffs-repair --all-targets -- -D warnings`. Score 2.3
    (Impact 1.16 x Confidence 4 / Effort 2).
12. `bd-xmh5g.149` ffs-repair RaptorQ source-coefficient plan cache:
    KEPT. Same-worker RCH remote `vmi1227854` criterion baseline after
    pass 11 was `raptorq_encode_group_16blocks` `[581.29,607.97,637.92] us`,
    `raptorq_decode_group_16blocks` `[586.47,604.08,621.02] us`, and
    `raptorq_decode_group_owned_symbols_16blocks`
    `[572.97,594.06,618.04] us`. The one-lever bounded plan cache keyed by
    `(source_count, seed)` changed the same command on the same worker to
    `raptorq_encode_group_16blocks` `[227.29,237.23,247.67] us`
    (2.56x midpoint, 2.35x conservative), `raptorq_decode_group_16blocks`
    `[263.52,276.91,293.75] us` (2.18x midpoint, 2.00x conservative), and
    `raptorq_decode_group_owned_symbols_16blocks`
    `[286.70,297.63,311.63] us` (2.00x midpoint, 1.84x conservative).
    Isomorphism: repair ESI order remains `K..K+repair_count-1`, corrupt
    output ordering and duplicates are unchanged, direct full-rank row
    tie-breaking is unchanged because row selection is unchanged, floating
    point N/A, RNG unchanged via `repair_seed`, cache poisoning falls back to
    fresh encoder construction, cached coefficient bytes were tested against a
    fresh uncached identity-source `SystematicEncoder`, encode golden SHA256
    stayed `c862675bccc157b71044085c51d2a4c1d0d41fe9edf95fac861d79142d81e759`,
    and decode golden stdout SHA256 for the current output block was
    `b561caffac6a560c1324846066dd1f8bee5bad9ed85f29da2c68190179e5fcd8`.
    Gates: `cargo fmt --package ffs-repair --check`, RCH remote
    `vmi1227854` `cargo test -p ffs-repair -- --nocapture`, RCH remote
    `vmi1227854` `cargo check -p ffs-repair --all-targets`, and RCH remote
    `vmi1227854` `cargo clippy -p ffs-repair --all-targets -- -D warnings`.
    Score 7.7 (Impact 2.56 x Confidence 0.9 / Effort 0.3).
13. `bd-xmh5g.150` ffs-repair RaptorQ source-coefficient row cache:
    REJECTED, no runtime code kept. The post-pass-12 baseline on RCH remote
    `vmi1227854` was `raptorq_encode_group_16blocks`
    `[227.29,237.23,247.67] us`, `raptorq_decode_group_16blocks`
    `[263.52,276.91,293.75] us`, and
    `raptorq_decode_group_owned_symbols_16blocks`
    `[286.70,297.63,311.63] us`. Candidate cached computed coefficient rows
    by `(source_count, seed, ESI)` and shared them as immutable slices; the
    focused row-cache oracle passed on `vmi1227854`, proving cached rows matched
    fresh uncached `repair_symbol_into` output. Same-worker RCH rebench
    regressed the target rows: encode `[301.17,317.59,333.59] us`, borrowed
    decode `[303.58,311.43,320.57] us`, and owned decode
    `[295.19,307.24,317.39] us`. Score 0.0 because the measured impact was
    negative. Shipped-state isomorphism is exact because the runtime source was
    restored to the pass-12 code: ESI order, repair equations, direct
    row-selection tie-breaking, corrupt output ordering/duplicates, floating
    point behavior (N/A), and RNG seed derivation are unchanged.
14. `bd-xmh5g.151` ffs-repair direct RaptorQ decode output-buffer move:
    REJECTED, no runtime code kept. A fresh pre-edit RCH baseline command on
    remote `vmi1227854` completed for the restored source but its stdout was
    lost when the local wrapper wait loop was terminated; scoring used the
    immediately preceding same-worker current-source baseline from pass 13:
    `raptorq_encode_group_16blocks` `[227.29,237.23,247.67] us`,
    `raptorq_decode_group_16blocks` `[263.52,276.91,293.75] us`, and
    `raptorq_decode_group_owned_symbols_16blocks`
    `[286.70,297.63,311.63] us`. The one-lever candidate moved solved direct
    decode buffers into output blocks when corrupt output order exactly matched
    unique corrupt index order, falling back to the existing clone path for
    duplicate or reordered corrupt requests. Candidate behavior checks passed:
    `cargo fmt --package ffs-repair --check`, RCH remote `vmi1227854`
    `decode_two_corrupt_blocks_uses_direct_small_erasure_path`, RCH remote
    `vmi1227854`
    `decode_direct_small_erasure_preserves_duplicate_output_order`, and RCH
    remote `vmi1227854` `raptorq_decode_golden_report -- --nocapture` with
    decode golden stdout sha256
    `5e11bf11a354338c346ceffaa88d681e03a0cd794bd4ffb280f31a14dc58608f`.
    Same-worker RCH rebench on `vmi1227854` changed encode to
    `[257.21,273.69,285.66] us`, borrowed decode to
    `[294.72,303.11,312.98] us`, owned decode to
    `[279.06,286.39,294.64] us`, and no-corruption decode to
    `[197.88,200.38,202.79] ns`. The owned-symbol row improved 1.039x by
    midpoint, but the borrowed decode row regressed 9.5% and the encode sanity
    row regressed 15.4%; Score 0.0 because the measured impact was mixed and
    not a keeper. Shipped-state isomorphism is exact because the runtime
    source was restored to the pass-12/pass-13 code: corrupt output ordering
    and duplicate materialization remain clone-based, direct row-selection
    tie-breaking is unchanged, floating point behavior is N/A, RNG seed
    derivation remains `repair_seed`, and no runtime code is kept.
15. `bd-xmh5g.152` ffs-repair LRC 8-parity tuple-matrix chunk kernel:
    REJECTED, no runtime code kept. Fresh post-pass-14 RCH profile on remote
    `vmi1227854` targeted `lrc_encode_global_64blocks_8parity` as the largest
    real codec row at `[369.74,402.39,432.14] us`; nearby rows were
    `raptorq_decode_group_owned_symbols_16blocks` `[279.40,290.09,300.74] us`,
    `raptorq_decode_group_16blocks` `[264.89,272.91,281.29] us`, and
    `raptorq_encode_group_16blocks` `[241.08,251.73,262.94] us`. The
    one-lever candidate added a cached GF(256) source-byte -> 8-parity tuple
    plan and chunked byte-range encoder for the 8-global-parity LRC kernel,
    falling back to the existing parity-major path for other parity counts.
    Candidate behavior proof passed before rejection: RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair global -- --nocapture` ran 16 focused
    tests, including the optimized 8-parity oracle against the parity-major
    reference and the 8-parity golden report. Candidate isomorphism: global
    parity output order remained `j=0..p-1`, data-block contribution semantics
    remained the same GF(256) coefficient matrix over `i=0..k-1` with exact XOR
    addition, tie-breaking N/A, floating point N/A, RNG N/A; the RCH-verified
    8-parity golden block sha256 was
    `62d1eebaddea30e8f11f4aa3eb90614ebc5c462ab091f3580daca60865d671de`.
    Same-worker RCH rebench on `vmi1227854` regressed the target row to
    `[1.0364,1.1328,1.2670] ms`. Score 0.0 because the measured impact was
    negative. Shipped-state isomorphism is exact because `lrc.rs` was restored
    to the pass-14 source: parity-major Rayon row order, coefficient generation,
    output bytes, floating point behavior (N/A), and RNG behavior (N/A) are
    unchanged.

## Next Primitive
Do not repeat RaptorQ coefficient-row memoization, direct decode output-buffer
materialization, or LRC source-byte tuple-table chunking. Re-profile with
crate-scoped RCH before pass 16 and attack a different algebraic primitive:
either a small-matrix GF(256) inverse/solve specialization in `repair_global`
or an LRC generator-coefficient recurrence that avoids modulo/exponent lookup
without changing the existing parity-major parallel work layout.

## Current Pass 16
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity`.
- Profile source: pass-15 RCH remote `vmi1227854` profile showed the LRC global
  encode row as the largest remaining real codec row at midpoint `402.39 us`
  before the rejected tuple-matrix chunk kernel.
- Alien primitive: retain LRC/RS dense GF(256) matrix-vector semantics, but
  remove redundant generator coefficient modulo/exponent lookup work inside the
  parity-major layout.
- Baseline: RCH remote `vmi1227854` `cargo bench -j 1 -p ffs-repair --bench
  scrub_codec -- lrc_encode_global_64blocks_8parity --noplot --sample-size 10
  --warm-up-time 1 --measurement-time 2` reported
  `[301.17 us, 344.08 us, 412.54 us]`.
- Change: kept the parity-major work layout and derived each parity row's
  generator coefficient progression recurrently with GF(256) multiplication.
- Isomorphism proof: parity block order `j=0..p-1` unchanged; data contribution
  order `i=0..k-1` unchanged; coefficient recurrence oracle matches the
  closed-form `alpha^((i+1)*(j+1))` formula for all allowed 255x255 indices;
  floating-point N/A; RNG N/A.
- Golden proof: RCH remote `cargo test -j 1 -p ffs-repair global --
  --nocapture --test-threads=1` passed 15 focused tests, including
  `lrc_global_parity_golden_report`; golden stdout SHA256
  `83143f5328208954330f8bd65454ef0e323bd4512833d9a966bf8edcbe3e31a3`.
- Gates: `cargo fmt --check --package ffs-repair`; RCH remote `vmi1227854`
  `cargo test -j 1 -p ffs-repair global -- --nocapture --test-threads=1`;
  RCH remote `cargo check -j 1 -p ffs-repair --all-targets`; RCH remote
  `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings`.
- Rebench: same-worker RCH remote `vmi1227854` after row
  `[232.49 us, 256.94 us, 296.73 us]`; midpoint speedup `1.34x`, intervals
  non-overlapping.
- Score: kept. Impact 2.7 x Confidence 3.0 / Effort 1.0 = 8.1.

## Post-Pass 16 Profile
- Command: RCH remote `vmi1227854` `cargo bench -j 1 -p ffs-repair --bench
  scrub_codec -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time
  2`.
- Full-suite rows after `bd-xmh5g.153`: `lrc_encode_global_64blocks_8parity`
  `[493.31 us, 565.16 us, 669.24 us]`;
  `raptorq_decode_group_owned_symbols_16blocks`
  `[363.10 us, 368.34 us, 372.69 us]`;
  `raptorq_decode_group_16blocks` `[254.27 us, 263.96 us, 281.08 us]`;
  `raptorq_encode_group_16blocks` `[250.53 us, 262.89 us, 273.23 us]`;
  `scrub_corrupted_256blocks` `[75.014 us, 77.980 us, 80.277 us]`;
  `scrub_clean_256blocks` `[76.891 us, 80.561 us, 85.034 us]`.
- `scrub_buffer_prep/old_zero_per_batch` still appears at
  `[371.10 us, 381.42 us, 387.48 us]`, but it is an A/B legacy comparator;
  the active `new_reuse_pool` row is `[30.608 us, 31.477 us, 32.096 us]`.
- Routing note: the full-suite LRC row conflicts with the focused same-worker
  after row `[232.49 us, 256.94 us, 296.73 us]`, so re-baseline LRC in
  isolation before attacking it again. Excluding A/B comparators and the
  just-touched noisy LRC row, the largest stable residual is now
  `raptorq_decode_group_owned_symbols_16blocks`. Do not repeat the rejected
  RaptorQ source-row cache or direct output-buffer move families; next pass
  should look for a different decode primitive, likely around small GF(256)
  system solve/layout or owned-symbol construction semantics.

## Current Pass 17
- Bead: `bd-ifkbb`.
- Target: `ffs-repair` `raptorq_decode_group_owned_symbols_16blocks`.
- Profile source: post-pass-16 RCH remote `vmi1227854` profile showed owned
  RaptorQ decode as the largest stable residual after excluding A/B legacy
  comparators and the just-touched noisy LRC row:
  `[363.10 us, 368.34 us, 372.69 us]`.
- Rejected-family exclusions: do not repeat RaptorQ source-coefficient row
  memoization, direct decode output-buffer materialization, or LRC
  source-byte tuple-table chunking.
- Required first step: fresh crate-scoped RCH baseline before editing.
- Candidate route: find a different coding-theory/data-layout primitive around
  owned repair-symbol construction, direct-decode setup, or small GF(256)
  system solve layout while preserving repair equations and repair-symbol input
  order.
- Lever kept: owned direct small-erasure decode now moves repair payload buffers
  into direct residual rows instead of cloning them, with inline source
  coefficient metadata and exact fallback restoration if the direct path cannot
  solve.
- Same-worker RCH baseline: `ovh-a`
  `raptorq_decode_group_owned_symbols_16blocks`
  `[318.83 us, 415.16 us, 493.13 us]`.
- Same-worker RCH after: `ovh-a` job `29879662679164292`
  `raptorq_decode_group_owned_symbols_16blocks`
  `[267.19 us, 267.78 us, 268.32 us]`.
- Median speedup: `1.55x`. Score: `4.5 = impact 3 x confidence 3 / effort 2`;
  kept.
- Isomorphism proof:
  - Ordering preserved: yes; `recover_from_direct_solution` still emits in the
    caller's `corrupt_indices` order, including duplicates.
  - Tie-breaking unchanged: yes; direct row selection still scans repair rows in
    input order and uses the same full-rank pair predicate.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; `repair_seed` and repair ESI equations are
    unchanged.
  - Goldens verified: RCH `ovh-a`
    `raptorq_decode_golden_report` marker-bounded SHA256
    `f751dab0a92e1476c2b57a94884162005cdf5fc1cdffc99f260c1aefad60f5c5`.
- Gates:
  - `cargo fmt --package ffs-repair --check`
  - `git diff --check -- crates/ffs-repair/src/codec.rs`
  - RCH `ovh-a`: `cargo test -j 1 -p ffs-repair --lib decode_owned_direct_small_erasure_preserves_duplicate_output_order -- --nocapture`
  - RCH `ovh-a`: `cargo test -j 1 -p ffs-repair decode -- --nocapture`
  - RCH `ovh-a`: `cargo test -j 1 -p ffs-repair raptorq_decode_golden_report -- --nocapture --test-threads=1`
  - RCH `ovh-a`: `cargo check -j 1 -p ffs-repair --all-targets`
  - RCH `ovh-a`: `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings`

## Current Pass 18
- Bead: `bd-xmh5g.154`.
- Target: `ffs-alloc` `bitmap_largest_free_run` over fragmented mixed-byte
  block bitmaps.
- Profile source: `bd-xmh5g` notes identified `bitmap_largest_free_run` as a
  remaining byte/halfword scan residual with Criterion coverage in
  `crates/ffs-alloc/benches/bitmap_ops.rs`.
- Lever kept: replaced the 65,536-entry halfword zero-run table path with a
  safe-Rust word-level bit-run detector. The new path preserves the existing
  all-zero/all-one fast exits, carries the preceding run through the word
  prefix with `trailing_zeros`, records the internal best run with repeated
  `free &= free << 1`, and carries the suffix with `leading_zeros`.
- Baseline and rebench: RCH remote `vmi1227854`
  `cargo bench -j 1 -p ffs-alloc --bench bitmap_ops -- largest_free_run_ab
  --noplot --sample-size 20 --warm-up-time 1 --measurement-time 2` compared
  the old halfword-table scan against the new production `bitmap_largest_free_run`
  in one binary. Old `largest_free_run_ab/old_halfword_table`
  `[8.4871 us, 8.8823 us, 9.3413 us]`; new
  `largest_free_run_ab/word_run_detector`
  `[1.6413 us, 1.7703 us, 1.9347 us]`. Midpoint speedup `5.02x`;
  conservative lower/upper speedup `4.39x`.
- Score: kept. Impact 5.0 x Confidence 0.85 / Effort 0.7 = 6.1.
- Isomorphism proof:
  - Ordering preserved: yes; bits are still consumed in little-endian
    byte/word order, and cross-word runs use the same prefix/suffix carry.
  - Tie-breaking unchanged: N/A for this API because it returns only the max
    run length, not a run position.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Goldens verified: local crate-scoped golden marker SHA256
    `4d3300c701fc62e241ad715f89ccf4701ecb6b72ced820ca0922cc8a18dbf9b0`.
    Two RCH remote `vmi1227854` golden-test attempts were canceled by the RCH
    stuck detector before the test body and are not counted as golden proof.
- Gates:
  - `cargo fmt --check --package ffs-alloc`
  - `git diff --check -- crates/ffs-alloc/src/lib.rs crates/ffs-alloc/benches/bitmap_ops.rs`
  - RCH remote `vmi1227854`: `cargo bench -j 1 -p ffs-alloc --bench bitmap_ops -- largest_free_run_ab --noplot --sample-size 20 --warm-up-time 1 --measurement-time 2`
  - local crate-scoped: `cargo test -j 1 -p ffs-alloc bitmap_largest_free_run -- --nocapture`
  - local crate-scoped: `cargo check -j 1 -p ffs-alloc --all-targets`
  - local crate-scoped: `cargo clippy -j 1 -p ffs-alloc --all-targets -- -D warnings`

## Current Pass 19
- Bead: `bd-xmh5g.155`.
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity`.
- Profile source: post-pass-16 full-suite RCH remote `vmi1227854` showed
  `lrc_encode_global_64blocks_8parity` at `[493.31 us,565.16 us,669.24 us]`
  but the prior focused after-row was `[232.49 us,256.94 us,296.73 us]`, so
  this pass first re-baselined LRC in isolation before editing.
- Rejected-family exclusions honored: did not repeat the rejected LRC
  source-byte tuple-table chunk kernel, source-major/panel layout, RaptorQ
  source-row memoization, or direct decode output-buffer materialization.
- Baselines: same-worker RCH remote `vmi1227854`
  `cargo bench -j 1 -p ffs-repair --bench scrub_codec --
  lrc_encode_global_64blocks_8parity --noplot --sample-size 10
  --warm-up-time 1 --measurement-time 2` reported
  `[903.20 us,969.97 us,1.0293 ms]`; confirmatory isolated baseline on the
  same worker reported `[820.29 us,1.0009 ms,1.1859 ms]`.
- Lever kept: fused each adjacent pair of source-block GF(256) projections
  inside the existing parity-major row layout. Each pair now scans the
  destination parity block once and applies `lhs_coeff*lhs ^ rhs_coeff*rhs`;
  an odd final source block falls back to the existing single-source helper.
- Rebench: same-worker RCH remote `vmi1227854` after row
  `[373.54 us,391.82 us,406.45 us]`. Midpoint speedup against the
  confirmatory isolated baseline is `2.55x`; midpoint speedup against the first
  isolated baseline is `2.48x`; conservative interval speedup is at least
  `2.02x` (`820.29 us / 406.45 us`).
- Score: kept. Impact 2.48 x Confidence 3.0 / Effort 1.0 = 7.4.
- Isomorphism proof:
  - Ordering preserved: yes; Rayon still collects parity rows in `j=0..p-1`
    order, and the source stream is consumed as consecutive pairs followed by
    the same odd-block remainder path.
  - Source contribution semantics unchanged: yes; coefficients still advance by
    the existing `coeff = gf256::mul(coeff, step)` recurrence, so pair `(i,i+1)`
    contributes exactly the same two GF(256) matrix terms the old two passes
    contributed.
  - XOR exactness: yes; `gf256_mul_xor_pair_matches_separate_passes` compares
    fused and separate-pass results across zero, identity, and nontrivial
    coefficients and lengths including empty, odd, cache-line-ish, and 4096.
  - Tie-breaking unchanged: N/A for encode-only parity generation.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Goldens verified: RCH remote `vmi1227854` full `ffs-repair` lib test passed
    444 tests; marker-bounded LRC golden stdout SHA256
    `9968a12c3b5c77999bd21b612756d65b0f40841deb23de7801525c561c5f53d0`.
- Gates:
  - `cargo fmt --check --package ffs-repair`
  - RCH remote `vmi1227854`: `cargo test -j 1 -p ffs-repair --lib -- --nocapture --test-threads=1`
  - RCH remote `vmi1227854`: `cargo bench -j 1 -p ffs-repair --bench scrub_codec -- lrc_encode_global_64blocks_8parity --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2`
  - RCH remote `vmi1227854`: `cargo check -j 1 -p ffs-repair --all-targets`
  - RCH remote `vmi1227854`: `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings`
- Next route: re-profile `ffs-repair` after this keep. If LRC is no longer the
  top real codec row, route to the next profile-backed RaptorQ decode or
  small-GF solve/layout primitive rather than another LRC row micro-lever.

## Current Pass 20
- Bead: `bd-0ewh3`.
- Target: `ffs-core` Btrfs delete path, specifically the
  `btrfs_remove_named_dir_index` full directory scan used by unlink and rename.
- Profile source: `[perf]` bead `bd-0ewh3`, filed as the follow-on to
  `bd-cjgab`. The old path ranged every `DIR_INDEX` in a parent directory and
  parsed payloads by name, even though the matching child `INODE_REF` already
  carries the monotonic index sequence that keys the exact `DIR_INDEX` item.
- Lever kept: threaded the `INODE_REF` index through unlink, rename, and
  `RENAME_EXCHANGE`, then changed `btrfs_remove_dir_entry` to load and validate
  the exact `DIR_INDEX` key before removing it. The old `DIR_ITEM` point-query
  path remains unchanged.
- Baseline: same-worker RCH remote `vmi1227854`
  `cargo bench -j 1 -p ffs-btrfs --bench readdir_dedup --
  btrfs_remove_named_dir_index --noplot --sample-size 20 --warm-up-time 1
  --measurement-time 2` reported old full scan
  `[188.42 us,189.15 us,190.03 us]` and point query
  `[165.18 ns,169.71 ns,172.95 ns]`.
- Rebench: same-worker RCH remote `vmi1227854` after production edit reported
  full scan `[138.93 us,155.22 us,173.95 us]` and point query
  `[206.69 ns,208.34 ns,209.91 ns]`.
- Score: kept. Midpoint gap on the rebench is `745x`; conservative lower/upper
  gap is `662x`. Impact 5.0 x Confidence 0.95 / Effort 0.3 = 15.8.
- Isomorphism proof:
  - Ordering preserved: yes; directory entry lookup still resolves the same
    name first, and deletion still removes the matching `DIR_ITEM`,
    `DIR_INDEX`, and `INODE_REF` for that name.
  - Tie-breaking unchanged: yes for valid Btrfs state; the `DIR_INDEX` offset
    is the exact sequence recorded at link time in the matching `INODE_REF`.
    The new path validates the payload name at that key before mutation, so it
    does not delete a different entry if metadata is corrupt.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Goldens verified: N/A for this non-golden path; RCH behavior tests covered
    Btrfs unlink, rename, hard-link, fallocate, and write cases.
- Gates:
  - `rustfmt --edition 2024 --check crates/ffs-core/src/lib.rs crates/ffs-btrfs/benches/readdir_dedup.rs`
  - `git diff --check -- crates/ffs-core/src/lib.rs crates/ffs-btrfs/benches/readdir_dedup.rs`
  - RCH remote `vmi1227854`: `cargo bench -j 1 -p ffs-btrfs --bench readdir_dedup -- btrfs_remove_named_dir_index --noplot --sample-size 20 --warm-up-time 1 --measurement-time 2`
  - RCH remote `vmi1227854`: `cargo clippy -j 1 -p ffs-core --all-targets -- -D warnings`
  - RCH remote `vmi1227854`: `cargo clippy -j 1 -p ffs-btrfs --all-targets -- -D warnings`
  - RCH remote `vmi1227854`: `cargo test -j 1 -p ffs-core --lib btrfs_write -- --nocapture --test-threads=1` (121 passed)
- Gate cleanup: first `ffs-core` clippy run exposed a pre-existing
  `needless_late_init` warning in `btrfs_write`; fixed mechanically by making
  `nbytes_after_write` the value of the existing inline/non-inline branch, then
  reran clippy and the focused Btrfs write tests cleanly.

## Current Pass 21
- Bead: `bd-xmh5g.156`.
- Target: `ffs-repair` `raptorq_decode_group_owned_symbols_16blocks`.
- Profile source: fresh same-worker RCH remote `vmi1227854`
  `cargo bench -j 1 -p ffs-repair --bench scrub_codec -- --noplot
  --sample-size 10 --warm-up-time 1 --measurement-time 2` before editing.
  Excluding the legacy A/B `old_zero_per_batch` comparator, the largest active
  row was `raptorq_decode_group_owned_symbols_16blocks`
  `[386.65 us,396.58 us,403.48 us]`; nearby rows were borrowed decode
  `[347.81 us,374.22 us,419.92 us]`, LRC global encode
  `[271.16 us,293.56 us,318.08 us]`, and RaptorQ encode
  `[275.27 us,293.69 us,314.04 us]`.
- Lever kept: `direct_solution_satisfies_all_repairs` now validates all direct
  repair rows with one reusable `expected` scratch buffer sized from the first
  row, instead of allocating and zeroing a fresh vector for every row. The
  scratch is reset with `fill(0)` before each row and all solved/residual
  lengths are checked before validation begins.
- Rebench: same-worker RCH remote `vmi1227854` after row
  `raptorq_decode_group_owned_symbols_16blocks`
  `[280.81 us,287.14 us,292.70 us]`. Midpoint speedup is `1.38x`; conservative
  lower/upper speedup is `1.32x` (`386.65 us / 292.70 us`). Borrowed decode
  also improved from `[347.81 us,374.22 us,419.92 us]` to
  `[278.29 us,301.86 us,326.06 us]` (`1.24x` midpoint).
- Score: kept. Impact 1.38 x Confidence 3.0 / Effort 1.0 = 4.1.
- Isomorphism proof:
  - Ordering preserved: yes; recovered corrupt block output order and duplicate
    handling are unchanged because validation only checks the already-solved
    direct rows before the existing recovery path.
  - Tie-breaking unchanged: yes; direct row selection and full-rank repair-row
    scan order are unchanged. The same selected rows are accepted or rejected
    by the same GF(256) equations.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; `repair_seed`, ESI selection, and coefficient
    generation are unchanged.
  - Goldens verified: RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair decode -- --nocapture --test-threads=1`
    passed 20 decode-focused tests; marker-bounded
    `RAPTORQ_DECODE_GOLDEN` stdout SHA256
    `f751dab0a92e1476c2b57a94884162005cdf5fc1cdffc99f260c1aefad60f5c5`.
- Gates:
  - `cargo fmt --check --package ffs-repair`
  - `git diff --check`
  - RCH remote `vmi1227854`: `cargo test -j 1 -p ffs-repair decode -- --nocapture --test-threads=1`
  - RCH remote `vmi1227854`: `cargo bench -j 1 -p ffs-repair --bench scrub_codec -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2`
  - RCH remote `vmi1227854`: `cargo check -j 1 -p ffs-repair --all-targets`
  - RCH remote `vmi1227854`: `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings`
- Artifact manifest: `artifacts/perf/pass21/evidence_sha256.txt`.

## Current Pass 22
- Bead: `bd-xmh5g.157`.
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity`.
- Profile source: fresh focused RCH baseline after pass 21. Initial RCH remote
  `ovh-a` row was `[702.23 us,786.07 us,862.41 us]`; after `ovh-a` became
  pressure-blocked, the comparable same-worker run used clean old-code and
  candidate measurements on RCH remote `vmi1227854`.
- Lever rejected: widened the existing adjacent-pair GF(256) projection in
  `encode_global` to a four-source fused projection while preserving the
  parity-major Rayon row layout and coefficient recurrence.
- Comparable baseline: clean old-code detached worktree on RCH remote
  `vmi1227854` reported `[253.42 us,275.59 us,305.81 us]`.
- Candidate rebench: same-worker RCH remote `vmi1227854` reported
  `[264.17 us,280.52 us,295.66 us]`.
- Score: rejected. Midpoint speedup was `0.982x`; conservative lower/upper was
  `0.857x`. Impact was negative/noisy, so Score `0.0`; no runtime code kept.
- Isomorphism proof:
  - Ordering preserved: yes for the candidate; parity rows remained `j=0..p-1`
    and source blocks contributed in increasing source order.
  - Tie-breaking unchanged: N/A for encode-only parity generation.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Goldens verified: RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair --lib lrc -- --nocapture --test-threads=1`
    passed 50 tests; marker-bounded LRC golden stdout SHA256
    `84e1fd0632220fbbba4154b19448e06ade4c648c724171b5e41c131242a2df8b`.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/lrc.rs` was restored
  to the pre-lever source before closeout.
- Gates:
  - `rustfmt --edition 2024 --check crates/ffs-repair/src/lrc.rs`
  - `git diff --check -- crates/ffs-repair/src/lrc.rs`
  - RCH remote `vmi1227854`: `cargo test -j 1 -p ffs-repair --lib lrc -- --nocapture --test-threads=1`
  - RCH remote `vmi1227854`: clean old-code and candidate Criterion runs for
    `lrc_encode_global_64blocks_8parity`
- Artifact manifest: `artifacts/perf/bd-xmh5g.157/`.

## Current Pass 23
- Bead: `bd-xmh5g.158`.
- Target: `ffs-repair` two-erasure RaptorQ direct small-erasure solve inside
  `raptorq_decode_group_16blocks` and
  `raptorq_decode_group_owned_symbols_16blocks`.
- Profile source: fresh focused RCH remote `vmi1227854` baseline on clean
  origin before editing:
  - `raptorq_decode_group_16blocks`
    `[248.90 us,268.14 us,286.17 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[279.60 us,313.22 us,351.63 us]`
  - `raptorq_decode_group_no_corruption_16blocks`
    `[193.61 ns,199.85 ns,206.32 ns]`
- Lever rejected: fused the determinant inverse into the four GF(256) addmul
  coefficients in `solve_two_erasures`, removing two full-block
  `gf256_mul_slice` passes after residual combination.
- Candidate rebench: same-worker RCH remote `vmi1227854`:
  - borrowed decode regressed to `[298.02 us,327.61 us,355.55 us]`
  - owned-symbol decode improved to `[271.68 us,282.96 us,301.42 us]`
  - no-corruption fast path regressed to `[202.01 ns,207.40 ns,212.88 ns]`
- Score: rejected. The owned-symbol row improved, but the borrowed row is part
  of the same public decode surface and regressed by `0.82x` midpoint; the
  no-corruption fast path also regressed. Score `0.0`; no runtime code kept.
- Isomorphism proof for the candidate:
  - Ordering preserved: yes; the candidate only changed algebraic scaling after
    row selection and before the existing recovery path.
  - Tie-breaking unchanged: yes; selected repair rows, fallback scan order, and
    validation row set were unchanged.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; `repair_seed`, ESI order, and coefficient
    generation were unchanged.
  - Goldens/behavior verified: RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair direct_small_erasure -- --nocapture
    --test-threads=1` passed 3 tests. A focused local-fallback RCH algebra test
    also passed before the remote gate, but only the remote direct-erasure test
    is counted as a gate.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/codec.rs` was
  restored to the pre-lever source before closeout.
- Gates:
  - `cargo fmt --package ffs-repair --check`
  - `git diff --check`
  - RCH remote `vmi1227854`: clean baseline and candidate Criterion runs for
    `raptorq_decode_group*`
  - RCH remote `vmi1227854`: direct small-erasure behavior tests
- Artifact manifest: `artifacts/perf/bd-xmh5g.158/`.
- Next route: do not repeat post-solve determinant-scaling micro-levers.
  Re-profile the restored tree and attack a different RaptorQ primitive, likely
  residual projection/validation structure rather than scaling already-solved
  blocks.

## Current Pass 24
- Bead: `bd-xmh5g.159`.
- Target: `ffs-repair` direct small-erasure setup in
  `raptorq_decode_group_16blocks` and
  `raptorq_decode_group_owned_symbols_16blocks`.
- Profile source: restored clean-code same-worker RCH remote `vmi1227854`
  baseline:
  - `raptorq_decode_group_16blocks`
    `[249.69 us,265.16 us,278.92 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[251.75 us,291.60 us,319.61 us]`
  - `raptorq_decode_group_no_corruption_16blocks`
    `[195.86 ns,199.44 ns,203.05 ns]`
- Lever rejected: delayed `InactivationDecoder` construction until after the
  direct small-erasure attempt and replaced direct-path repair-ESI validation
  with the equivalent cheap `ESI + (K' - K)` overflow guard used by
  `SystematicParams::rfc_repair_equation`.
- Candidate behavior proof before rejection: RCH remote `vmi1227854`
  `cargo test -j 1 -p ffs-repair decode -- --nocapture --test-threads=1`
  passed 21 decode-focused tests, including the temporary invalid repair ESI
  fallback test and `raptorq_decode_golden_report`.
- First candidate rebench on same-worker RCH remote `vmi1227854` was mixed:
  - borrowed decode `[252.69 us,273.25 us,287.86 us]`
  - owned-symbol decode `[243.37 us,258.73 us,277.36 us]`
  - no-corruption fast path `[196.97 ns,200.65 ns,204.15 ns]`
- Confirmatory same-worker RCH remote `vmi1227854` rebench rejected the lever:
  - borrowed decode regressed to `[329.33 us,338.01 us,346.40 us]`
  - owned-symbol decode was effectively neutral/noisy at
    `[271.24 us,290.88 us,311.87 us]`
  - no-corruption fast path regressed to `[205.94 ns,207.01 ns,208.48 ns]`
- Score: rejected. The public borrowed decode row regressed `0.78x` by
  midpoint versus baseline and the no-corruption row also regressed. Score
  `0.0`; no runtime code kept.
- Isomorphism proof for the candidate:
  - Ordering preserved: yes; corrupt output ordering and duplicate
    materialization remained in the existing recovery path.
  - Tie-breaking unchanged: yes; repair row scan order and full-rank selection
    were unchanged.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; `repair_seed`, repair ESI order, and coefficient
    generation were unchanged.
  - Golden/behavior verified: decode-focused RCH test gate passed before the
    rejection decision.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/codec.rs` was
  restored to the pre-lever source before closeout.
- Gates:
  - `cargo fmt --package ffs-repair --check`
  - `git diff --check -- crates/ffs-repair/src/codec.rs .beads/issues.jsonl`
  - RCH remote `vmi1227854`: decode-focused behavior tests
  - RCH remote `vmi1227854`: clean baseline and two candidate Criterion runs
    for `raptorq_decode_group*`
- Artifact manifest: `artifacts/perf/bd-xmh5g.159/`.
- Next route: do not repeat decoder-construction deferral or ESI-validation
  micro-levers. Re-profile the restored tree and attack a deeper RaptorQ
  primitive, likely source/repair coefficient projection or direct known-source
  residual structure.

## Current Pass 25
- Bead: `bd-xmh5g.160`.
- Target: `ffs-repair` direct small-erasure validation in
  `raptorq_decode_group_16blocks` and
  `raptorq_decode_group_owned_symbols_16blocks`.
- Profile source: fresh restored-source RCH profile initially selected
  `ovh-a` despite the `vmi1227854` preference; those rows were treated as
  routing/profile evidence only:
  - `raptorq_encode_group_16blocks`
    `[250.15 us,251.62 us,252.49 us]`
  - `raptorq_decode_group_16blocks`
    `[261.49 us,263.75 us,266.16 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[263.90 us,264.99 us,266.51 us]`
  - `lrc_encode_global_64blocks_8parity`
    `[211.04 us,219.35 us,226.04 us]`
- Comparable baseline: restored clean-code same-worker RCH remote `vmi1227854`
  from pass 24:
  - `raptorq_decode_group_16blocks`
    `[249.69 us,265.16 us,278.92 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[251.75 us,291.60 us,319.61 us]`
  - `raptorq_decode_group_no_corruption_16blocks`
    `[195.86 ns,199.44 ns,203.05 ns]`
- Lever rejected: after one/two-erasure direct solve, skipped recomputing the
  expected bytes for the selected pivot repair rows while still checking all row
  lengths and all non-selected repair rows.
- Candidate behavior proof before rejection:
  - RCH remote `vmi1227854`
    `cargo check -j 1 -p ffs-repair --all-targets` passed.
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair direct -- --nocapture` passed 6 focused
    direct-decode tests, including a temporary oracle proving non-selected rows
    still reject.
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair raptorq -- --nocapture` passed 3 RaptorQ
    golden tests.
  - RCH remote `vmi1227854`
    `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings` passed.
- Golden proof: normalized marker-bounded RaptorQ golden stdout matched prior
  committed artifacts exactly: decode SHA256
  `5e11bf11a354338c346ceffaa88d681e03a0cd794bd4ffb280f31a14dc58608f`, encode
  SHA256 `7b53276f2759953be509118ba260c659ed8c55eec7f8f6a2537c80dcbe2e033a`.
- Candidate rebench: same-worker RCH remote `vmi1227854`
  `cargo bench -j 1 -p ffs-repair --bench scrub_codec --
  raptorq_decode_group --noplot --sample-size 10 --warm-up-time 1
  --measurement-time 2`:
  - borrowed decode regressed to `[366.80 us,417.31 us,494.07 us]`
  - owned-symbol decode regressed to `[341.33 us,361.07 us,383.19 us]`
  - no-corruption fast path regressed to `[209.68 ns,216.63 ns,231.05 ns]`
- Score: rejected. Borrowed decode regressed `0.64x` by midpoint versus the
  same-worker baseline, owned-symbol decode regressed `0.81x`, and the
  no-corruption row also regressed. Score `0.0`; no runtime code kept.
- Isomorphism proof for the candidate:
  - Ordering preserved: yes; corrupt output ordering and duplicate
    materialization remained in the existing recovery path.
  - Tie-breaking unchanged: yes; selected repair row scan order and full-rank
    pair selection were unchanged.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; `repair_seed`, repair ESI order, and coefficient
    generation were unchanged.
  - Golden/behavior verified before rejection as above.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/codec.rs` was
  restored to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.160/`.
- Next route: do not repeat selected-row validation pruning,
  decoder-construction deferral, ESI-validation shortcuts, determinant scaling,
  row caching, or output-buffer materialization. Re-profile the restored tree
  and attack a deeper RaptorQ primitive such as streamed selected-row residual
  materialization or multi-row source projection, with the baseline and after
  runs pinned to an actual same worker.

## Current Pass 26
- Bead: `bd-xmh5g.161`.
- Target: `ffs-repair` direct small-erasure known-source projection in
  `raptorq_decode_group_16blocks` and
  `raptorq_decode_group_owned_symbols_16blocks`.
- Profile source: fresh focused RCH remote `vmi1227854` baseline before editing:
  - `raptorq_decode_group_16blocks`
    `[277.19 us,307.67 us,347.26 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[269.93 us,285.61 us,300.02 us]`
  - `raptorq_decode_group_no_corruption_16blocks`
    `[191.62 ns,195.39 ns,200.09 ns]`
- Lever kept: `read_known_source_symbols` now returns compact intact source
  symbols with their original source indices instead of allocating zero-filled
  placeholder blocks for corrupt indices. Direct row projection keeps total
  `source_count` explicit and projects only the compact intact list.
- Candidate rebench: same-worker RCH remote `vmi1227854`:
  - `raptorq_decode_group_16blocks`
    `[263.11 us,269.96 us,277.69 us]` (`1.14x` midpoint, `1.00x`
    conservative lower/upper)
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[246.01 us,256.55 us,276.10 us]` (`1.11x` midpoint, `0.98x`
    conservative lower/upper)
  - `raptorq_decode_group_no_corruption_16blocks`
    `[186.76 ns,192.94 ns,198.31 ns]` (`1.01x` midpoint)
- Score: kept. Impact 4.0 x Confidence 4.0 / Effort 2.0 = 8.0.
- Isomorphism proof:
  - Ordering preserved: yes; intact source symbols are still read in ascending
    source index order, corrupt output ordering and duplicates are unchanged by
    the existing `recover_from_direct_solution` path, and the golden output
    still emits duplicate corrupt block `5` in request order.
  - Tie-breaking unchanged: yes; repair row scan order, full-rank row
    selection, and fallback order are unchanged. Only the known-source storage
    layout changed.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; `repair_seed`, repair ESI order, and coefficient
    generation are unchanged.
  - Goldens verified: RCH remote `vmi1227854`
    `raptorq_decode_golden_report -- --nocapture --test-threads=1` passed;
    marker-bounded `RAPTORQ_DECODE_GOLDEN` stdout SHA256
    `f751dab0a92e1476c2b57a94884162005cdf5fc1cdffc99f260c1aefad60f5c5`.
  - Residual equivalence verified: RCH remote `vmi1227854`
    `decode_direct_source_major_residuals_match_row_major_reference` passed,
    comparing compact source-major projection against the prior full-list
    row-major reference with corrupt zero placeholders.
- Gates:
  - `cargo fmt --package ffs-repair --check`
  - RCH remote `vmi1227854`: row-major residual proof test
  - RCH remote `vmi1227854`: RaptorQ decode golden report
  - RCH remote `vmi1227854`: same-worker focused Criterion baseline/rebench
  - RCH remote `vmi1227854`: `cargo check -j 1 -p ffs-repair --all-targets`
  - RCH remote `vmi1227854`: `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings`
- Artifact manifest: `artifacts/perf/bd-xmh5g.160/`.
- Next route: re-profile after this compact-layout keep; avoid repeating
  placeholder-skip, row-cache, decoder-construction deferral, ESI-validation,
  determinant-scaling, and output-materialization micro-levers.

## Current Pass 27
- Bead: `bd-xmh5g.162`.
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity`, selected from the
  fresh post-pass-26 RCH profile on `vmi1227854`:
  - `lrc_encode_global_64blocks_8parity`
    `[353.90 us,391.09 us,453.80 us]`
  - `raptorq_encode_group_16blocks`
    `[318.22 us,319.93 us,322.04 us]`
  - `raptorq_decode_group_16blocks`
    `[274.98 us,295.27 us,307.59 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[283.22 us,293.98 us,305.87 us]`
- Focused baseline before editing: RCH remote `vmi1227854`
  `lrc_encode_global_64blocks_8parity`
  `[278.80 us,296.23 us,318.83 us]`.
- Lever rejected: routed small LRC global parity cases (`p <= 8`,
  `block_size <= 4096`) through a sequential scheduler while preserving the
  exact current parity-major block body and Rayon path for larger cases.
- Behavior proof before rejection:
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-repair global -- --nocapture --test-threads=1`
    passed 15 focused global/LRC tests.
  - Marker-bounded `LRC_GLOBAL_PARITY_GOLDEN` SHA256:
    `84e1fd0632220fbbba4154b19448e06ade4c648c724171b5e41c131242a2df8b`.
- Candidate rebench: same-worker RCH remote `vmi1227854`
  `lrc_encode_global_64blocks_8parity`
  `[630.69 us,661.68 us,693.66 us]`.
- Score: rejected. Candidate was `0.45x` by midpoint versus the focused
  baseline and `0.40x` by conservative lower/upper. Score `0.0`; no runtime
  code kept.
- Isomorphism proof for the candidate:
  - Ordering preserved: yes; global parity output order remained `j=0..p-1`.
  - Tie-breaking unchanged: N/A.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - GF coefficient recurrence unchanged: yes; the per-parity body retained the
    same `alpha^((i+1)*(j+1))` recurrence and data-block contribution order.
  - Goldens verified before rejection as above.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/lrc.rs` was restored
  to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.162/`.
- Next route: do not repeat LRC small-parity sequential scheduling,
  source-major global projection, tuple-matrix chunking, or quad-source fusion.
  Re-profile and attack a different primitive, likely RaptorQ encode/decode
  memory layout or a fundamentally different LRC parity kernel rather than
  scheduler reshaping.

## Current Pass 28
- Bead: `bd-xmh5g.163`.
- Target: `ffs-repair`
  `raptorq_decode_group_no_corruption_16blocks` after the pass-26 compact
  known-source projection keep.
- Profile source:
  - Fresh pre-edit RCH remote `vmi1227854` baseline on current `origin/main`:
    `[216.41 ns,217.50 ns,218.29 ns]`.
  - Controlled same-worker RCH remote `vmi1152480` source-state pair:
    baseline `[219.47 ns,247.44 ns,290.49 ns]`, optimized
    `[34.107 ns,36.683 ns,39.784 ns]`.
- Lever kept: `decode_group_impl` returns the empty `DecodeOutcome` before
  computing `block_size`, `k`, and `repair_seed` when `corrupt_indices` is
  empty. Non-empty corruption paths remain below the same branch and are
  unchanged.
- Candidate rebench:
  - Same-worker RCH remote `vmi1152480`: baseline midpoint `247.44 ns`,
    optimized midpoint `36.683 ns` (`6.74x` midpoint, `5.52x` conservative
    lower/upper).
- Score: kept. Impact 5.52 x Confidence 0.95 / Effort 0.25 = 20.9.
- Isomorphism proof:
  - Ordering preserved: yes; empty-corruption decode still returns an empty
    `recovered` vector and `complete=true`, and all non-empty corrupt output
    ordering/duplicates remain in the unchanged lower paths.
  - Tie-breaking unchanged: yes; empty-corruption decode performs no row
    selection, while all non-empty repair row scan and fallback ordering remain
    unchanged.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; the empty path skips only an unused seed
    derivation, and non-empty paths still call `repair_seed(fs_uuid, group)`.
  - Goldens verified: RCH remote `vmi1227854`
    `raptorq_decode_golden_report -- --nocapture` passed; marker-bounded
    `RAPTORQ_DECODE_GOLDEN` stdout SHA256
    `5e11bf11a354338c346ceffaa88d681e03a0cd794bd4ffb280f31a14dc58608f`.
- Gates:
  - `cargo fmt --check -p ffs-repair`
  - `git diff --check`
  - RCH remote `vmi1227854`:
    `cargo test -j 1 -p ffs-repair decode_no_corruption_succeeds -- --nocapture`
  - RCH remote `vmi1227854`:
    `cargo test -j 1 -p ffs-repair raptorq_decode_golden_report -- --nocapture`
  - RCH remote `vmi1227854`:
    `cargo check -j 1 -p ffs-repair --all-targets`
  - RCH remote `vmi1227854`:
    `cargo clippy -j 1 -p ffs-repair --all-targets -- -D warnings`
- Artifact manifest: `artifacts/perf/bd-xmh5g.163/`.
- Next route: re-profile RaptorQ after this no-corruption bypass; do not repeat
  empty-path seed/setup deferral. Look for a deeper non-empty decode primitive
  or a distinct encode/decode memory-layout lever.

## Current Pass 29
- Bead: `bd-xmh5g.164`.
- Target: `ffs-repair`
  `raptorq_decode_group_16blocks` and
  `raptorq_decode_group_owned_symbols_16blocks` after the pass-28
  no-corruption bypass.
- Profile source:
  - Same-worker RCH remote `vmi1152480` Criterion baseline:
    - `raptorq_decode_group_16blocks`
      `[345.04 us,381.31 us,418.63 us]`
    - `raptorq_decode_group_owned_symbols_16blocks`
      `[338.42 us,364.50 us,383.21 us]`
    - `raptorq_decode_group_no_corruption_16blocks`
      `[31.279 ns,31.818 ns,32.789 ns]`
- Lever rejected: pair-fused direct known-source projection using a local safe
  RFC6330 GF(256) multiplication table for two direct-repair rows at a time.
  This was the "multi-row source projection" primitive suggested after pass 26,
  but it lost to the existing `gf256_addmul_slice` dispatch.
- Behavior proof before rejection:
  - RCH remote `vmi1152480`
    `cargo test -j 1 -p ffs-repair direct_projection_pair_kernel_matches_sequential_addmul -- --nocapture --test-threads=1`
    passed. The test compared the candidate pair kernel against two sequential
    `gf256_addmul_slice` calls for zero, one, and general GF(256)
    coefficients on a non-multiple-of-8 length.
- Candidate rebench:
  - Same-worker RCH remote `vmi1152480` Criterion:
    - `raptorq_decode_group_16blocks`
      `[361.67 us,392.87 us,426.04 us]`
    - `raptorq_decode_group_owned_symbols_16blocks`
      `[425.85 us,493.22 us,564.92 us]`
    - `raptorq_decode_group_no_corruption_16blocks`
      `[64.045 ns,93.936 ns,136.33 ns]`
- Score: rejected. Borrowed decode regressed by midpoint, owned decode
  regressed heavily, and the no-corruption fast path picked up table/static
  overhead noise. Score `0.0`; no runtime code kept.
- Isomorphism proof for the candidate:
  - Ordering preserved: yes; row order, repair row selection, recovered block
    order, and duplicate corrupt-index handling were unchanged.
  - Tie-breaking unchanged: yes; `select_full_rank_pair` and fallback ordering
    were unchanged.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: yes; seed derivation and repair ESI order were
    unchanged.
  - GF arithmetic identical: yes by the focused pair-kernel test above.
  - Golden SHA256: not promoted because the candidate failed the same-worker
    keep threshold before golden promotion. Shipped state is exact revert.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/codec.rs` was
  restored to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.164/`.
- Next route: do not repeat pair-fused same-source projection, determinant
  scaling, validation pruning, decoder deferral, ESI validation shortcuts,
  determinant caching, row caching, output materialization, empty-path
  seed/setup deferral, LRC small-parity scheduling, source-major LRC projection,
  tuple-matrix chunking, or quad-source fusion. Attack a different primitive:
  a structural RaptorQ encode/decode layout change that avoids repair-row
  materialization or a regenerating-code/LRC coefficient-layout primitive from
  the alien-graveyard corpus, with a target ratio of at least `1.20x` on the
  non-empty decode or LRC global parity row.

## Current Pass 30
- Bead: `bd-xmh5g.165`.
- Target: `ffs-repair` `raptorq_decode_group_16blocks`, selected from fresh
  RCH full-suite profile on remote `vmi1152480` after pass 28 and after
  SnowyHeron's LRC reservation:
  - `raptorq_decode_group_16blocks` `[311.28 us,341.56 us,371.19 us]`
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[313.55 us,325.14 us,341.98 us]`
  - `raptorq_encode_group_16blocks` `[283.36 us,307.15 us,324.77 us]`
  - `raptorq_decode_group_no_corruption_16blocks`
    `[32.050 ns,33.677 ns,34.622 ns]`
  - `lrc_encode_global_64blocks_8parity`
    `[397.87 us,488.34 us,565.08 us]`, out of scope due SnowyHeron's LRC
    reservation.
- Lever rejected: streamed borrowed direct-decode known-source projection from
  the block device into repair-row residuals, avoiding the intermediate
  `Vec<KnownSourceSymbol>` only on the borrowed path.
- Behavior proof before rejection:
  - RCH remote `vmi1153651`: `cargo check -j 1 -p ffs-repair --lib`.
  - RCH remote `vmi1152480`:
    `cargo test -j 1 -p ffs-repair --lib -- --nocapture --test-threads=1`
    passed 444 tests.
  - `decode_direct_source_major_residuals_match_row_major_reference` passed.
  - Marker-bounded `RAPTORQ_DECODE_GOLDEN` SHA256 stayed
    `f751dab0a92e1476c2b57a94884162005cdf5fc1cdffc99f260c1aefad60f5c5`.
- Candidate rebench: same-worker RCH remote `vmi1152480`:
  - `raptorq_decode_group_16blocks` `[365.15 us,397.48 us,452.72 us]`
    (`0.86x` midpoint; `0.69x` conservative lower/upper)
  - `raptorq_decode_group_owned_symbols_16blocks`
    `[337.14 us,349.48 us,365.27 us]` (`0.93x` midpoint)
  - `raptorq_decode_group_no_corruption_16blocks`
    `[29.904 ns,31.415 ns,32.960 ns]` (`1.07x` midpoint)
- Score: rejected. Target row regressed on the same worker, so Score `0.0`;
  no runtime code kept.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/codec.rs` was
  restored to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.165/`.
- Next route: do not repeat streamed borrowed projection, pair projection,
  placeholder-skip, row-cache, decoder-construction deferral, ESI-validation
  shortcuts, determinant-scaling, selected-row validation pruning, or
  output-buffer materialization. Attack a different RaptorQ primitive/layout
  that changes the projection shape rather than reordering the same residual
  work.

## Current Pass 31
- Bead: `bd-xmh5g.166`.
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity` after the pass-29
  RaptorQ pair-projection rejection.
- Profile source:
  - Same-worker RCH remote `vmi1152480` focused baseline:
    `[817.26 us,887.32 us,973.85 us]`.
- Lever rejected: safe-Rust 8-byte chunked XOR/table kernel inside the existing
  `gf256_mul_xor_pair_into` pair body. This preserved the same branch cases
  and parity/source iteration order, but replaced byte loops with `u64` byte
  carriers plus table lookups for the `(1,1)`, `(1,c)`, `(c,1)`, and `(c,c)`
  coefficient cases.
- Behavior proof before rejection:
  - RCH remote `vmi1152480`
    `cargo test -j 1 -p ffs-repair gf256_mul_xor_pair_matches_separate_passes -- --nocapture --test-threads=1`
    passed. The focused test compares the pair kernel to two separate
    `gf256_mul_xor_into` passes across zero, one, and general coefficient
    cases.
- Candidate rebench:
  - Same-worker RCH remote `vmi1152480` Criterion:
    `[993.84 us,1.0890 ms,1.1738 ms]`.
- Score: rejected. The candidate regressed the midpoint from `887.32 us` to
  `1.0890 ms`, so Score `0.0`; no runtime code kept.
- Candidate isomorphism:
  - Ordering preserved: yes; parity order `j=0..p-1`, data-block pair order,
    and within-block byte order were unchanged.
  - Tie-breaking unchanged: N/A for LRC encode.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - GF arithmetic identical: yes by the focused pair-kernel equivalence test.
  - Golden SHA256: not promoted because the candidate failed the same-worker
    keep threshold before golden promotion. Shipped state is exact revert.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/lrc.rs` was
  restored to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.166/`.
- Next route: do not repeat byte-chunking in the LRC pair body. The next
  primitive should be algorithmically different: either a coefficient-layout
  primitive that avoids repeated GF table row construction across global parity
  rows, or a regenerating-code/LRC encode layout harvested from the
  alien-graveyard corpus with a target ratio of at least `1.20x` on
  `lrc_encode_global_64blocks_8parity`.

## Current Pass 32
- Bead: `bd-xmh5g.167`.
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity` after the
  pass-31 pair-kernel rejection.
- Profile source:
  - Same-worker RCH remote `vmi1152480` focused baseline:
    `[409.81 us,511.14 us,621.98 us]`.
- Lever rejected: safe-Rust p=8 source-chunked byte-tiled global parity. The
  candidate split the 64 data blocks into 8-source chunks, computed eight
  partial global parity rows per chunk using preselected GF(256) coefficient
  rows, then XOR-reduced the partial parity blocks.
- Behavior proof before rejection:
  - `cargo fmt -p ffs-repair -- --check` passed.
  - RCH remote `vmi1152480`
    `cargo test -j 1 -p ffs-repair global -- --nocapture --test-threads=1`
    passed 16 focused tests, including
    `encode_global_8_parity_byte_tiled_matches_reference`.
  - Marker-bounded `LRC_GLOBAL_PARITY_GOLDEN` SHA256 stayed
    `84e1fd0632220fbbba4154b19448e06ade4c648c724171b5e41c131242a2df8b`
    before and after the candidate.
- Candidate rebench:
  - Same-worker RCH remote `vmi1152480` Criterion:
    `[2.8195 ms,3.1006 ms,3.5421 ms]`.
- Score: rejected. The candidate regressed the midpoint from `511.14 us` to
  `3.1006 ms`, so Score `0.0`; no runtime code kept.
- Candidate isomorphism:
  - Ordering preserved: yes; output parity row order `j=0..p-1`, data-block
    order `i=0..k-1`, and per-byte GF accumulation were equivalent to the
    parity-major reference test.
  - Tie-breaking unchanged: N/A for LRC encode.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - GF arithmetic identical: yes by the focused p=8 reference-equivalence
    test and golden SHA match.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/lrc.rs` was
  restored to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.167/`.
- Next route: do not repeat source-chunk partial reductions, byte-chunking in
  the pair body, source-major sequential projection, tuple-matrix chunking, or
  quad-source fusion. Attack a different LRC primitive: precompute a compact
  coefficient schedule for the parity-major path or add a same-binary old/new
  microkernel bench for a register-blocked GF(256) projection kernel before
  promoting it to `encode_global`.

## Current Pass 33
- Bead: `bd-xmh5g.169`.
- Target: `ffs-repair` `lrc_encode_global_64blocks_8parity` after the
  pass-32 source-chunk partial-reduction rejection.
- Profile source:
  - Clean detached `HEAD` RCH remote `vmi1152480` focused baseline:
    `[553.08 us,636.80 us,691.71 us]`.
  - Same-path old baseline for the final A/B used RCH remote `ovh-a`:
    `[193.84 us,196.57 us,199.79 us]`.
- Lever rejected: safe-Rust seed microkernel for the first global-parity source
  pair. The candidate assigned the first one or two GF(256) projections into the
  zero-initialized parity row with `gf256_mul_into` / `gf256_mul_pair_into`
  before continuing the existing parity-major XOR loop.
- Behavior proof before rejection:
  - `cargo fmt -p ffs-repair -- --check` passed while the candidate diff was
    present.
  - A late targeted RCH attempt for
    `global_parity_seed_microkernel_matches_zero_initialized_xor` matched zero
    tests because the runtime source had already been restored to clean `HEAD`;
    therefore it is not counted as candidate behavior proof.
  - The candidate was rejected by same-worker benchmark evidence before any
    code was kept, and the shipped state is the pre-lever source.
  - Marker-bounded `LRC_GLOBAL_PARITY_GOLDEN` SHA256 stayed
    `84e1fd0632220fbbba4154b19448e06ade4c648c724171b5e41c131242a2df8b`.
- Candidate rebench:
  - Initial candidate route on RCH remote `vmi1149989`:
    `[208.20 us,225.52 us,242.60 us]`; this was routing evidence only because
    its same-worker old baseline was unavailable.
  - Same-path/same-worker RCH remote `ovh-a` old baseline:
    `[193.84 us,196.57 us,199.79 us]`.
  - Same-path/same-worker RCH remote `ovh-a` candidate:
    `[228.05 us,246.63 us,268.45 us]`.
- Score: rejected. The candidate regressed the same-worker midpoint from
  `196.57 us` to `246.63 us`, so Score `0.0`; no runtime code kept.
- Candidate isomorphism:
  - Ordering preserved: yes; parity row order `j=0..p-1`, data-block order
    `i=0..k-1`, and byte order were unchanged.
  - Tie-breaking unchanged: N/A for LRC encode.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - GF arithmetic identical: not promoted; final shipped state is pre-lever.
    The golden SHA remained unchanged after source restoration.
- Shipped-state isomorphism: exact. `crates/ffs-repair/src/lrc.rs` was
  restored to the pre-lever source before closeout.
- Artifact manifest: `artifacts/perf/bd-xmh5g.169/`.
- Next route: do not repeat seed-assignment microkernels, source-chunk partial
  reductions, byte-chunking in the pair body, source-major sequential
  projection, tuple-matrix chunking, or quad-source fusion. Attack a different
  LRC primitive: a compact coefficient-table/schedule with a same-binary
  old/new microkernel gate, or a fundamentally different GF(256) matrix-vector
  layout that changes coefficient reuse rather than the first-row write.

## Current Pass 34
- Bead: `bd-eiywc`.
- Target: `ffs-btrfs` parsed-node leaf walks where `BtrfsParsedNode::Leaf`
  already owns the verified `Arc<[u8]>` block but wide walks still clone every
  in-range item payload into `Vec<u8>`.
- Profile source:
  - Same-worker RCH remote `vmi1149989` baseline Criterion
    `btrfs_parsed_node_walk_full/parsed_cached`:
    `[193.43 us,197.25 us,203.97 us]`.
- Lever kept: expose and prove the borrowed parsed-node walker path in the
  benchmark/gate surface so leaf entries borrow Arc-backed block ranges instead
  of materializing each payload into an owned vector.
- Behavior proof:
  - RCH remote `vmi1227854`
    `cargo check -j 1 -p ffs-btrfs --all-targets` passed.
  - RCH remote `vmi1227854`
    `cargo clippy -j 1 -p ffs-btrfs --all-targets -- -D warnings` passed.
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-btrfs walk_tree_range_borrowed_with_nodes_matches_owned_entries_bd_eiywc -- --nocapture --test-threads=1`
    passed 1 focused unit test.
  - File-scoped `rustfmt --edition 2024 --check` passed for touched Rust files.
  - `git diff --check` passed.
  - UBS scanned touched Rust files; its fmt/clippy/check/test sections were
    clean, while broad pre-existing panic/unwrap/thread lifecycle inventory
    remains outside this lever.
- Rebench:
  - Same-worker RCH remote `vmi1149989` candidate Criterion
    `btrfs_parsed_node_walk_full/parsed_cached_borrowed`:
    `[53.754 us,57.278 us,60.683 us]`.
  - Midpoint speedup over baseline parsed-cache owned materialization:
    `197.25 / 57.278 = 3.44x`; conservative bounds: `193.43 / 60.683 = 3.19x`.
  - Same-run owned-vs-borrowed midpoint on `vmi1149989`:
    `331.26 / 57.278 = 5.78x`.
- Score: kept. Impact `4.5` x Confidence `4.0` / Effort `1.0` = `18.0`.
- Isomorphism:
  - Ordering preserved: yes; borrowed batches are emitted in the same root,
    child, and leaf item order as the owned byte walker.
  - Tie-breaking unchanged: N/A; range keys and traversal comparisons are
    unchanged.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Byte payloads unchanged: yes; the focused test converts every borrowed
    batch back to owned entries and asserts exact `Vec<BtrfsLeafEntry>`
    equality across empty, point, partial, full, and repeated-pass ranges.
- Golden SHA256: N/A for this synthetic benchmark/test path; byte-for-byte
  equality is asserted directly in the focused unit test and benchmark
  precondition.
- Artifact manifest: `artifacts/perf/bd-eiywc/`.
- Next route: profile the newly-filed `bd-dy41g` contiguous block-read path
  after this closeout; do not keep tuning the borrowed materialization path
  until downstream consumers expose the next copy/allocation boundary.

## Current Pass 35
- Bead: `bd-upc25`.
- Target: `ffs-block` no-default ARC clean-victim selection during dirty-front
  memory-pressure trim. The cc reassessment correctly made this
  measurement-first because default `ffs-block` is S3-FIFO and realistic ARC
  promotion often keeps clean blocks at the eviction end.
- Profile source:
  - Added an adversarial Criterion target
    `arc_dirty_front_pressure_trim_4096_dirty3072` that warms 4096 resident
    blocks, stages the oldest 3072 blocks dirty without promotion, and times
    `memory_pressure_callback(MemoryPressure::Critical)`.
  - True old-code ARC baseline used a detached worktree at `4321b7e7` with
    only the benchmark added and `--no-default-features` on same RCH remote
    `hz1`: `[115.99 ms,119.31 ms,123.65 ms]`.
- Lever kept: maintain recency-ordered clean-resident indexes for ARC T1/T2
  under `not(feature = "s3fifo")`, updating them on admission, promotion,
  dirty/clean transitions, explicit eviction, and pressure eviction. This
  replaces the old scan for the first clean T1/T2 victim while leaving the
  resident `VecDeque` ARC lists in place.
- Rebench:
  - Same-worker RCH remote `hz1` candidate Criterion
    `arc_dirty_front_pressure_trim_4096_dirty3072` with `--no-default-features`:
    `[4.3725 ms,4.7234 ms,5.2208 ms]`.
  - Midpoint speedup: `119.31 / 4.7234 = 25.3x`; conservative bounds:
    `115.99 / 5.2208 = 22.2x`.
- Behavior proof:
  - RCH remote `hz1`
    `cargo test -j 1 -p ffs-block --no-default-features arc_state_ -- --nocapture --test-threads=1`
    passed 13 ARC/state tests, including
    `arc_state_clean_index_preserves_dirty_front_victim_order`.
  - RCH remote `hz1`
    `cargo check -j 1 -p ffs-block --no-default-features --all-targets`
    passed.
  - RCH remote `hz1`
    `cargo clippy -j 1 -p ffs-block --no-default-features --all-targets -- -D warnings`
    passed after tightening a pre-existing benchmark mutex guard lifetime in
    `benches/read_contiguous.rs`.
  - RCH remote `hz1`
    `cargo check -j 1 -p ffs-block --all-targets` passed.
  - RCH remote `hz1`
    `cargo clippy -j 1 -p ffs-block --all-targets -- -D warnings` passed.
  - Local `cargo fmt --package ffs-block --check` passed.
  - Local `git diff --check` passed for touched paths.
- Score: kept. Impact `5.0` x Confidence `4.0` / Effort `2.0` = `10.0`.
- Isomorphism:
  - Ordering preserved: yes; the invariant test derives the old scan-order
    clean subsequence directly from T1/T2 and asserts the clean indexes match.
  - Tie-breaking unchanged: yes; pressure victim still prefers oldest clean
    T1 then oldest clean T2, while `replace()` preserves its old target-list
    then fallback-list ordering.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Dirty handling unchanged: yes; dirty blocks are removed from the clean
    indexes, pressure trim still skips dirty residents, and cleaning a resident
    restores it at its original recency position.
- Golden SHA256: N/A for this ARC state-machine path; no persisted golden
  output is produced. The focused state test asserts the golden victim sequence
  `6 -> none -> 0` and the invariant suite compares every indexed clean order
  against the legacy scan model.
- Next route: close `bd-upc25`, re-run live `br ready --json`, and only claim
  the next `[perf]` bead with a profile-backed target.

## Current Pass 36
- Bead: `bd-xmh5g.170`.
- Target: `ffs-mvcc` SSI dangerous-structure validation on
  `mvcc_commit_ssi_pivot_192reads_32records`, a read-heavy pivot scanned
  against 32 post-snapshot committed records with disjoint key spans.
- Profile source:
  - Broad remote `ffs-mvcc` profile identified
    `mvcc_commit_ssi_pivot_192reads_32records` at
    `[89.789 us,96.431 us,104.79 us]`.
  - Same-worker targeted RCH baseline on `vmi1227854`:
    `[110.04 us,114.62 us,118.49 us]`.
- Lever kept: add min/max BTree key-span guards before SSI incoming/outgoing
  membership probes. The guard returns early only when a candidate key is
  outside the opposite set's span; keys inside the span still use the old
  `get`/`contains` lookup path.
- Rebench:
  - Same-worker RCH remote `vmi1227854`
    `mvcc_commit_ssi_pivot_192reads_32records`:
    `[93.150 us,96.370 us,99.484 us]`.
  - Median speedup: `114.62 / 96.370 = 1.19x`; conservative bounds:
    `110.04 / 99.484 = 1.11x`.
- Behavior proof:
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-mvcc ssi_ -- --nocapture --test-threads=1`
    passed 32 unit tests plus filtered SSI integration/stress coverage.
  - RCH remote `vmi1153651`
    `cargo check -j 1 -p ffs-mvcc --all-targets` passed.
  - RCH remote `vmi1227854`
    `cargo clippy -j 1 -p ffs-mvcc --all-targets -- -D warnings`
    passed after a test-only checked-size cleanup.
  - Local `cargo fmt -p ffs-mvcc -- --check` passed.
- Score: kept. Impact `3.0` x Confidence `3.0` / Effort `2.0` = `4.5`.
- Isomorphism:
  - Ordering preserved: yes; overlapping spans still use the existing ordered
    smaller-set scan, so the first edge remains the minimum intersection block.
  - Tie-breaking unchanged: yes; the new guard cannot produce a match, only
    skip keys that cannot be in the opposite set.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
  - Empty-set behavior unchanged: yes; helper spans return `None` exactly when
    the old scan would have produced no edge.
- Golden SHA256:
  - SSI span-guard digest in the focused test:
    `1800c846028c2f980ca158d0ea6c41114d05e293588da3cbd25ad3b8f001eb37`.
  - Manual behavior vector SHA256:
    `bf6e42a6042972ae339577d235742a329549e7ed96aa48092a7e89c3ffcbf930`.
- Next route: close `bd-xmh5g.170`, re-run live `[perf]` triage, and pick the
  next profile-backed hotspot under `bd-xmh5g`.

## Current Pass 37
- Bead: `bd-9m1g4`.
- Target: `ffs-mvcc` SSI no-read commit path
  `mvcc_commit_ssi_0reads`, where `commit_ssi_internal` cloned staged writes
  through `resolved_writes_for_commit` and `compress_data` on the
  no-conflict/no-compression path.
- Profile source:
  - Bead-filed profile target plus chronological same-worker RCH baseline on
    `vmi1153651`: `[2.8647 us,3.1170 us,3.4123 us]`.
  - Detached old-code confirmation baseline on RCH remote `vmi1149989` from
    committed `HEAD`: `[1.3163 us,1.3431 us,1.3736 us]`.
- Lever kept: split conflict-only merge resolution from commit storage so
  no-conflict SSI writes move the staged `Vec<u8>` directly into
  `VersionData::Full` after FCW preflight, SSI read-set validation, and commit
  sequence allocation. Conflicting writes still use the same merge-proof path.
- Rebench:
  - Same-worker RCH remote `vmi1149989` candidate
    `mvcc_commit_ssi_0reads`: `[974.78 ns,1.1138 us,1.2182 us]`.
  - Median speedup: `1.3431 / 1.1138 = 1.21x`; conservative bounds:
    `1.3163 / 1.2182 = 1.08x`.
- Behavior proof:
  - RCH remote `vmi1152480`
    `cargo test -j 1 -p ffs-mvcc commit_ssi_move_through -- --nocapture --test-threads=1`
    passed 3 focused tests plus filtered integration targets.
  - RCH remote `vmi1152480`
    `cargo check -j 1 -p ffs-mvcc --all-targets` passed.
  - RCH remote `vmi1152480`
    `cargo clippy -j 1 -p ffs-mvcc --all-targets -- -D warnings` passed after
    extracting the conflict-only merge helper.
  - Local `cargo fmt -p ffs-mvcc -- --check` passed.
- Score: kept. Impact `3.0` x Confidence `4.0` / Effort `2.0` = `6.0`.
- Isomorphism:
  - Ordering preserved: yes; committed write keys remain `BTreeMap`/`BTreeSet`
    ordered, and the helper only records conflict-resolved bytes by block key.
  - Tie-breaking unchanged: yes; FCW conflict policy and SSI read-set
    validation run before consuming the transaction, and same-block merge
    ordering is covered by the append-only proof test.
  - Abort semantics unchanged: yes; strict FCW conflicts still return the
    original transaction with its staged bytes intact.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - SSI move-through digest in the focused test:
    `dc236d4739346b0642de6982db3bf2afee6019b1f4637fd65699f7a8e6e984be`.
- Next route: close `bd-9m1g4`, sync/push, then re-run live `[perf]` triage
  because the next MVCC bottleneck may have shifted.

## Current Pass 38
- Bead: `bd-yd3a0`.
- Target: `ffs-core` ext4 `lseek_data` / `fiemap` extent collection. The
  profile-backed issue was repeated suffix queries over fragmented ext4 files:
  the old path flattened every extent-tree leaf, then caller-side filters
  discarded extents ending before the query position.
- Lever kept: `collect_extents_from_with_scope` prunes completed index children
  before flattening, while `collect_extents_with_scope` remains the from-block
  zero path. `ext4_lseek_data` and `ext4_fiemap` pass their query block.
- Commit/closeout:
  - Production commit: `8917465c`.
  - Bead close commit: `dc33070b`.
  - Remote pushed `main` and `main:master` were already at `dc33070b` when
    local proof validation completed.
- Rebench:
  - RCH remote `vmi1149989`
    `ext4_extent_collect_window_64x340` parse-backed model:
    `collect_all [52.335 us,53.898 us,55.629 us]` ->
    `collect_from_75pct [12.321 us,12.636 us,12.966 us]`.
    Median speedup: `4.27x`; conservative bounds: `4.04x`.
  - RCH remote `vmi1149989`
    `ext4_extent_suffix_collect_340x340` suffix-filter model:
    `full_collect_then_filter [67.537 ms,69.090 ms,70.736 ms]` ->
    `suffix_pruned_collect [17.914 ms,18.326 ms,18.752 ms]`.
    Median speedup: `3.77x`; conservative bounds: `3.60x`.
- Behavior proof:
  - RCH remote `vmi1149989`
    `cargo test -p ffs-core lseek_data -- --nocapture` passed.
  - RCH remote `vmi1149989`
    `cargo test -p ffs-core fiemap -- --nocapture` passed.
  - RCH remote `vmi1149989`
    `cargo test -p ffs-core collect_extents_from_scope_prunes_completed_index_children_bd_yd3a0 -- --nocapture`
    passed.
  - RCH remote `vmi1149989`
    `cargo check -p ffs-core --all-targets` passed.
  - RCH remote `vmi1149989`
    `cargo clippy -p ffs-core --all-targets -- -D warnings` passed after a
    bench-only `type_complexity` alias cleanup.
  - Local `cargo fmt -p ffs-core --check` passed.
- Score: kept. Impact `4.0` x Confidence `4.0` / Effort `2.0` = `8.0`.
- Isomorphism:
  - Ordering preserved: yes; retained leaves are still traversed in ext4 index
    order, and caller-side filtering keeps the same sorted suffix.
  - Tie-breaking unchanged: yes; the new guard only skips children whose next
    sibling starts at or before the query block, so no covering child is lost.
  - FIEMAP last-extent semantics unchanged: yes; the retained suffix still runs
    through the final file extent.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - Ext4 suffix-pruning digest:
    `50c0f4504bdc1a3fe71c04f8d4be2ffcc45b9c6e9c638defe2b4d1eb33bfffe4`.
- Next route: re-run live `[perf]` triage and claim the next top ready bead;
  current expected lane is `bd-hfkty` unless another agent has claimed it.

## Current Pass 39
- Bead: `bd-hfkty`.
- Target: `ffs-btrfs` COW-tree sequential write mutation lane. The
  profile-backed issue was per-write same-tree COW work: extent insert plus
  inode update cloned/retired the root leaf twice when both keys lived on the
  same COW path.
- Rejected micro-lever: csum `upsert` update-or-insert measured as noise
  locally (`619.97 us` -> `616.22 us` median) and was not kept.
- Lever kept: `InMemoryCowBtrfsTree::insert_then_update` batches one insert
  and one update into a single atomic COW mutation. The root-leaf fast path
  mutates both sorted entries in one cloned leaf and retires the old leaf once;
  the fallback keeps insert-then-update error semantics with staged rollback.
- Rebench:
  - RCH remote `ovh-a`
    `cargo bench -j 1 -p ffs-btrfs --bench cow_write_mutation -- btrfs_cow_write_mutation_256x4k --noplot --sample-size 10 --warm-up-time 1 --measurement-time 1`:
    `sequential_insert_then_update [1.8236 ms,1.8411 ms,1.8728 ms]` ->
    `batched_insert_then_update [1.0941 ms,1.0966 ms,1.0997 ms]`.
    Median speedup: `1.68x`; conservative bounds: `1.66x`.
  - Local isolated-target confirmation:
    `sequential_insert_then_update [2.6223 ms,2.7332 ms,2.8456 ms]` ->
    `batched_insert_then_update [1.2476 ms,1.2826 ms,1.3439 ms]`.
    Median speedup: `2.13x`; conservative bounds: `1.95x`.
- Behavior proof:
  - Local isolated target
    `cargo test -j 1 -p ffs-btrfs --lib insert_then_update_matches_sequential_and_preserves_order_bd_hfkty -- --nocapture --test-threads=1`
    passed and printed the pinned golden digest below.
  - Local isolated target `cargo check -j 1 -p ffs-btrfs --all-targets`
    passed; dependency warning in `ffs-block` is unrelated.
  - Local isolated target
    `cargo clippy -j 1 -p ffs-btrfs --all-targets --no-deps -- -D warnings`
    passed. Full dependency clippy is blocked by pre-existing
    `ffs-block::next_pressure_victim` dead code from another lane.
  - `cargo fmt -p ffs-btrfs -- --check` and `git diff --check` passed.
- Score: kept. Impact `3.0` x Confidence `3.0` / Effort `2.0` = `4.5`.
- Isomorphism:
  - Ordering preserved: yes; the root-leaf fast path uses the same
    `partition_point` ordering and the fallback uses existing `insert_into`.
  - Tie-breaking unchanged: yes; duplicate insert keys still return
    `KeyAlreadyExists`, missing update keys still return `KeyNotFound`, and
    equal insert/update keys preserve the sequential final payload.
  - Atomicity improved: yes; fallback work stays staged until both operations
    succeed, with rollback on error.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - Batch COW mutation digest:
    `bd523dce52c8af3935c65703cec9a593e76a0a9eb505799247cf882a05ab730f`.
- Next route: close `bd-hfkty`, sync/push, then re-profile because the next
  write-path bottleneck may shift to csum construction or MVCC staging.

## Current Pass 40
- Bead: `bd-xmh5g.171`.
- Target: `ffs-core` / `ffs-btrfs` btrfs sequential write checksum-tree
  mutation lane. After `bd-hfkty`, the same profile-backed write path still
  paid an update miss plus insert fallback for new csum-tree entries.
- Lever kept: `InMemoryCowBtrfsTree::upsert` performs exact-key replace or
  sorted insert through the existing COW mutation path, and
  `btrfs_capture_data_extent_csums` uses it instead of caller-side
  `update(...).or_else(insert)`.
- Rebench:
  - RCH remote `vmi1149989`
    `cargo bench -p ffs-harness --bench metadata_parse --profile release-perf -- btrfs_write_path/btrfs_sequential_write_1m_4k --sample-size 10`:
    `[19.428 ms,21.706 ms,23.927 ms]` ->
    `[18.009 ms,18.686 ms,19.286 ms]`.
    Median speedup: `1.16x`; conservative bounds: `1.01x`.
- Behavior proof:
  - RCH remote `vmi1149989`
    `cargo test -v -p ffs-btrfs --lib upsert_matches_update_or_insert_and_preserves_order_bd_hfkty -- --nocapture --test-threads=1`
    passed and printed the pinned csum upsert golden digest below.
  - RCH remote `vmi1149989`
    `cargo test -v -p ffs-core btrfs_write_captures_data_csums_for_datasum_inode_bd_x3fcu -- --nocapture --test-threads=1`
    passed.
  - RCH remote `vmi1227854`
    `cargo check -p ffs-btrfs -p ffs-core --all-targets` passed.
  - RCH remote `vmi1227854`
    `cargo clippy -p ffs-btrfs -p ffs-core --all-targets -- -D warnings`
    passed.
  - Local `cargo fmt -p ffs-btrfs -p ffs-core --check` passed.
- Score: kept. Impact `3.0` x Confidence `4.0` / Effort `1.5` = `8.0`.
- Isomorphism:
  - Ordering preserved: yes; the new method delegates to the same sorted-key
    COW insertion path, and the proof asserts csum block order
    `[1, 3, 6, 9, 12, 27, 33, 45, 51, 60]`.
  - Tie-breaking unchanged: yes; duplicate exact `BtrfsKey` writes replace the
    existing payload, matching the prior successful `update` branch.
  - Error classes unchanged: yes; structural COW-tree errors still come from
    the existing insert/replace path and map through the same btrfs mutation
    adapter at the call site.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - Csum-shaped upsert digest:
    `a4098f58b4744652ef7cf8b227537c092989ffc8b91712c5a60c73d89ab42644`.
- Next route: close `bd-xmh5g.171`, sync/push, then re-run live `[perf]`
  triage for the shifted btrfs write-path bottleneck.

## Current Pass 41
- Bead: `bd-a2x93`.
- Target: project-level `release-perf` CPU deployment policy for
  `target-cpu=x86-64-v3`, using the zero-scan lane named in the bead title.
- Baseline:
  - RCH remote `vmi1152480`
    `cargo bench -j 1 -p ffs-ondisk --profile release-perf --bench casefold_key -- zero_scan --noplot --sample-size 10 --warm-up-time 1 --measurement-time 1`:
    - `zero_scan_chunked_all_zero_4k [87.706 ns,93.939 ns,100.73 ns]`
    - `zero_scan_chunked_late_nonzero_4k [58.669 ns,64.999 ns,72.810 ns]`
- Candidate result:
  - Rejected/no-code. A corrected `RUSTFLAGS="-C target-cpu=x86-64-v3"`
    run selected a different worker from the baseline, so it was cancelled as
    non-comparable. A same-worker shell wrapper was also rejected by `rch` as a
    non-compilation command and was terminated; no local output was used.
- Behavior proof:
  - No code or Cargo/profile change was kept, so ordering, tie-breaking,
    floating-point behavior, and RNG are unchanged by construction.
  - Golden output SHA256: N/A for no-code no-ship closeout; no production output
    path changed.
- Score: rejected/no-code. Impact `1.0` x Confidence `2.0` / Effort `2.0` =
  `1.0`, below the keep threshold.
- Next route: avoid global CPU-policy tuning without same-worker evidence.
  Attack a per-kernel primitive next: broadword/portable-SIMD zero-scan in
  `ffs-types::all_zero_bytes`, or the active peer GF256 probe if handed off,
  with same-binary A/B proof.

## Current Pass 42
- Bead: `bd-gn4zb`.
- Target: `ffs-core`/`ffs-fuse` paginated readdir snapshot serving. The
  existing snapshot cache held an `Arc<Vec<DirEntry>>` but cloned a capped
  `Vec<DirEntry>` page for every FUSE readdir/readdirplus call.
- Lever kept: `FsOps::readdir` now returns `ReaddirPage`, an Arc-backed
  full-listing view plus visible bounds. The production ext4/btrfs snapshot
  path returns shared pages, and the FUSE readdir/readdirplus hot path iterates
  borrowed entries without per-call `DirEntry`/name clones. Public convenience
  and fuzzing wrappers still materialize owned vectors only at those boundaries.
- Baseline:
  - RCH remote `vmi1227854`
    `cargo bench -j 1 -p ffs-core --profile release-perf --bench readdir_snapshot_page -- readdir_snapshot_full_enumeration_20k --noplot --sample-size 10 --warm-up-time 1 --measurement-time 1`:
    - `old_clone_whole_tail [21.798 ms,23.080 ms,24.977 ms]`
    - `new_capped_page [1.6737 ms,1.7215 ms,1.7753 ms]`
- Rebench:
  - RCH remote `vmi1227854`
    same command:
    - `old_clone_whole_tail [29.718 ms,30.859 ms,32.172 ms]`
    - `new_capped_page [1.9459 ms,2.0131 ms,2.1083 ms]`
    - `new_shared_arc_page [7.1894 us,7.7114 us,8.3078 us]`
    Mean speedup vs the baseline production-equivalent capped page:
    `1.7215 ms / 7.7114 us = 223.24x`.
- Behavior proof:
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-core readdir_snapshot_pagination_reconstructs_full_listing -- --nocapture`
    passed and printed the pinned golden digest below.
  - RCH remote `vmi1227854`
    `cargo check -j 1 -p ffs-core -p ffs-fuse --all-targets` passed.
  - RCH remote `vmi1227854`
    `cargo clippy -j 1 -p ffs-core -p ffs-fuse --all-targets -- -D warnings` passed.
  - Local `cargo fmt --check -p ffs-core -p ffs-fuse` passed.
- Score: kept. Impact `4.0` x Confidence `4.0` / Effort `1.5` = `10.67`.
- Isomorphism:
  - Ordering preserved: yes; page start still uses the same
    `partition_point(|entry| entry.offset <= offset)`, and callers consume the
    same ascending cookie slice.
  - Tie-breaking unchanged: yes; entries with equal external state are still
    represented by the same `DirEntry` values from the same full listing, with
    no new sorting or filtering.
  - Error classes unchanged: yes; filesystem read/validation paths are
    unchanged, and only the successful readdir result carrier changed.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - Readdir ordered listing digest:
    `51a7e8413882063df6cc4489a52f9c370d7f2884b986e1d5a5056d84d477de84`.
- Next route: close `bd-gn4zb`, sync/push, then re-run `[perf]` triage. The
  active next non-overlapping candidate is the btree extent-insert overlap
  localization bead if it remains ready and profile-backed.

## Current Pass 43
- Bead: `bd-xmh5g.172`.
- Target: `OpenFs::largest_contiguous_free_run` first-call ext4 largest-run
  scan. Fresh RCH routing showed the remaining scan-many-groups CPU row in
  `ffs-alloc`:
  - RCH remote `vmi1227854`
    `cargo bench -j 1 -p ffs-alloc --profile release-perf --bench bitmap_ops -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 1`:
    - `largest_free_run_cached_ab/bitmap_scan_128_groups [120.50 us,126.67 us,133.39 us]`
    - `largest_free_run_cached_ab/cached_group_stats_128_groups [120.73 ns,123.90 ns,126.19 ns]`
- Lever kept: while computing the maximum across uncached ext4 groups, still
  read every uncached bitmap to preserve I/O/error behavior, but skip the CPU
  `bitmap_largest_free_run` scan when `group.free_blocks <= current_best`.
  Since a group's largest run cannot exceed its free-block count, the skipped
  scan cannot change the maximum.
- Rebench:
  - RCH remote `vmi1227854`
    same command:
    - `largest_free_run_cached_ab/bitmap_scan_128_groups [134.50 us,143.80 us,156.07 us]`
    - `largest_free_run_free_count_prune_ab/bitmap_scan_128_groups [161.24 us,168.08 us,175.96 us]`
    - `largest_free_run_free_count_prune_ab/free_count_pruned_128_groups [460.06 ns,462.90 ns,466.48 ns]`
    Conservative speedup against the original routing baseline:
    `126.67 us / 462.90 ns = 273.65x`; same-binary A/B speedup:
    `168.08 us / 462.90 ns = 363.10x`.
- Behavior proof:
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-core -p ffs-alloc largest -- --nocapture`
    passed: 17 `ffs-alloc` largest-run tests and 3 `ffs-core`
    largest-contiguous tests.
  - RCH remote `vmi1227854`
    `cargo clippy -j 1 -p ffs-core -p ffs-alloc --all-targets -- -D warnings`
    passed.
  - Local `cargo fmt --check -p ffs-core -p ffs-alloc` passed.
  - Local `git diff --check` passed.
- Score: kept. Impact `3.0` x Confidence `3.5` / Effort `1.5` = `7.0`.
- Isomorphism:
  - Ordering preserved: yes; group iteration remains ascending `GroupNumber`
    order, and only CPU scans proven unable to improve `best` are skipped.
  - Tie-breaking unchanged: yes; the API returns only the maximum run length,
    not the winning group. Equal-run groups remain observationally identical.
  - Error classes unchanged: yes; uncached group bitmaps are still read before
    the skip, so bitmap read/validation errors remain observable.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - `bitmap_largest_free_run_golden_report` canonical block:
    `4d3300c701fc62e241ad715f89ccf4701ecb6b72ced820ca0922cc8a18dbf9b0`.
- Next route: close `bd-xmh5g.172`, sync/push, then re-run live `[perf]`
  triage. If the btree overlap bead is still stale against main or peer-dirty,
  attack the next non-overlapping measured primitive under `bd-xmh5g`.

## Current Pass 44
- Bead: `bd-3wal3`.
- Target: profile-first ffs-harness write-path measurement coverage, not a
  production optimization lever.
- Evidence added:
  - Added the `ext4_write_path/ext4_fragmented_overwrite_256ext` Criterion row
    in `crates/ffs-harness/benches/metadata_parse.rs` and kept the row
    deterministic: fresh mkfs fixture, deterministic extent fragmentation, fixed
    offsets, fixed overwrite length.
  - RCH remote `vmi1227854`
    `cargo bench -j 1 -p ffs-harness --profile release-perf --bench metadata_parse -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 1`
    timed out after 1800s before producing useful rows. Treat this as a suite
    sizing/routing failure, not a benchmark result.
  - RCH remote `vmi1227854`
    `cargo run -j 1 -p ffs-harness --profile release-perf -- profile-read-path --fixture conformance/golden/ext4_8mb_reference.ext4 --duration-sec 2 --mode direct-read`
    built, then failed because the `.ext4` fixture is local/untracked and was
    not transferred to the remote worker.
  - RCH remote `vmi1227854`
    `cargo bench -j 1 -p ffs-harness --bench metadata_parse -- ext4_fragmented_overwrite_256ext --noplot --sample-size 10 --warm-up-time 1 --measurement-time 1`
    produced the focused row:
    `ext4_write_path/ext4_fragmented_overwrite_256ext [16.757 ms,17.313 ms,17.803 ms]`.
- Gates:
  - RCH remote `vmi1227854`
    `cargo check -j 1 -p ffs-harness --all-targets` passed.
  - RCH remote `vmi1153651`
    `cargo clippy -j 1 -p ffs-harness --all-targets -- -D warnings` passed.
  - Local `cargo fmt --check -p ffs-harness` passed.
- Score: keep as profile infrastructure and closeout evidence. This is not a
  perf lever, so there is no before/after speedup claim.
- Isomorphism:
  - Ordering preserved: yes; production code paths are unchanged by this pass.
    The new benchmark drives deterministic writes in fixed offset order.
  - Tie-breaking unchanged: yes; no production tie-breaking logic changed.
  - Error classes unchanged: yes; benchmark-only changes and clippy hygiene did
    not change runtime library behavior.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: unchanged/N/A; no random inputs were added.
- Golden SHA256:
  - Production golden output: N/A because this pass only added measurement
    coverage and bench hygiene. Existing behavior was protected by the
    crate-scoped RCH check/clippy gates above.
- Next route: close `bd-3wal3`, sync/push, then re-run live `[perf]` triage.
  The next candidate to inspect is `bd-dwiti` if it remains ready and
  unclaimed.

## Current Pass 45
- Bead: `bd-6tooy`.
- Target: profile-backed `ext4_write_path/ext4_fragmented_overwrite_256ext`
  per-block write/MVCC staging cost from `bd-3wal3`.
- Alien primitive: inline-small-object storage layout (`SmallVec<[T; 1]>`) for
  the common one-range merge-proof payload; this removes the heap allocation
  from every ext4 full/partial block write proof while preserving the public
  range-overlay proof labels and serde array shape.
- Baseline:
  - RCH wrapper pinned to `thinkstation1` via
    `RCH_WORKER=vmi1227854 RCH_WORKERS=vmi1227854 rch exec -- sh -lc
    'hostname && cargo bench -j 1 -p ffs-harness --bench metadata_parse --
    ext4_fragmented_overwrite_256ext'`
  - `ext4_write_path/ext4_fragmented_overwrite_256ext
    [18.824 ms,19.573 ms,20.352 ms]`.
- Rebench:
  - RCH same host `thinkstation1`, fresh target dir
    `/data/tmp/frankenfs-bd-6tooy-bench-20260613`:
    `[17.353 ms,18.848 ms,20.384 ms]`.
  - RCH same host warm repeat, same target dir:
    `[17.922 ms,18.915 ms,19.416 ms]`.
  - Mean speedup vs baseline: `19.573 / 18.915 = 1.0348x` (`3.48%`).
- Behavior proof:
  - RCH remote `vmi1149989`
    `cargo test -j 1 -p ffs-mvcc
    merge_proof_inline_ranges_preserve_serde_golden_bd_6tooy -- --nocapture
    --test-threads=1` passed.
  - RCH remote `vmi1153651`
    `cargo check -j 1 -p ffs-mvcc -p ffs-core --all-targets` passed.
  - RCH same host `thinkstation1`
    `CARGO_TARGET_DIR=/data/tmp/frankenfs-bd-6tooy-clippy-20260613
    cargo clippy -j 1 -p ffs-mvcc -p ffs-core --all-targets --
    -D warnings` passed. A first clippy attempt hit a stale target-dir rustc
    mismatch (`E0514` on `cfg_if`/`getrandom`); no code diagnostic.
  - Local `cargo fmt --check -p ffs-mvcc -p ffs-core` passed.
  - Local `git diff --check` passed.
- Score: kept. Impact `2.2` x Confidence `2.1` / Effort `2.0` = `2.31`.
- Isomorphism:
  - Ordering preserved: yes; `BTreeMap` transaction write ordering is unchanged,
    and proof range iteration order is the same single declared range order.
  - Tie-breaking unchanged: yes; no conflict winner, extent resolver, directory,
    or allocation tie-break logic changed.
  - Error classes unchanged: yes; invalid/overlapping/out-of-bounds
    `MergeByteRange` validation remains in `merge_non_overlapping_ranges`.
  - Serde/log contract unchanged: yes; the range-overlay variants keep the same
    externally tagged variant names and `touched_ranges` array representation.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - `merge_proof_inline_ranges_preserve_serde_golden_bd_6tooy` printed and
    asserted
    `e3a0f052016fa83985a7b1968c06b0d21589999a1e441915fff89c2d8c88a324`.
- Next route: the deeper staging target is fusing transaction bytes and merge
  proof metadata to remove the second `BTreeMap` insertion/lookup per staged
  block; do not continue micro-tuning range construction.

## Current Pass 46
- Bead: `bd-xmh5g.173`.
- Target: profile-backed `ext4_write_path/ext4_fragmented_overwrite_256ext`
  staging overhead after pass 45.
- Alien primitive: fuse the hot staged-write record into one sorted map
  (`BlockNumber -> StagedWrite { bytes, merge_proof }`) instead of maintaining
  parallel `writes` and `merge_proofs` maps. This removes the second BTreeMap
  insert/lookup while keeping the externally visible transaction JSON shape.
- Baseline:
  - RCH same worker `vmi1152480`
    `cargo bench --profile release-perf -p ffs-harness --bench metadata_parse -- ext4_write_path/ext4_fragmented_overwrite_256ext --warm-up-time 1 --measurement-time 3 --sample-size 10 --noplot`
    produced `[14.363 ms,15.545 ms,17.550 ms]`.
- Rebench:
  - RCH same worker `vmi1152480`, same focused row, produced
    `[14.501 ms,14.799 ms,15.013 ms]`.
  - Midpoint speedup: `15.545 / 14.799 = 1.0504x`.
  - A final exact-code rebench after the serde guardrail was attempted on
    `vmi1152480` but overlapped a concurrent ffs-core commit and built the
    pre-commit `child_data.as_slice()` source, then was cancelled after the
    failed release-perf compile. A rerun was scheduled on `vmi1149989`, so it
    was cancelled as non-comparable. The post-benchmark serde guardrail is not
    on the ext4 write staging hot path.
- Behavior proof:
  - RCH remote `vmi1152480`
    `cargo test -j 1 -p ffs-mvcc transaction_ -- --nocapture --test-threads=1`
    passed: 11 focused lib tests plus the matching evidence integration test.
  - RCH remote `vmi1149989`
    `cargo check -j 1 -p ffs-mvcc -p ffs-core --all-targets` passed.
  - RCH remote `vmi1152480`
    `cargo clippy -j 1 -p ffs-mvcc --all-targets -- -D warnings`
    passed.
  - After adding the legacy-JSON `#[serde(default)]` guard for missing
    `merge_proofs`, RCH remote `vmi1293453`
    `cargo test -j 1 -p ffs-mvcc transaction_ -- --nocapture --test-threads=1`
    passed again, and RCH remote `vmi1152480`
    `cargo clippy -j 1 -p ffs-mvcc --all-targets -- -D warnings` passed.
  - RCH remote `vmi1149989`
    `cargo clippy -j 1 -p ffs-mvcc -p ffs-core --all-targets -- -D warnings`
    reached `ffs-mvcc` cleanly but failed in unrelated dirty `ffs-core`
    bd-v388x test code on two stale `unused_mut` diagnostics while that file
    was changing concurrently; not used as the MVCC proof gate.
  - Local `cargo fmt --check -p ffs-mvcc` passed.
  - Local `git diff --check` passed.
- Score: kept. Impact `1.05` x Confidence `4.0` / Effort `2.0` = `2.10`.
- Isomorphism:
  - Ordering preserved: yes; writes still live in one `BTreeMap<BlockNumber,
    _>`, so iteration and shard-lock ordering remain ascending block order.
  - Tie-breaking unchanged: yes; conflict policy selection, latest-version
    checks, and merge-proof validation are unchanged. The proof now travels
    with the staged bytes that already owned the block key.
  - Abort semantics unchanged: yes; strict FCW/SSI conflicts return the
    original staged bytes and proof together, covered by focused tests.
  - Serde/log contract unchanged: yes; manual `Transaction` serde emits and
    accepts the legacy `writes` and `merge_proofs` maps, and the round-trip test
    asserts that shape.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - `git diff --exit-code -- conformance/fixtures conformance/golden
    tests/fixtures` passed.
  - Aggregate fixture/golden manifest:
    `9709dd044f40a7b3422976dcbd0d04b1e778596f64d42c0cf062e3a65f94e61e`.
- Next route: re-run live `[perf]` triage after committing/pushing, then take
  the next open profile-backed bead that is not already claimed.

## Current Pass 47
- Bead: `bd-307e4`.
- Target: profile-backed `btrfs_read_file` multi-extent compressed read gather
  after `bd-m6g2o` moved decompression itself into rayon.
- Alien primitive: move the independent per-extent compressed-blob device reads
  into the same parallel job graph as decompression. The serial pass still
  validates metadata, overlap, and size limits, while the parallel phase only
  performs independent read+decompress jobs and stores results by original
  extent index.
- Baseline:
  - RCH remote `vmi1153651`
    `cargo bench -j 1 -p ffs-core --bench btrfs_decompress_extents --
    btrfs_read_gather_latency_n16_4k`.
  - The same-process A/B row uses `serial_extent_reads` as the baseline over
    the same latency-injected `BlockDevice` and deterministic block payloads:
    `[2.1352 ms,2.2982 ms,2.4676 ms]`.
- Rebench:
  - Same RCH criterion process on `vmi1153651`, `parallel_extent_reads`:
    `[903.88 us,1.1970 ms,1.5098 ms]`.
  - Midpoint speedup: `2.2982 / 1.1970 = 1.9200x`.
- Behavior proof:
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-core btrfs_read -- --nocapture
    --test-threads=1` passed: 21 focused btrfs read tests, 1 ignored perf
    A/B, 1102 filtered.
  - RCH remote `vmi1153651`
    `cargo check -j 1 -p ffs-core --all-targets` passed.
  - RCH remote `vmi1149989`
    `cargo clippy -j 1 -p ffs-core --all-targets -- -D warnings` passed.
  - Local `cargo fmt --check -p ffs-core` passed.
  - Local `git diff --check` passed.
- Score: kept. Impact `1.92` x Confidence `3.2` / Effort `2.0` = `3.07`.
- Isomorphism:
  - Ordering preserved: intended yes; results are collected by extent index and
    consumed by the existing serial assembly loop in original extent order.
  - Tie-breaking unchanged: intended yes; malformed extents and I/O/decompress
    errors are returned through the lowest original extent index.
  - Output layout unchanged: intended yes; all destination slice copies remain
    in the serial assembly loop.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - Aggregate fixture/golden manifest:
    `9709dd044f40a7b3422976dcbd0d04b1e778596f64d42c0cf062e3a65f94e61e`.

## Current Pass 50
- Bead: `bd-xmh5g.180`.
- Target: profile-backed ffs-alloc multi-block allocation group miss path.
  RCH `ovh-a` bitmap profile showed rescanning 128 group bitmaps for largest
  run at `164.38 us` median while cached group summaries took `78.830 ns`.
- Lever: before reading/searching a group bitmap, `try_alloc_in_group` and
  `try_alloc_safe` now skip multi-block requests when exact
  `GroupStats::cached_block_largest_free_run()` is `Some(run) < count`.
  `None` preserves the old bitmap-probe path.
- Baseline/Rebench:
  - RCH remote `vmi1152480`
    `cargo bench --profile release-perf -j 1 -p ffs-alloc --bench
    batch_alloc -- alloc_contiguous_cached_skip_128groups --warm-up-time 1
    --measurement-time 3 --sample-size 10 --noplot`.
  - `cache_disabled_bitmap_probe`: `[672.74 us,741.87 us,869.27 us]`.
  - `exact_cache_skip`: `[35.556 us,43.386 us,53.789 us]`.
  - Same-binary midpoint ratio: `741.87 / 43.386 = 17.098x`.
- Behavior proof:
  - The criterion bench asserts the cache-disabled and exact-cache paths return
    the identical `BlockAlloc` before measuring.
  - RCH remote `vmi1293453`
    `cargo check -j 1 -p ffs-alloc --all-targets` passed.
  - RCH remote `vmi1227854`
    `cargo test -j 1 -p ffs-alloc --lib -- --nocapture --test-threads=1`
    passed: 206 tests, including contiguous-search and largest-free-run golden
    report tests.
  - RCH remote `vmi1152480`
    `cargo clippy -j 1 -p ffs-alloc --all-targets -- -D warnings` passed.
  - Local `cargo fmt --check -p ffs-alloc` passed.
  - Local `git diff --check -- crates/ffs-alloc/src/lib.rs
    crates/ffs-alloc/benches/batch_alloc.rs` passed.
- Score: kept. Impact `4.0` x Confidence `4.0` / Effort `1.5` = `10.67`.
- Isomorphism:
  - Ordering preserved: yes; group traversal order and successful allocation
    search order are unchanged. The new guard only removes groups whose exact
    cached largest run proves no contiguous run of `count` can exist.
  - Tie-breaking unchanged: yes; when a group can satisfy the request, the same
    bitmap scan chooses the same first run. `None` cache entries keep the old
    path.
  - Output layout unchanged: yes; returned `BlockAlloc` shape and group stats
    mutation path are unchanged for successful allocations.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: N/A.
- Golden SHA256:
  - `git diff --exit-code -- conformance/fixtures conformance/golden
    tests/fixtures` passed.
  - Tracked fixture/golden content manifest over 82 tracked files:
    `370d1cedf7f6986e11996b33fd115f6fd86ead5724207451ad68d00c5e34702b`.
  - Existing manifest caveat: `sha256sum -c conformance/fixtures/checksums.sha256`
    is already stale for missing `ext4_fast_commit_clean_replay.json` and
    `ext4_fast_commit_fallback_missing_tail.json`; those paths are not touched
    by this pass.

## Current Pass 51
- Bead: `bd-xmh5g.178`.
- Target: profile-backed parser primitive density from
  `docs/perf/2026-06-01_plumfern/HOTSPOT_TABLE.md` rank 3 / H5:
  `ensure_slice` + `read_le_*` + `Result` branch overhead in metadata parse
  paths.
- Baseline:
  - RCH remote `vmi1152480`
    `cargo bench -j 1 -p ffs-harness --bench metadata_parse --
    metadata_parse --warm-up-time 1 --measurement-time 3 --sample-size 10
    --noplot`.
  - `metadata_parse`: `[284.69 ns,304.27 ns,334.38 ns]`.
- Candidate tried and rejected before promotion:
  - One-lever patch added hot success paths for `read_le_u16/u32/u64` and a
    cold shared fixed-width error helper intended to preserve existing
    `ParseError` overflow/insufficient-data semantics.
  - Same-worker rebench on `vmi1152480` timed out before criterion output:
    first rebench hit rch `RCH-E104` after 1800s; the attempted remote target
    reuse was rewritten by rch into a fresh job target, so that second cold
    build was terminated locally before wasting another timeout window.
  - Runtime source change was manually reverted; no parser code was kept.
- Score: rejected. Impact cannot be scored without a valid after timing; kept
  code score is `0`.
- Isomorphism:
  - Shipped code is unchanged after manual revert.
  - Candidate intended ordering/tie-breaking/FP/RNG behavior: N/A for scalar
    little-endian reads; exact error-variant parity was required but not
    promoted without rebench proof.
- Golden SHA256:
  - Not recomputed for the reverted candidate; no fixture or golden files were
    touched.
- Next route:
  - Do not repeat this helper micro-lever through the heavy `metadata_parse`
    bench unless a persistent rch target or lighter parser microbench is first
    established. Prefer the rank-1 structural read-path/syscall batching
    primitive or another existing measured bench with reliable crate-scoped
    turnaround.

## Current Pass 50
- Beads: `bd-mxq92` plus duplicate tracker row `bd-xmh5g.177`.
- Target: `ffs-alloc` `SuccinctBitmap::select1` final in-block scan.
- Outcome: kept one production lever. `select1` now reuses a word-level
  `select1_in_block` helper, symmetric with the existing `select0_in_block`,
  instead of scanning the selected 256-bit rank block one bit at a time.
- Baseline/Rebench:
  - Baseline RCH remote `ovh-a`, detached clean worktree at `8ec81156`:
    `cargo bench --profile release-perf -j 1 -p ffs-alloc --bench bitmap_ops
    -- select1 --warm-up-time 1 --measurement-time 3 --sample-size 10
    --noplot`.
  - Production baseline `succinct_select1`:
    `[16.400 ns,18.603 ns,21.637 ns]`.
  - Baseline primitive A/B `select1_in_block/old_bit_scan`:
    `[24.591 ns,26.207 ns,29.388 ns]`.
  - Baseline primitive A/B `select1_in_block/new_broadword`:
    `[9.1929 ns,9.6397 ns,10.816 ns]`.
  - Candidate RCH remote `ovh-a`, dirty worktree with the one lever:
    same command and benchmark filter.
  - Production after `succinct_select1`:
    `[11.595 ns,11.637 ns,11.680 ns]`.
  - Production midpoint ratio: `18.603 / 11.637 = 1.60x`
    (`37.4%` median reduction).
- Behavior proof:
  - The criterion A/B helper asserts the old bit-scan and broadword selectors
    return identical bit positions before measuring.
  - RCH remote `ovh-a`
    `cargo test -j 1 -p ffs-alloc select1 -- --nocapture
    --test-threads=1` passed: `2` selected tests, including the
    rank1/select1 proptest.
  - RCH remote `ovh-a`
    `cargo check -j 1 -p ffs-alloc --all-targets` passed.
  - RCH remote `ovh-a`
    `cargo clippy -j 1 -p ffs-alloc --all-targets -- -D warnings` passed
    after removing the obsolete fulfilled `select1` lint expectation.
  - Local `cargo fmt --check -p ffs-alloc` passed.
  - Local `git diff --check` passed.
  - Local `git diff --exit-code -- conformance/fixtures conformance/golden
    tests/fixtures` passed.
- Score: kept. Impact `3` x Confidence `3` / Effort `2` = `4.5`.
- Isomorphism:
  - Ordering preserved: yes; superblock/block selection is unchanged, and the
    helper walks 64-bit words in ascending order before selecting the
    `remaining`-th set bit in that word.
  - Tie-breaking unchanged: yes; `select1` returns the unique k-th set bit in
    ascending bit-position order.
  - Output layout unchanged: yes; returned positions and `None` behavior keep
    the same `Option<u32>` contract, with the existing `k >= self.ones` guard.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: deterministic benchmark bitmap unchanged.
- Golden SHA256:
  - Aggregate fixture/golden manifest:
    `9709dd044f40a7b3422976dcbd0d04b1e778596f64d42c0cf062e3a65f94e61e`.

## Current Pass 49
- Bead: `bd-xmh5g.174`.
- Target: profile-backed scalar owned-block fallback from `bd-kq3b4` and
  `docs/perf/2026-06-01_plumfern`: `OpenFs::read_block_with_scope` still
  routed base-device misses through `BlockBuf::zeroed` and a trailing
  `as_slice().to_vec()` even when the caller already needed owned `Vec<u8>`.
- Lever: add a private direct owned-`Vec` fallback. The public scope reader
  still resolves staged writes first, tx snapshot-visible MVCC bytes second,
  and the no-tx/current fallback third. The fallback now checks the current
  MVCC snapshot and then reads base-device bytes directly into the final
  `Vec`, avoiding the transient aligned `BlockBuf` and second copy.
- Baseline:
  - RCH remote `ovh-a`
    `cargo bench --profile release-perf -j 1 -p ffs-block --bench
    block_buf_construct -- block_buf_via_aligned_zeroed --warm-up-time 1
    --measurement-time 3 --sample-size 10 --noplot`.
  - `block_buf_via_aligned_zeroed`: `[179.91 ns,180.58 ns,181.31 ns]`.
- Rebench:
  - Same worker `ovh-a`, same bench binary after adding
    `block_vec_direct_final_buffer`:
    `cargo bench --profile release-perf -j 1 -p ffs-block --bench
    block_buf_construct -- --warm-up-time 1 --measurement-time 3
    --sample-size 10 --noplot`.
  - `block_buf_via_aligned_zeroed`: `[176.21 ns,177.49 ns,178.43 ns]`.
  - `block_vec_direct_final_buffer`: `[72.228 ns,72.321 ns,72.383 ns]`.
  - Same-binary midpoint ratio: `177.49 / 72.321 = 2.4542x`.
- Behavior proof:
  - The criterion bench asserts the direct final `Vec` bytes match the aligned
    `BlockBuf` bytes before measuring.
  - RCH remote `ovh-a`
    `cargo test -j 1 -p ffs-core --lib
    read_block_with_empty_scope_matches_current_adapter_view -- --nocapture
    --test-threads=1` passed.
  - RCH remote `ovh-a`
    `cargo test -j 1 -p ffs-core --lib
    write_setxattr_sequential_external_block_rmw_across_ops_roundtrips --
    --nocapture --test-threads=1` passed.
  - RCH remote `ovh-a` `cargo check -j 1 -p ffs-core --lib` passed.
  - RCH remote `ovh-a`
    `cargo clippy -j 1 -p ffs-core --lib -- -D warnings` passed.
  - RCH remote `ovh-a`
    `cargo check -j 1 -p ffs-block --bench block_buf_construct` passed.
  - RCH remote `ovh-a`
    `cargo clippy -j 1 -p ffs-block --bench block_buf_construct --
    -D warnings` passed.
  - Local `rustfmt --edition 2024 --check crates/ffs-core/src/lib.rs
    crates/ffs-block/benches/block_buf_construct.rs` passed.
  - Local `cargo fmt --check -p ffs-core` passed. `cargo fmt --check -p
    ffs-block` is blocked by unrelated pre-existing formatting drift in
    `crates/ffs-block/src/lib.rs`; touched `ffs-block` bench formatting is
    file-scoped checked.
  - Local `git diff --check` passed.
- Score: kept. Impact `2.45` x Confidence `3.5` / Effort `2.0` = `4.29`.
- Isomorphism:
  - Ordering preserved: yes; staged write, tx snapshot-visible MVCC, and
    no-tx/current fallback precedence is unchanged.
  - Tie-breaking unchanged: yes; the newest visible MVCC version remains chosen
    by the existing store resolver, and base-device range errors keep the same
    checks/messages as the block adapter.
  - Output layout unchanged: yes; callers still receive one full filesystem
    block as owned `Vec<u8>`.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: deterministic bench source payload unchanged.
- Golden SHA256:
  - Aggregate fixture/golden manifest:
    `9709dd044f40a7b3422976dcbd0d04b1e778596f64d42c0cf062e3a65f94e61e`.

## Current Pass 48
- Bead: `bd-wipi2`.
- Target: measurement substrate for btrfs multi-extent I/O overlap.
- Outcome: measurement substrate landed via
  `crates/ffs-core/benches/btrfs_read_io_overlap.rs`: a generic
  `LatencyBlockDevice<D: BlockDevice>` decorator around a blob-backed
  `BlockDevice`, with serial-read-then-par-decompress and
  par-read-and-decompress criterion arms.
- Baseline/Rebench:
  - RCH remote `vmi1293453`
    `cargo bench -j 1 -p ffs-core --bench btrfs_read_io_overlap --
    btrfs_read_io_overlap_16extents --warm-up-time 1 --measurement-time 3
    --sample-size 10 --noplot`.
  - `serial_read_then_par_decompress`:
    `[8.3999 ms,8.9612 ms,10.071 ms]`.
  - `par_read_and_decompress`: `[2.3103 ms,2.4837 ms,2.7313 ms]`.
  - Midpoint ratio: `8.9612 / 2.4837 = 3.6072x`.
- Behavior proof:
  - The criterion bench asserts byte-identical decompressed extents before
    measuring.
  - RCH remote `vmi1227854`
    `cargo check -j 1 -p ffs-core --bench btrfs_read_io_overlap` passed.
  - RCH remote `vmi1149989`
    `cargo clippy -j 1 -p ffs-core --bench btrfs_read_io_overlap --
    -D warnings` passed after removing the redundant `+ Sync` bound already
    implied by `BlockDevice`.
  - Local `cargo fmt --check -p ffs-core` passed.
  - Local `git diff --check` passed.
- Score: measurement closeout, not a production optimization lever.
- Isomorphism:
  - Ordering preserved: yes; both arms sort by original extent index before
    returning decompressed blobs.
  - Tie-breaking unchanged: N/A for the component bench; it panics on bench
    setup/read/decompress failure in either arm.
  - Output layout unchanged: yes; one decompressed blob per original extent.
  - Floating-point identical: N/A.
  - RNG seeds unchanged: deterministic PRNG seed formula unchanged.
- Golden SHA256:
  - Aggregate fixture/golden manifest:
    `9709dd044f40a7b3422976dcbd0d04b1e778596f64d42c0cf062e3a65f94e61e`.
