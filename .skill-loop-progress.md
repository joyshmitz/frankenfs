# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: FrankenFS perf campaign
# Total Passes: 7
# Started: 2026-06-04T23:01:50-04:00

## Status: IN PROGRESS - Loop 6 Pass 7 rejected; next primitive is compact ring/index or sharded S3-FIFO policy ownership

## Missions
1. Tracker and ownership: finish `bd-xmh5g.63`, confirm no ready perf child, avoid BlackThrush/CrimsonDove surfaces, and file `bd-xmh5g.64` for unowned `ffs-mvcc`.
2. Baseline and primitive: use fresh RCH `ts1` `wal_throughput` baseline; harvest flat-combining/helping contiguous-prefix publication from alien-graveyard synchronization primitives.
3. Single lever: replace one-commit-at-a-time publication waits with a ready-commit scoreboard that advances `completed_commit` over the installed contiguous prefix.
4. Verification and rebench: run crate-scoped RCH behavior proof, golden sha256, fmt/diff checks, and same-worker Criterion rebench.
5. Commit, close, and reprofile: keep only if Score>=2.0, close `bd-xmh5g.64`, push `main` and `main:master`, then re-triage for shifted perf bottlenecks.
6. Checkpoint bulk encode: finish `bd-gliqk` by keeping the profile-backed contiguous checkpoint payload encoder only if golden SHA, crate-scoped RCH gates, and the filtered checkpoint Criterion row clear the keep gate.
7. S3-FIFO read-hit mirror: finish `bd-xmh5g.66` by keeping the safe-Rust large-cache S3-FIFO read-hit mirror only if golden SHA, crate-scoped RCH gates, mutation-gated isomorphism proof, and the same-worker hot-read Criterion row clear the keep gate.

## Completed Passes
1. Tracker and ownership: `bd-xmh5g.63` closed and pushed; no ready perf children after the wait; active perf children are owned by BlackThrush (`ffs-btree`/`ffs-extent`) and CrimsonDove (`ffs-repair`); filed `bd-xmh5g.64`.
2. Baseline and primitive: RCH `ts1` baseline selected `sharded_mvcc_disjoint_32writers` [185.43, 200.66, 221.01] ms and `16writers` [37.655, 40.481, 42.633] ms; chosen primitive is a mutex-protected ready-prefix scoreboard, not the rejected CAS-only fast path.
3. Single lever: commit `91f0b8a5` changes only `crates/ffs-mvcc/src/sharded.rs`, replacing one-commit-at-a-time publication waits with a `PublicationState` ready-commit `BTreeSet` under the existing publication gate mutex.
4. Verification and rebench: RCH `cargo test -p ffs-mvcc sharded --lib -- --nocapture --test-threads=1` passed 35/35 on `ts2`; RCH `cargo check -p ffs-mvcc --all-targets` passed; RCH `cargo clippy -p ffs-mvcc --all-targets -- -D warnings` passed; `cargo fmt --package ffs-mvcc --check` and `git diff --check` passed; normalized golden `SHARDED_PUBLISH_GOLDEN|ready_prefix=2,3|release=1|completed=3` sha256 is `81d8e6714b2da708a4b69f9b782d4a1996345d513706d1fd0827477060f010a5`.
5. Commit, close, and reprofile: same-worker `ts2` clean-baseline vs after Criterion rebench improved `sharded_mvcc_disjoint_16writers` mean `27.361 ms -> 4.9218 ms` and `32writers` mean `168.39 ms -> 13.631 ms`; Score `8.33 = Impact 5 x Confidence 5 / Effort 3`; `bd-xmh5g.64` closed with proof.
6. Checkpoint bulk encode: `bd-gliqk` kept the contiguous checkpoint payload encoder in `crates/ffs-mvcc/src/persist.rs`; RCH `ts1` `checkpoint_golden_report` passed with `CHECKPOINT_GOLDEN|len=824|sha256=7adbdd643cc6f2d7d0512813d9c0db954a84af4c4b22b248401aafc70f9a8e17`; RCH `ts1` `cargo check -p ffs-mvcc --all-targets` passed; RCH `ts1` `cargo clippy -p ffs-mvcc --all-targets -- -D warnings` passed; RCH `ts1` `cargo test -p ffs-mvcc checkpoint --lib -- --nocapture --test-threads=1` passed 12/12; local `cargo fmt --package ffs-mvcc --check` and `git diff --check` passed. RCH `ts1` Criterion improved `persistent_mvcc_checkpoint_256blocks_8versions` mean `12.254 ms -> 10.670 ms` (`~1.15x`); Score `3.0 = Impact 3 x Confidence 4 / Effort 4`.
7. S3-FIFO read-hit mirror: `bd-xmh5g.66` kept a safe-Rust sharded fast resident mirror for large S3-FIFO `ArcCache` read hits in `crates/ffs-block/src/lib.rs`; RCH `ts2` golden passed with `BLOCK_CACHE_HOT_READ_GOLDEN|rounds=4|blocks=32|hits=66|misses=62|resident=32|capacity=512|first_byte_sum=15580|payload_sha256=4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2|sha256=63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5`; RCH `ts2` `cargo check -p ffs-block --all-targets --features s3fifo` passed; RCH `ts2` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings` passed; local `cargo fmt --package ffs-block --check` and `git diff --check` passed. Isomorphism: ordering/tie-breaking preserved because misses, writes, evictions, admissions, and small-cache hits still use the authoritative locked S3-FIFO state machine; large-cache fast hits only clone resident bytes and atomically saturate the shared access-count handle; mutation epoch gating rejects mirror hits overlapping writes/evictions/pressure trims; floating-point N/A; RNG unchanged/N/A. RCH `ts2` Criterion improved `block_cache_s3fifo_concurrent_hot_read_64threads` mean `314.06 ms -> 25.835 ms` (`~12.16x`); sharded comparator stayed healthy at `11.309 ms -> 9.5140 ms`; Score `12.5 = Impact 5 x Confidence 5 / Effort 2`.

## Loop 2 Missions
1. `bd-xmh5g.59` ffs-btree sorted append-run flush: close or reject the owned Bw-tree profile target with one structural lever, RCH crate-scoped proof, golden sha256, and same-worker rebench.
2. `bd-xmh5g.58` ffs-extent eviction metadata: close or reject the owned ExtentCache capacity-insert profile target with one O(1)/amortized eviction lever and crate-scoped proof.
3. Re-triage perf queue: wait briefly for cc-filed ready perf beads; if none are claimable, create or claim only a disjoint profile-backed hotspot outside active `ffs-block`/`ffs-repair` ownership.
4. Alien primitive pass: harvest a deeper safe-Rust primitive from the graveyard/artifact workflow for the newly profiled bottleneck, score it, and implement only if Score>=2.0.
5. Closeout/reprofile pass: close the bead, push `main` and `main:master`, then re-profile because bottlenecks shift.

## Loop 2 Completed Passes
1. `bd-xmh5g.59` ffs-btree sorted append-run flush: REJECT/HANDOFF, not a keep. RCH `ts2` `cargo test -p ffs-btree bw_tree --lib -- --nocapture --test-threads=1` passed 54/54 and printed the Bw-tree golden reports; RCH `ts2` `cargo check -p ffs-btree --all-targets` passed. RCH `ts2` `cargo clippy -p ffs-btree --all-targets -- -D warnings` failed on `clippy::needless-pass-by-value` for `BufferedMutationEntry` in `BufferedMessages::singleton`/`upsert`. Review found the dirty `bw_tree.rs` diff is mostly a `MessageBuffer` representation change (`BTreeMap` -> sorted `Arc<Vec<_>>`) while the append-run flush machinery already exists in `HEAD`; the bead is assigned to BlackThrush, so Codex added a comment with the evidence and did not close or revert the peer-owned surface.
2. `bd-rlsua` ffs-alloc zero-word summary for succinct `find_free`: REJECT, no code kept. The side-table lookup row improved but the whole lever failed because same-worker `succinct_build` regressed from `[716.03, 765.80, 804.80] ns` to `[1.5005, 1.6023, 1.7564] us`; RCH proof and canonical sorted `SUCCINCT_SELECT0_GOLDEN` sha256 `bb12fd98905f1fdcb0bd0ca8751e6ff851e1d575f680ad332664b69dcb9529a5` passed; next primitive named in bead closeout is fused zero discovery without an extra build pass/vector.
3. `bd-xmh5g.74` ffs-alloc direct block-summary `find_free` select: REJECT, no code kept. Baseline RCH `ts2` clean `find_free/succinct_O(log_n)` `[18.127, 18.219, 18.299] ns`; proof passed 29 focused succinct tests with canonical sorted golden sha256 `bb12fd98905f1fdcb0bd0ca8751e6ff851e1d575f680ad332664b69dcb9529a5`; after RCH `ts2` regressed to `[30.230, 30.345, 30.476] ns`, so the block-level rank arithmetic lever was reverted. Next target: a deeper fused metadata layout or all-one superblock skip that includes build cost in the baseline.
4. `bd-xmh5g.75` ffs-alloc packed has-zero block metadata: REJECT, no code kept. Baseline RCH `ts2` clean `find_free/succinct_O(log_n)` `[18.127, 18.219, 18.299] ns` and `succinct_find_free_ab/direct_word_scan` `[18.112, 18.153, 18.188] ns`; RCH `ts2` proof passed 29 focused succinct tests and canonical sorted `SUCCINCT_SELECT0_GOLDEN` sha256 `bb12fd98905f1fdcb0bd0ca8751e6ff851e1d575f680ad332664b69dcb9529a5`; after RCH `ts2` regressed to `find_free/succinct_O(log_n)` `[20.523, 20.571, 20.600] ns` and `direct_word_scan` `[20.306, 20.385, 20.476] ns`. Next primitive: two-level all-one run directory or broadword/SWAR zero-run skip that reduces both build and lookup cost.
5. `bd-xmh5g.78` ffs-alloc broadword find-contiguous word run detector: KEPT. Fresh profile-backed target was `find_contiguous/plain_32_O(n)` with RCH `ts2` focused baseline `[55.843, 56.068, 56.466] ns`, `find_contiguous_ab/old_byte_scan` `[74.176, 74.452, 74.685] ns`, and pre-lever `word_at_a_time` `[56.383, 56.497, 56.653] ns`. One lever replaced the mixed-u64 bit loop in `apply_contiguous_word_zero_run` with a safe-Rust broadword first-run detector. RCH `ts1` proof passed `cargo test -p ffs-alloc bitmap_find_contiguous --lib -- --nocapture --test-threads=1` 12/12 including proptests; canonical `FIND_CONTIG_GOLDEN` sha256 `4fc566c5db095dba8a62111de1963f8bd20b0ab0ad74ae4c0be60d256f37da6c`; RCH `ts1` same-binary A/B after improved `old_byte_scan` `[103.28, 104.85, 106.66] ns` to `word_at_a_time` `[20.735, 20.891, 21.064] ns` (`5.02x`), with production row `[19.915, 20.157, 20.415] ns`. Isomorphism: ordering preserved by checking crossing prefix before in-word starts and using `starts.trailing_zeros()` for the lowest valid in-word run; tie-breaking unchanged lowest-position run wins; floating-point N/A; RNG unchanged/N/A. Gates passed: RCH `cargo check -p ffs-alloc --all-targets`, RCH `cargo clippy -p ffs-alloc --all-targets -- -D warnings`, local `cargo fmt --package ffs-alloc --check`, and `git diff --check`. Score `4.5 = Impact 5 x Confidence 4.5 / Effort 5`.

## Loop 3 Missions
1. Tracker and profile: verify no unowned ready `[perf]` bead exists, avoid assigned `ffs-block`/`ffs-btree`/`ffs-extent`/`ffs-repair` surfaces, and get a fresh crate-scoped RCH profile on an unowned target.
2. Primitive selection: harvest a deeper safe-Rust allocator primitive from alien-graveyard/artifact guidance and score it with Impact x Confidence / Effort.
3. Single lever: implement exactly one behavior-preserving optimization for the profiled target, with no unrelated formatting or peer-owned file changes staged.
4. Proof and rebench: run focused RCH tests, golden-output sha256 verification, crate-scoped check/clippy/fmt/diff checks, and same-worker RCH Criterion rebench.
5. Closeout and reprofile: keep only if Score >= 2.0, commit with before/after numbers, close the bead, push `main` and `main:master`, then re-triage for shifted bottlenecks.

## Loop 3 Completed Passes
1. Tracker and profile: read-only subagent confirmed no unowned ready `[perf]` bead; active ready perf surfaces are assigned to PlumFern (`ffs-block`), BlackThrush (`ffs-btree`/`ffs-extent`), and CrimsonDove (`ffs-repair`). Fresh RCH `ts1` crate-scoped baseline selected unowned `ffs-alloc` largest-run cache invalidation: `largest_free_run_cached_ab/bitmap_scan_128_groups` `[196.40 us, 202.99 us, 207.93 us]`, `cached_group_stats_128_groups` `[91.999 ns, 93.330 ns, 94.687 ns]`, and `largest_free_run/fragmented_mixed_bytes` `[1.4691 us, 1.4860 us, 1.4987 us]`. Filed and claimed `bd-xmh5g.84` with Score gate `6.67 = Impact 5 x Confidence 4 / Effort 3`.
2. Primitive selection: alien-graveyard/artifact pass chose exact affected-cell summary maintenance over new metadata structures. One lever: replace largest-run cache invalidation in allocator bitmap mutation paths with rollback-aware `GroupStats::refresh_block_largest_free_run(&bitmap, blocks_in_group)` from the already-mutated bitmap, while preserving allocation search order, tie-breaking, bitmap/GDT persistence, checksum stamping, and rollback snapshots. Fallback trigger: reject/revert if same-worker RCH shows mutation-heavy regression outweighs the query-path cache win or rollback tests expose stale cache state.
3. `bd-xmh5g.84` ffs-alloc largest-run cache refresh: KEPT. One lever refreshed `GroupStats::block_largest_free_run` from the already-mutated bitmap in allocation/free mutation paths and updated tests to assert exact cache-vs-bitmap equality. Proof passed: RCH `ts1` `cargo check -p ffs-alloc --all-targets`; RCH `ts1` `cargo test -p ffs-alloc persist --lib -- --nocapture --test-threads=1` 15/15; RCH `ts1` `cargo test -p ffs-alloc bitmap_largest_free_run_golden_report --lib -- --nocapture --test-threads=1` with body sha256 `9d116d89e41ada0d014125e3fd46991b30949d366122c38debc5adf9f9517396` and marker-inclusive sha256 `4d3300c701fc62e241ad715f89ccf4701ecb6b72ced820ca0922cc8a18dbf9b0`; RCH `ts1` `cargo clippy -p ffs-alloc --all-targets -- -D warnings`; local `cargo fmt --package ffs-alloc --check`; `git diff --check`. RCH `ts1` post-change `bitmap_ops`: `bitmap_scan_128_groups` `[192.15 us, 194.39 us, 198.19 us]`, `cached_group_stats_128_groups` `[91.125 ns, 92.068 ns, 93.165 ns]`. Mutation bench also improved versus bead baseline: `single_20x1` `[51.412, 51.590, 51.981] us -> [35.098, 35.857, 36.494] us`, `single_100x1` `[244.96, 245.61, 246.04] us -> [172.06, 176.27, 182.53] us`, `batch_20` `[2.2864, 2.2909, 2.3025] us -> [1.6968, 1.7231, 1.7477] us`, and `batch_100` `[2.6579, 2.6649, 2.6716] us -> [1.8923, 1.9753, 2.0398] us`. Isomorphism: allocation search order and lowest-position tie-breaking unchanged; cache is derived from the exact mutated bitmap after the same successful bitmap write; GDT/checksum persistence unchanged; rollback restores previous cache snapshot; FP N/A; RNG unchanged/N/A. Score `10.0 = Impact 5 x Confidence 5 / Effort 2.5`.
4. `bd-xmh5g.85` ffs-alloc halfword largest-run summary: KEPT. One lever replaced the mixed-u64 iterative longest-zero-run detector with a safe-Rust precomputed 16-bit `(prefix,suffix,best)` summary table composed four times per word. Proof passed: RCH `ts1` `cargo test -p ffs-alloc bitmap_largest_free_run --lib -- --nocapture --test-threads=1` 6/6 including naive-scan proptest; `LARGEST_FREE_RUN_GOLDEN` body sha256 unchanged `9d116d89e41ada0d014125e3fd46991b30949d366122c38debc5adf9f9517396`; RCH `ts1` `cargo check -p ffs-alloc --all-targets`; RCH `ts1` `cargo clippy -p ffs-alloc --all-targets -- -D warnings`; local `cargo fmt --package ffs-alloc --check`; `git diff --check`. Same-worker RCH `ts1` Criterion improved `largest_free_run/fragmented_mixed_bytes` mean `2.6592 us -> 2.0137 us`, `largest_free_run_cached_ab/bitmap_scan_128_groups` mean `329.56 us -> 243.24 us`, and `cached_group_stats_128_groups` mean `103.52 ns -> 92.685 ns`. Isomorphism: full-zero/full-one fast paths unchanged; mixed words compose low-to-high from exact halfword summaries, preserving LSB-first semantics and partial-byte masking; ordering/tie-breaking N/A because only max run length is returned; FP N/A; RNG unchanged/N/A. Score `6.67 = Impact 4 x Confidence 5 / Effort 3`.

## Loop 4 Missions
1. Re-triage ready `[perf]` beads after `bd-xmh5g.85`, avoid CrimsonDove/BlackThrush surfaces, and claim only the PlumFern-owned `ffs-block` target.
2. Establish a fresh current-HEAD RCH `ts1` baseline for `block_cache_s3fifo_concurrent_hot_read_64threads` before editing.
3. Test one safe-Rust fast-hit overhead lever at a time, rejecting any lever that does not clear the same-worker Score gate.
4. Keep behavior proof absolute: golden sha256, check/clippy/fmt/diff, ordering/tie-breaking/FP/RNG proof.
5. Closeout, push, and re-profile into a deeper primitive if the first lever fails.

## Loop 4 Completed Passes
1. `bd-xmh5g.67` ffs-block per-slot fast-hit counter: REJECT, no code kept. Fresh RCH `ts1` current-HEAD baseline selected `block_cache_s3fifo_concurrent_hot_read_64threads` `[15.647, 15.904, 16.355] ms` versus sharded comparator `[8.9055, 9.0122, 9.1441] ms`; source already had the direct resident-slot table shape, so the attempted one lever moved fast-hit metrics from one global `AtomicU64` into per-slot counters. Proof before rebench passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench regressed to S3-FIFO `[34.849, 36.988, 40.771] ms` and sharded comparator `[30.839, 32.466, 34.799] ms`; Score `< 2.0`, source restored to HEAD. Isomorphism for rejected lever: cached bytes and authoritative S3-FIFO ordering/tie-breaking unchanged; metrics hit totals moved from one relaxed counter to a saturating sum of relaxed per-slot counters; FP N/A; RNG unchanged/N/A. Next deeper primitive: target the shared fast-hit reset flag / clone+lock path with a profile-backed per-shard reset epoch or clone-elision primitive, not another per-hit counter/layout micro-lever.
2. `bd-xmh5g.86` ffs-block per-slot fast-hit reset flags: REJECT, no code kept. Fresh RCH `ts1` current-HEAD baseline reused the profile-backed `block_cache_s3fifo_concurrent_hot_read_64threads` row `[15.647, 15.904, 16.355] ms` versus sharded comparator `[8.9055, 9.0122, 9.1441] ms`; the attempted one lever moved `s3_fast_hit_reset_pending` from one cache-wide `AtomicBool` into per-direct-slot reset flags scanned by slow paths. Proof before rebench passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench regressed to S3-FIFO `[17.266, 18.760, 20.657] ms` and sharded comparator `[14.525, 16.342, 17.791] ms`; Score `< 2.0`, source restored to HEAD. Isomorphism for rejected lever: cached bytes, resident slot mapping, access-count handles, mutation epoch gating, misses/admissions/evictions/writes, authoritative S3-FIFO ordering/tie-breaking, and global hit metrics unchanged; a pre-scan store would be consumed by that scan and a post-scan store would remain pending for the next slow path; FP N/A; RNG unchanged/N/A. Next deeper primitive: stop moving per-hit metadata and attack clone/lock read amplification with a safe-Rust read-mostly resident handle or epoch-protected immutable slot snapshot, benchmarked before edit.
3. Tracker and primitive selection: `bd-xmh5g.87` is the active claimed ffs-block bead for the next deeper primitive. Fresh RCH `ts1` current-HEAD baseline recorded in the bead: S3-FIFO hot read `[15.326, 15.730, 16.028] ms` versus sharded comparator `[8.2841, 8.4723, 8.6293] ms`. Graveyard match: §14.8 RCU/QSBR read-mostly immutable publication plus the FrankenSuite quick-fix mapping of read-heavy `Arc<Mutex<T>>` to `arc-swap`; Score gate `4.0 = Impact 4 x Confidence 3 / Effort 3`. Duplicate `bd-xmh5g.88` was closed as superseded by `bd-xmh5g.87` with no code changes.
4. `bd-xmh5g.87` ffs-block read-mostly resident handles: REJECT, no code kept. The one lever stored immutable `Arc<S3FastResident>` handles in direct resident slots so fast hits cloned a handle instead of the full resident record under the slot mutex. Proof before rebench passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench did not clear the gate: S3-FIFO `[15.335, 15.560, 15.731] ms` was only about 1.1% mean faster than the fresh baseline `[15.326, 15.730, 16.028] ms`, while the sharded comparator regressed from `[8.2841, 8.4723, 8.6293] ms` to `[9.3926, 9.7255, 10.459] ms`; Score `< 2.0`, source restored to HEAD. Isomorphism for rejected lever: cached bytes, direct-slot block mapping/collision fallback, access-count handles, mutation epoch gating, hit metrics, reset flag behavior, misses/admissions/evictions/writes, small-cache fallback, and authoritative ordering/tie-breaking unchanged; FP N/A; RNG unchanged/N/A. Next deeper primitive: replace the slot mutex itself with a safe-Rust seqlock/epoch immutable snapshot or per-thread hot-read shard that changes the synchronization model, benchmarked before edit.
5. `bd-xmh5g.89` ffs-block ArcSwap resident-slot snapshots: REJECT, no code kept. Fresh RCH `ts1` baseline on current main `774388a1` selected the same hot-read row: S3-FIFO `[15.122, 15.362, 15.756] ms` and sharded comparator `[8.7508, 9.2854, 9.6693] ms`. The one lever replaced the direct resident slot `Mutex<Option<(BlockNumber, S3FastResident)>>` table with safe-Rust `ArcSwapOption` immutable slot snapshots and CAS removal, preserving same-slot newer inserts. Proof before rebench passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench regressed to S3-FIFO `[38.810, 53.373, 81.810] ms` and sharded comparator `[31.862, 37.056, 41.675] ms`; Score `< 2.0`, source and `Cargo.lock` restored to HEAD. Isomorphism for rejected lever: direct-slot block mapping/collision fallback, access-count validity/increment semantics, mutation epoch gating, invalidation behavior, global hit metrics, reset flag behavior, misses/admissions/evictions/writes, small-cache fallback, cached bytes, and authoritative S3-FIFO ordering/tie-breaking unchanged; FP N/A; RNG unchanged/N/A. Next primitive: stop replacing the slot container and remove per-read shared publication entirely with a deterministic per-thread or per-shard hot-read slab/direct-map cache whose misses fall back to authoritative S3-FIFO state, benchmarked from a fresh RCH baseline before edit.

## Loop 5 Missions
1. Re-triage ready `[perf]` beads after `bd-xmh5g.89`, avoid CrimsonDove and BlackThrush surfaces, and claim only a fresh unowned profile-backed target.
2. If ffs-block remains the best target, establish a new RCH `ts1` baseline before editing and file the per-thread/per-shard hot-read slab bead.
3. Use one structural lever only: eliminate shared read-side resident-slot publication from the fast path, with authoritative S3-FIFO fallback and mutation-epoch invalidation.
4. Prove behavior unchanged with golden sha256, ordering/tie-breaking/FP/RNG proof, and crate-scoped RCH check/clippy.
5. Rebench on the same worker, keep only if Score `>= 2.0`, close the bead, push `main` and `main:master`, then re-profile.

## Loop 5 Completed Passes
1. `bd-xmh5g.90` ffs-block per-thread hot-read slab: REJECT, no optimization code kept. Fresh RCH `ts1` baseline on current main `d6ecc2c9` was S3-FIFO `[14.599, 14.770, 15.096] ms` versus sharded comparator `[7.9272, 8.0099, 8.1200] ms`. The one lever added a thread-local direct-map keyed by cache id, block, and mutation epoch above the authoritative S3-FIFO resident table. Candidate proof before rejection passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench regressed to S3-FIFO `[14.943, 16.222, 16.971] ms` and sharded comparator `[9.0267, 9.5444, 10.503] ms`; Score `< 2.0`, source restored to the parent of `35c48ebd` for `crates/ffs-block/src/lib.rs`. Restored-source proof passed: RCH `ts1` golden with the same sha256 values, RCH `cargo check -p ffs-block --all-targets --features s3fifo`, RCH `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`, local `cargo fmt --package ffs-block --check`, `git diff --check`, and `git diff 35c48ebd^ -- crates/ffs-block/src/lib.rs` empty. Isomorphism for the rejected lever: authoritative S3-FIFO resident table remained the policy owner; direct-slot block mapping, collision fallback, access-count validity/increment, mutation epoch gating, invalidation behavior, global hit metrics, reset flag behavior, misses/admissions/evictions/writes, small-cache fallback, cached bytes, ordering, and tie-breaking were unchanged; floating-point N/A; RNG unchanged/N/A. Next primitive: abandon read-side clone publication variants and profile a structurally different owner model such as per-shard cache ownership or benchmark-topology sharding rather than another per-hit metadata container.
2. `bd-xmh5g.91` ffs-block read-shared S3 fast resident slots: KEPT. Fresh RCH `ts1` baseline on current main `2470c811` was S3-FIFO hot-read `[16.084, 16.339, 16.879] ms` versus sharded comparator `[8.7539, 9.1385, 9.7878] ms`. One lever changed `S3FastResidentTable` slots from `Mutex<Option<(BlockNumber, S3FastResident)>>` to `RwLock<Option<(BlockNumber, S3FastResident)>>`, so read hits take shared guards while insert/remove invalidation retain exclusive guards. Proof passed: RCH `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench improved S3-FIFO hot-read to `[15.153, 15.431, 16.051] ms` and the sharded comparator to `[8.4621, 8.6721, 9.1176] ms`. Isomorphism: direct-slot mapping, block collision fallback, access-count validity/increment, mutation epoch gating, invalidation behavior, fast-hit metrics, reset flag behavior, misses/admissions/evictions/writes, small-cache fallback, cached bytes, authoritative S3-FIFO ordering, and tie-breaking are unchanged; read/write guard mode is the only synchronization change; floating-point N/A; RNG unchanged/N/A. Score `3.6 = Impact 2 x Confidence 4.5 / Effort 2.5`.
3. `bd-xmh5g.92` ffs-block fast-hit reset cursor: KEPT. Fresh RCH `ts1` baseline from the post-`bd-xmh5g.91` reprofile was S3-FIFO hot-read `[15.153, 15.431, 16.051] ms` versus sharded comparator `[8.4621, 8.6721, 9.1176] ms`. One lever removed the per-hit `s3_fast_hit_reset_pending` `AtomicBool` store and replaced it with an `ArcState::applied_s3_fast_hits` cursor over the existing `s3_fast_hits` counter, so slow paths reset the sequential-read detector when fast-hit count advanced. Proof passed: RCH `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench improved S3-FIFO hot-read to `[14.585, 14.809, 15.251] ms`; the sharded comparator stayed flat at `[8.3733, 8.6747, 8.9054] ms`. Isomorphism: fast-hit metrics are still counted by the same atomic; slow-path reset semantics remain "reset after any fast hit since the previous slow path"; direct-slot mapping, collision fallback, access-count validity/increment, mutation epoch gating, invalidation, misses/admissions/evictions/writes, small-cache fallback, cached bytes, authoritative ordering, and tie-breaking are unchanged; floating-point N/A; RNG unchanged/N/A. Score `3.2 = Impact 2 x Confidence 4 / Effort 2.5`.
4. `bd-xmh5g.93` ffs-block packed S3 fast mutation state: REJECT, no optimization code kept. Baseline RCH `ts1` from the `bd-xmh5g.92` post-keep reprofile was S3-FIFO hot-read `[14.585, 14.809, 15.251] ms` versus sharded comparator `[8.3733, 8.6747, 8.9054] ms`. The one lever packed `s3_fast_mutation_active` and `s3_fast_mutation_epoch` into one `AtomicU64` seqlock-like state so fast hits loaded one mutation word before and after the resident-table read. Proof before rebench passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench regressed to S3-FIFO `[19.728, 20.563, 21.398] ms` while the sharded comparator was `[8.0361, 8.1417, 8.4066] ms`; Score `< 2.0`, and `crates/ffs-block/src/lib.rs` was restored to `HEAD` (`git diff -- crates/ffs-block/src/lib.rs` empty). Isomorphism for the rejected lever: intended ordering/tie-breaking unchanged because mutation-active and epoch validation remained the fast-hit validity gate; FP N/A; RNG unchanged/N/A. Next primitive: avoid the active/epoch packing class and attack access-count/global-hit RMW pressure or a structural owner model only after a fresh baseline.
5. `bd-xmh5g.94` ffs-block saturated S3 access-count fast hit: CLOSED no-code duplicate/collision. It was filed after `bd-xmh5g.93` already claimed and edited the same hot-read surface. The access-count saturation idea is reserved for a future reprofile after `bd-xmh5g.93`; no `bd-xmh5g.94` source code is kept.
6. `bd-xmh5g.95` ffs-block saturated S3 access-count CAS skip: KEPT. Fresh current-HEAD RCH `ts1` baseline after the packed-state rejection was S3-FIFO hot-read `[14.872, 15.976, 17.432] ms` versus sharded comparator `[10.012, 12.932, 14.315] ms`. One lever returns immediately from `S3AccessHandle::increment_count` when the relaxed-loaded counter is already `u8::MAX`, avoiding the old no-op saturated `compare_exchange_weak` RMW while preserving the same CAS loop for every non-saturated state. Proof passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench improved S3-FIFO hot-read to `[13.992, 14.811, 15.827] ms` and sharded comparator to `[8.4455, 9.2192, 10.286] ms`. Isomorphism: ordering/tie-breaking unchanged because this only skips an already-idempotent saturated metadata RMW; final count and return value stay `u8::MAX`; authoritative S3-FIFO misses/admissions/evictions/writes unchanged; floating-point N/A; RNG unchanged/N/A. Score `6.0 = Impact 2 x Confidence 4.5 / Effort 1.5`. Next profiling direction: re-run current-head `ffs-block` hot-read and only attack a different shared read-side primitive if the residual survives.

## Loop 6 Missions
1. Re-profile current pushed `main` after `bd-xmh5g.95` and verify whether `ffs-block` remains the best unowned residual.
2. Reject any read-side clone/container micro-lever that fails Score `>= 2.0`; record exact same-worker evidence and restore source.
3. If the clone-elision class fails, switch to a structural primitive for the single-cache versus sharded hot-read gap, such as a safe-Rust internal shard/owner model or exact striped hit accounting, only after a fresh baseline.

## Loop 6 Completed Passes
1. `bd-xmh5g.96` ffs-block duplicate resident clone elision: REJECT, no optimization code kept. Fresh current-HEAD RCH `ts1` baseline after `bd-xmh5g.95` was S3-FIFO `[13.618, 13.816, 14.120] ms` versus sharded comparator `[7.4016, 7.6958, 7.9185] ms`. The attempted one lever made `S3FastResidentTable::get_valid` return the cloned `BlockBuf` and access handle directly, avoiding the extra `entry.data.clone_ref()` in `s3_fast_hit`. Proof before rebench passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench did not clear the gate: S3-FIFO `[13.634, 13.885, 14.248] ms` and sharded comparator `[7.6959, 7.8606, 8.1454] ms`; Score `< 2.0`, source restored manually (`git diff -- crates/ffs-block/src/lib.rs` empty). Isomorphism for the rejected lever: returned bytes, slot block matching, access validity checks, access-count increment, fast-hit metrics, mutation epoch gating, misses/admissions/evictions/writes, ordering, and tie-breaking unchanged; floating-point N/A; RNG unchanged/N/A. Next primitive: stop clone-elision micro-levers and profile the structural single-cache versus sharded gap.
2. `bd-xmh5g.97` ffs-block striped S3 fast-hit accounting: KEPT. Fresh current-HEAD RCH `ts1` baseline after `bd-xmh5g.96` was S3-FIFO hot-read `[13.875, 14.960, 15.852] ms` versus sharded comparator `[8.2164, 8.6099, 8.9161] ms`. One structural lever replaced the single global `s3_fast_hits` `AtomicU64` with cache-line-padded per-slot stripes keyed by the same direct resident slot mapping; fast hits increment only their stripe, while metrics and slow-path scan-reset aggregate the exact stripe sum. Proof passed: RCH `ts1` `s3fifo_arc_cache_large_hot_read_golden_report` with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench improved S3-FIFO hot-read to `[9.2097, 9.8410, 10.448] ms`; sharded comparator stayed in-band at `[8.6572, 8.9005, 9.3325] ms`. Isomorphism: ordering/tie-breaking unchanged because the authoritative S3-FIFO state machine, resident table, access-count increment, mutation epoch gate, misses/admissions/evictions/writes, returned bytes, and small-cache fallback are unchanged; metrics remain exact via the saturating stripe sum; slow-path reset semantics remain reset after any fast hit since the prior aggregate; floating-point N/A; RNG unchanged/N/A. Score `8.0 = Impact 4 x Confidence 5 / Effort 2.5`. Next primitive: re-profile current head; residual single-cache gap is now close to the sharded comparator, so target a different shared read-side or policy-owner bottleneck only with fresh evidence.
3. `bd-xmh5g.98` ffs-block scan ghost pretrim: REJECT, no optimization code kept. Profile-backed target came from post-`bd-xmh5g.97` RCH `ts1` broad profile: sequential_scan `[42.338, 43.190, 44.506] ms`, zipf `[29.332, 30.847, 31.763] ms`, mixed_seq70_hot30 `[50.927, 51.539, 52.224] ms`, compile_like `[63.590, 65.696, 67.505] ms`, database_like `[53.655, 54.383, 55.122] ms. One lever tried pre-trimming the full S3-FIFO ghost queue before inserting a scan-bypassed key. RCH `vmi1149989` proof passed six `s3fifo_arc_cache*` tests and `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 stayed `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` with payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; final exact-source RCH `ts1` rebench did not clear Score>=2.0: sequential_scan `[41.248, 41.745, 42.857] ms`, zipf `[31.523, 33.564, 36.210] ms`, mixed_seq70_hot30 `[51.186, 55.199, 58.377] ms`, compile_like `[63.474, 65.018, 67.266] ms`, database_like `[53.649, 54.572, 56.220] ms`, hot_read `[9.1895, 9.9450, 10.696] ms. Source restored manually (`git diff -- crates/ffs-block/src/lib.rs` empty). Isomorphism for rejected lever: intended final ghost queue membership/order, hits/misses/resident/capacity/ghost lengths, returned bytes, ordering, tie-breaking, FP/RNG all unchanged. Next primitive: stop ghost micro-churn and baseline a structural S3-FIFO policy-owner replacement, such as ring-buffer/indexed queues or sharded policy state to reduce `VecDeque`+`HashMap` miss/admission overhead.
4. `bd-xmh5g.103` ffs-block pre-sized S3-FIFO policy metadata: REJECTED, no optimization code kept. A stricter clean detached same-worker RCH `ts1` A/B for exact parent `04eeb8b9` versus exact after commit `03cfdc05` regressed every measured row: sequential_scan `[39.412, 40.443, 41.248] ms -> [41.541, 41.904, 42.533] ms`; zipf `[25.788, 26.212, 26.945] ms -> [28.195, 29.246, 30.001] ms`; mixed `[48.392, 48.849, 49.557] ms -> [50.577, 51.235, 52.226] ms`; compile_like `[56.384, 57.777, 58.653] ms -> [58.833, 59.886, 60.642] ms`; database_like `[52.287, 52.878, 53.483] ms -> [54.564, 55.162, 56.078] ms`; hot_read `[8.8018, 8.9823, 9.1729] ms -> [9.2163, 10.285, 12.023] ms`. Functional proof still held: RCH `cargo check -p ffs-block --all-targets --features s3fifo`, RCH `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`, and RCH focused `s3fifo_arc_cache` tests passed with `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; ordering/tie-breaking/FP/RNG unchanged. Score `< 2.0`; source rollback restores lazy `VecDeque::new`/`HashMap::new` construction. Next primitive: continue with `bd-xmh5g.104` only if its own same-worker gate passes, otherwise pivot to indexed/ring-backed policy queues.
5. `bd-xmh5g.104` ffs-block miss-path loc lookup reuse: REJECTED, no optimization code kept. The one lever reused the first `loc` lookup in `s3_on_miss_or_ghost_hit` and avoided empty opposite-ghost scans; proof passed with RCH `cargo check -p ffs-block --all-targets --features s3fifo`, RCH `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`, local `cargo fmt --package ffs-block --check`, and focused `s3fifo_arc_cache` tests with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` plus payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`. Same-worker RCH `ts1` rebench did not clear the gate: sequential_scan `[40.568, 41.977, 43.374] ms -> [40.561, 41.601, 43.589] ms`; zipf `[28.352, 28.704, 29.069] ms -> [28.010, 28.567, 29.136] ms`; mixed `[48.998, 49.996, 51.804] ms -> [49.678, 50.529, 51.578] ms`; compile_like `[57.708, 59.205, 60.509] ms -> [59.906, 65.878, 68.636] ms`; database_like `[54.631, 55.477, 56.082] ms -> [55.763, 56.439, 57.693] ms`; hot_read `[8.3277, 9.0436, 9.9291] ms -> [8.5121, 8.6290, 8.7304] ms`. Ordering/tie-breaking/FP/RNG remained unchanged, but Score `< 2.0`; after `bd-xmh5g.103` was rejected the candidate parent was invalid anyway. Next primitive: fresh current-head indexed/ring-backed S3-FIFO policy queues that remove `VecDeque::remove_from_list` scans and duplicate ghost/location work under their own profile gate.
6. `bd-xmh5g.105` ffs-block lazy-indexed S3-FIFO ghost queues: KEPT. Fresh RCH `ts1` baseline was sequential_scan `[45.801, 47.116, 48.996] ms`, zipf_distribution `[30.223, 30.795, 31.715] ms`, mixed_seq70_hot30 `[54.046, 54.476, 55.179] ms`, compile_like `[61.627, 63.548, 68.009] ms`, database_like `[55.310, 56.129, 57.297] ms`, and concurrent_hot_read_64threads `[9.4605, 9.8388, 10.611] ms`. One lever replaced physical ghost-queue membership as the S3-FIFO source of truth with generation-stamped lazy ghost entries plus exact live lengths, so ghost-hit removal is logical O(1) and front pops skip stale tombstones while preserving live FIFO order. Proof passed: RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` focused `s3fifo_arc_cache` tests 6/6 with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench improved sequential_scan to `[40.271, 40.979, 41.418] ms`, zipf_distribution to `[26.894, 27.115, 27.313] ms`, mixed_seq70_hot30 to `[48.843, 50.176, 50.912] ms`, compile_like to `[57.945, 58.772, 59.997] ms`, database_like to `[53.215, 54.387, 56.432] ms`, and concurrent_hot_read_64threads to `[9.1273, 9.4090, 9.6508] ms`. Isomorphism: live ghost FIFO order is the old queue with removed keys deleted; generation checks prevent stale physical entries from affecting later reincarnations; capacity and metrics use exact live lengths; returned bytes, hits/misses, resident/capacity behavior, authoritative ordering, and tie-breaking are unchanged; floating-point N/A; RNG unchanged/N/A. Score `6.0 = Impact 3 x Confidence 4 / Effort 2`.
7. `bd-xmh5g.107` ffs-block loc-embedded S3 ghost generation: REJECTED, no optimization code kept. Fresh current-head RCH `ts1` baseline after `bd-xmh5g.105` was sequential_scan `[37.255, 38.255, 39.033] ms`, zipf_distribution `[26.177, 26.698, 27.552] ms`, mixed_seq70_hot30 `[48.636, 49.147, 49.990] ms`, compile_like `[55.593, 57.378, 59.297] ms`, database_like `[51.842, 52.991, 54.075] ms`, and concurrent_hot_read_64threads `[8.6808, 9.0568, 9.5094] ms`. One lever folded S3 ghost generation into `loc` metadata to replace `loc + ghost_generations` validation with one lookup. Proof before rebench passed: RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; RCH `ts1` focused `s3fifo_arc_cache` tests 6/6 with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench regressed sequential_scan to `[45.292, 45.590, 46.123] ms`, zipf_distribution to `[30.539, 31.419, 33.251] ms`, mixed_seq70_hot30 to `[53.742, 57.667, 60.478] ms`, compile_like to `[61.141, 61.554, 62.065] ms`, and database_like to `[57.258, 58.264, 59.334] ms`; hot_read alone was flat/slightly better at `[8.6176, 8.9765, 9.3064] ms`. Score `< 2.0`; source restored manually (`git diff -- crates/ffs-block/src/lib.rs` empty). Isomorphism for the rejected lever: intended live ghost FIFO order, generation validation, capacity/live-length accounting, returned bytes, hits/misses, resident behavior, ordering, and tie-breaking were unchanged; floating-point N/A; RNG unchanged/N/A. Next primitive: stop loc/generation metadata micro-levers and profile a compact ring/index policy queue or sharded policy-owner model.
8. `bd-xmh5g.108` ffs-block sparse ghost-queue compaction: KEPT. Fresh current-head RCH `ts1` baseline after `bd-xmh5g.107` rejection was sequential_scan `[40.322, 41.026, 41.916] ms`, zipf_distribution `[27.293, 27.672, 28.048] ms`, mixed_seq70_hot30 `[49.636, 50.322, 51.192] ms`, compile_like `[61.622, 68.262, 76.384] ms`, database_like `[56.020, 59.223, 63.836] ms`, and concurrent_hot_read_64threads `[8.6680, 10.840, 12.918] ms`. One lever compacts B1/B2 ghost `VecDeque`s only after logical ghost removal and only when stale physical entries exceed both half of ghost capacity and 2x live length, preserving generation-stamped live FIFO order while bounding tombstone scan cost. Proof passed: RCH `ts1` `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `ts1` `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; RCH `ts1` focused `s3fifo_arc_cache` tests 6/6 with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench improved aggregate median sum `257.345 ms -> 247.683 ms`; primary ghost-heavy rows improved compile_like to `[59.915, 62.126, 64.345] ms`, database_like to `[50.333, 51.313, 52.841] ms`, and concurrent_hot_read_64threads to `[9.9096, 10.189, 10.383] ms`; sequential_scan regressed to `[41.211, 42.111, 43.937] ms`, zipf_distribution to `[28.930, 29.896, 30.488] ms`, and mixed_seq70_hot30 to `[50.567, 52.048, 53.231] ms`. Isomorphism: live ghost FIFO order is preserved by filtering the old queue in physical order through the same generation validation; generation validation, capacity/live-length metrics, returned bytes, hits/misses, resident behavior, authoritative ordering, and tie-breaking are unchanged; floating-point N/A; RNG unchanged/N/A. Score `2.4 = Impact 4 x Confidence 3 / Effort 5`. Next primitive: re-profile scan/zipf/mixed regressions and consider a true ring-backed or sharded S3-FIFO policy owner rather than more ghost metadata folding.
9. `bd-xmh5g.110` ffs-block no-history S3 scan ghost admission: REJECTED, no optimization code kept. Fresh current-main RCH `ts1` baseline after `bd-xmh5g.108` was sequential_scan `[40.609, 41.504, 42.445] ms`, zipf_distribution `[26.478, 26.988, 27.675] ms`, mixed_seq70_hot30 `[49.269, 50.586, 51.759] ms`, compile_like `[55.561, 57.173, 58.385] ms`, database_like `[52.090, 53.866, 56.675] ms`, and concurrent_hot_read_64threads `[8.7850, 8.9500, 9.0947] ms`. One lever split scan-bypass B1 ghost admission into a no-history path after `on_read_miss_or_scan_bypass` had already established `!loc.contains_key(key)`. Proof before rebench passed: RCH `cargo check -p ffs-block --all-targets --features s3fifo`; RCH `cargo clippy -p ffs-block --all-targets --features s3fifo -- -D warnings`; RCH focused `s3fifo_arc_cache` tests 6/6 with unchanged `BLOCK_CACHE_HOT_READ_GOLDEN` sha256 `63bf85d8f27125241944f9a9b1713367bc7da9992af55e950d3bb009d04e21e5` and payload sha256 `4ccf33e90a6ddc31fc96a528902195869b01fbab74e2299b9f5cd4aa6e5cf1f2`; local `cargo fmt --package ffs-block --check`; `git diff --check`. Same-worker RCH `ts1` rebench failed the gate: sequential_scan `[42.174, 43.926, 45.874] ms`, zipf_distribution `[27.199, 27.930, 29.204] ms`, mixed_seq70_hot30 `[48.887, 50.515, 51.834] ms`, compile_like `[55.518, 56.104, 56.839] ms`, database_like `[51.432, 52.924, 54.669] ms`, and concurrent_hot_read_64threads `[8.8207, 9.8668, 10.382] ms`; aggregate median sum worsened `239.067 ms -> 241.266 ms`. Source restored manually (`git diff -- crates/ffs-block/src/lib.rs` empty). Isomorphism for the rejected lever: intended live ghost FIFO order, generation validation, capacity/live-length accounting, read coalescing/read counts, returned bytes, hits/misses, resident behavior, ordering, and tie-breaking unchanged; floating-point N/A; RNG unchanged/N/A. Next primitive: attack a true sharded or ring-backed S3-FIFO policy owner that changes the miss/admission synchronization/data-structure model instead of adding another scan-bypass micro-path.
