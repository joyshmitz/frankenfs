# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankenfs performance campaign
# Total Passes: 14
# Started: 2026-06-01T19:21:38-04:00

## Status: IN PROGRESS - Pass 14 rejected/no code kept bd-xmh5g.12

Completed bead: `bd-pyxaz` (`[perf] inline little-endian parser reads in ffs-types`)

Completed bead: `bd-l4lxw` (`[perf] batch fsck/scrub 4 KiB block reads`)

Deferred bead: `bd-kq3b4` (`[perf] read-buffer movement dominates after bd-l4lxw`)

Completed bead: `bd-thkm1` (`[perf] ffs-core direct-read path remained syscall-bound after scrub batching`)

Completed bead: `bd-r8zw8` (`[perf] ffs-ondisk parser primitive density`) - rejected/no code kept after retry

Completed bead: `bd-srh8m` (`[perf] cache read-only ext4 group descriptors to cut direct-read scalar pread64`)

Completed bead: `bd-7l5f0` (`[perf] cache read-only ext4 inode table blocks to remove remaining direct-read pread64`)

Closed bead: `bd-htsob` (`[perf] asupersync Cx::checkpoint ~1.8-2.0% self on hot read loop`) - REJECTED/no code kept (ironfox, pass 8). The prior pass could not obtain a release-perf after-benchmark; this pass got the clean build lane and completed it. Lever = drop the redundant trailing `cx_checkpoint` in `FileByteDevice::read_exact_at` (keep leading cancel-before-IO). `perf stat -r5`, `direct-read` 3M iters: baseline 30.595G insns / 24.791G cycles vs candidate 31.496G insns / 25.400G cycles = +2.95% insns / +2.46% cycles — REMOVAL REGRESSES under LTO (inliner perturbation, +300 insns/iter). checksum 16877 unchanged (isomorphic). `cx_checkpoint` is near-free per call; its self% is not separately optimizable without the rank-1 per-block-read restructure (bd-kq3b4, gated). Local artifacts: `artifacts/perf/2026-06-01_ironfox_bd-htsob/`.

Rejected bead: `bd-a80jk` (`[perf] Optimize ffs-btrfs inode-ref fixed header decode`) - no code kept. Fresh same-worker rch Criterion baseline on `vmi1153651`: `btrfs_parse_inode_refs` `[46.032 ns, 47.389 ns, 48.951 ns]`. One lever attempted: direct fixed-offset little-endian loads for `index` and `name_len` after the existing 10-byte header guard. Focused remote behavior proof passed (`cargo test -p ffs-btrfs --lib inode_ref`: 9/9 on `vmi1149989`), and checked-in golden sha256 manifests passed. Same-worker after benchmark on `vmi1153651`: `[45.225 ns, 46.571 ns, 48.394 ns]`, only -1.73% mean with heavily overlapping intervals. Score: Impact 1 x Confidence 1 / Effort 1 = 1.0, below the keep threshold. Artifact: `tests/artifacts/perf/2026-06-02_plumfern_btrfs_inode_ref.md`.

Completed bead: `bd-a384r` (`[perf] coalesce contiguous per-block reads into one syscall on file-read path`) - evidence closeout after already-landed code commits `1051687b`, `48d81a6a`, and `0b24b1e7`. Fresh rch release-perf build on `vmi1149989` produced `/data/tmp/cargo-target/release-perf/ffs-cli`; rch release-perf harness build on `vmi1293453` produced `/data/tmp/cargo-target/release-perf/ffs-harness`. Baseline artifact from `tests/artifacts/perf/2026-06-01_plumfern/baseline_fsck.json`: mean 15.566 ms, median 15.284 ms, p95 17.800 ms, p99 18.877 ms. Current hyperfine: mean 14.952 ms, median 15.001 ms, p95 16.512 ms, p99 17.916 ms. The wall-clock win is small/variance-overlapping, but the rank-1 profile target is proven: strace syscall mix dropped from 2,051 `pread64` calls and 2,155 total syscalls to 32 `preadv` + 3 `pread64` and 152 total syscalls. One-iteration direct-read checksum stayed 16877; checked-in golden sha256 manifests passed. Score: Impact 4 x Confidence 4 / Effort 2 = 8.0, based on syscall collapse plus modest p95 improvement. Artifact: `tests/artifacts/perf/2026-06-02_plumfern_bd-a384r/README.md`.

Profile evidence:
- Checked-in `profiles/flamegraph_cli_inspect.svg` and `profiles/flamegraph_fuse_read.svg`
  show repeated `ffs_types::read_le_u16`, `ffs_types::read_le_u32`, and
  `ffs_types::ensure_slice` frames on parser/read-path workloads.

Baseline:
- `rch exec -- cargo bench --profile release-perf -p ffs-harness --bench metadata_parse -- metadata_parse`
- `metadata_parse`: `[140.82 ns, 150.14 ns, 158.98 ns]`

Outcome:
- No code kept. `bd-pyxaz` was closed as a rejected/no-change attempt.
- Candidate 1 (cold error helpers + fixed-byte-copy path): `metadata_parse`
  measured `[407.55 ns, 408.88 ns, 410.41 ns]`, worse than original baseline.
- Candidate 2 (cold error helpers only): `metadata_parse` measured
  `[447.13 ns, 448.98 ns, 451.16 ns]`.
- The tiny helper effect is below the noise of unpinned `rch` worker selection:
  the original baseline ran on `vmi1149989`; the candidate runs selected `ts2`.
  Treat tiny-helper optimization as inconclusive unless the campaign has a
  same-worker or same-invocation comparison protocol.

## Missions
1. Completed: perf bead and benchmark target selection. No ready `[perf]` beads
   were available initially; fallback bead `bd-pyxaz` was attempted and closed
   with no code kept. Ready cc-filed perf beads are now available.
2. Completed for pass 1: baseline/profile/opportunity matrix. Remote criterion
   baseline captured; checked-in flamegraphs provided the hotspot target.
3. Completed for pass 1: single-lever optimization attempted twice, both
   rejected by measurement.
4. Completed for pass 1: behavior proof used `ffs-types` tests plus listed
   golden sha256 manifests. `scripts/verify_golden.sh --checksums-only` has a
   pre-existing nested-fixture coverage failure, filed as `bd-3vink`.
5. Completed for pass 2: `bd-l4lxw` resumed the current in-flight optimization pass,
   replacing the previously blocked parser pass because `br ready --json`
   reports it as assigned to PlumFern and it owns the compiler blocker that
   stopped the earlier run. Focused remote `rch` check, clippy, and unit tests
   pass for the `ffs-block`/`ffs-repair` batch-read lever.
   - Baseline: p50=16.0 ms, p95=18.3 ms, p99=18.9 ms; strace=2051 pread64,
     2048 of them 4096-byte block reads.
   - After: p50=14.52 ms, p95=16.45 ms, p99=16.84 ms; strace=32 preadv plus
     3 pread64 metadata reads.
   - Score: Impact 4 x Confidence 4 / Effort 2 = 8.0, so the lever is kept.
   - Isomorphism: ordering preserved (same ascending block range), tie-breaking
     N/A, floating-point N/A, RNG N/A. Golden proof: `sha256sum -c` passed for
     `conformance/golden/checksums.sha256` and `tests/fixtures/golden/checksums.txt`;
     ext4 inspect output matched the checked-in `ext4_8mb_reference.json`
     metadata subset with `diff`.
   - Caveat: `rch exec` warns and falls through for non-compilation `hyperfine`;
     the timing was run against the release-perf binary built and retrieved by
     `RCH_FORCE_REMOTE=true rch exec -- cargo build -p ffs-cli --profile release-perf`.
6. Blocked for pass 2 before this resume: `bd-r8zw8` was claimed, baseline was established, and a
   single `ffs-ondisk` group-descriptor decode lever was attempted. The
   candidate was reverted because the required post-change `rch` benchmark
   could not run while peer-owned `bd-l4lxw` edits in `crates/ffs-block/src/lib.rs`
   fail to compile on `std::os::unix::fs::FileExt::read_vectored_at`
   (`E0658`, `unix_file_vectored_at`). `bd-r8zw8` is back to `open` with a
   blocker note.
7. Completed for pass 3: `bd-kq3b4` was re-profiled after `bd-l4lxw` and
   deferred rather than optimized because the profile did not support the bead
   hypothesis. Fresh `direct-read` samply profile after scrub batching:
   15,162 samples total, `__internal_syscall_cancel` 9,945 samples (65.59%),
   `__memset_avx2_unaligned_erms` 882 (5.82%),
   `__memcpy_avx_unaligned_erms` 747 (4.93%), `Cx::checkpoint` 291 (1.92%).
   The dominant target was still scalar direct-read syscall count, not buffer
   movement, so `bd-kq3b4` was reopened with evidence notes.
8. Completed for pass 3: `bd-thkm1` added one lever: contiguous block reads are
   exposed through the `OpenFs`/MVCC adapter path, and the harness direct-read
   profile target coalesces adjacent metadata block reads 0..2. Baseline after
   `bd-l4lxw`: 2,394,739 `direct-read` iterations in 15 s, checksum 16877;
   samply profile had `__internal_syscall_cancel` at 65.59%. After:
   2,968,867 iterations in 15 s, checksum 16877 (+23.98% throughput).
   Post-change syscall profile for a 2 s run shows the new coalesced read path
   active: 23,736 `preadv` calls and 47,483 remaining `pread64` calls. Samply
   post-profile could not start on this host (`mmap failed`); `perf_event_paranoid`
   was restored to 4 and strace supplied the post-profile syscall mix. Score:
   Impact 4 x Confidence 4 / Effort 2 = 8.0, so the lever is kept.
   Isomorphism: ordering preserved (same block order, only adjacent physical
   reads coalesced), tie-breaking N/A, floating-point N/A, RNG N/A. Golden
   proof: `sha256sum -c` passed for `conformance/golden/checksums.sha256` and
   `tests/fixtures/golden/checksums.txt`; current `ffs-harness check-fixtures`
   passed for ext4 and btrfs fixtures.
9. Completed for pass 4: `bd-r8zw8` was retried after the `ffs-block` compiler
   blocker was resolved. One lever was attempted: validate `Ext4GroupDesc` size
   once, then decode fixed offsets from in-bounds slices to reduce per-field
   parser branch density. Focused remote test proof passed for
   `cargo test -p ffs-ondisk ext4_group_desc -- --nocapture`. Baseline on `ts2`:
   `ext4_group_desc_32byte` `[11.374 ns, 11.418 ns, 11.462 ns]`,
   `ext4_group_desc_64byte` `[11.825 ns, 11.839 ns, 11.854 ns]`.
   Cross-worker apparent wins were rejected as confounded because unchanged write
   benches also halved. A temporary same-invocation control on `ts2` measured the
   candidate/current parser at `32byte` `[11.745 ns, 11.847 ns, 11.948 ns]` and
   `64byte` `[11.979 ns, 12.025 ns, 12.076 ns]`, which does not beat the
   same-worker baseline. Score: Impact 0 x Confidence 4 / Effort 2 = 0, so no
   code was kept. Isomorphism: final diff has no parser code changes; ordering,
   tie-breaking, floating-point, and RNG are unchanged/N/A.
10. Completed for pass 5: `bd-srh8m` added one lever: a read-only
    `OpenFs` ext4 group descriptor cache, bypassed for writable opens,
    staged writes, and visible MVCC versions. The profile-backed target was the
    post-`bd-thkm1` direct-read syscall profile: 23,736 iterations in 2 s with
    23,736 `preadv` calls and 47,483 remaining `pread64` calls, showing one
    coalesced metadata read plus two scalar metadata reads per iteration.
    Baseline after `bd-thkm1`: 2,968,867 `direct-read` iterations in 15 s,
    checksum 16877. After: 4,302,375 iterations in 15 s, checksum 16877
    (+44.92% throughput). Post-change syscall profile for a 2 s run:
    37,953 iterations, 37,953 `preadv` calls, and 37,963 `pread64` calls, so
    the remaining scalar descriptor read was removed from the hot loop. Focused
    test proof passed:
    `RCH_FORCE_REMOTE=true rch exec -- cargo test -p ffs-core read_group_desc_cache_hits_in_read_only_mode -- --nocapture`.
    Score: Impact 5 x Confidence 4 / Effort 2 = 10.0, so the lever is kept.
    Isomorphism: ordering preserved (same descriptor parse and inode-table
    lookup sequence; only repeated immutable descriptor block reads are served
    from cache), tie-breaking N/A, floating-point N/A, RNG N/A. Golden proof:
    `sha256sum -c` passed for `conformance/golden/checksums.sha256` and
    `tests/fixtures/golden/checksums.txt`; current `ffs-harness check-fixtures`
    passed for ext4 and btrfs fixtures.
11. Completed for pass 6: `bd-7l5f0` added one lever: a read-only
    `OpenFs` ext4 inode-table block cache, keyed by `BlockNumber`, storing
    cached blocks as `Arc<[u8]>` and bypassed for writable opens, staged writes,
    and visible MVCC versions. The profile-backed target was the post-`bd-srh8m`
    syscall profile: 37,953 iterations in 2 s with 37,953 `preadv` calls and
    37,963 remaining `pread64` calls, showing one scalar inode-table read per
    iteration after the descriptor cache removed the other scalar read. Baseline
    after `bd-srh8m`: 4,302,375 `direct-read` iterations in 15 s. After:
    7,301,188 iterations in 15 s (+69.70%). The 15 s XOR checksum was `0`
    because the faster run completed an even number of identical iterations;
    a fixed one-iteration proof returned checksum 16877, matching the prior
    per-iteration checksum. Post-change syscall profile for a 2 s run:
    75,636 iterations, 75,636 `preadv` calls, and 9 `pread64` calls, reducing
    scalar `pread64` to startup/open noise. Focused test proof passed:
    `RCH_FORCE_REMOTE=true rch exec -- cargo test -p ffs-core read_inode_table_block_cache_hits_in_read_only_mode -- --nocapture`.
    Score: Impact 5 x Confidence 4 / Effort 2 = 10.0, so the lever is kept.
    Isomorphism: ordering preserved (same inode lookup and parse; repeated
    immutable inode-table block reads are served from cache), tie-breaking N/A,
    floating-point N/A, RNG N/A. Golden proof: `sha256sum -c` passed for
    `conformance/golden/checksums.sha256` and
    `tests/fixtures/golden/checksums.txt`; current `ffs-harness check-fixtures`
    passed for ext4 and btrfs fixtures.
12. Blocked/no code kept for pass 7: `bd-htsob` was re-profiled after
    `bd-7l5f0` with remote `samply` on `thinkstation1`. Fresh direct-read
    profile evidence: 4,800,234 iterations in 10 s, checksum 0; syscall still
    dominated (`__internal_syscall_cancel` 61.15% self, `preadv` 61.85%
    cumulative), but checkpoint-related frames were current and measurable:
    312/10,129 samples (3.08%) with `cx_checkpoint` cumulative 2.22%, mostly
    under `FileByteDevice` vectored reads reached through
    `ByteDeviceBlockAdapter::read_contiguous_blocks`. Baseline hyperfine via
    `rch` on `thinkstation1`: 5 s measured runs at 2,497,311 / 2,483,578 /
    2,416,785 direct-read iterations. One candidate was attempted: move the
    file-backed vectored-read checkpoint into the pre-`preadv` loop and remove
    the redundant post-read checkpoint. Focused remote test
    `cargo test -p ffs-block file_byte_device_vectored_read_cancels_before_io -- --nocapture`
    passed on `vmi1153651`, but the required
    `cargo build --profile release-perf -p ffs-harness --bin ffs-harness`
    after-build stalled silently at the release link/build stage on
    `vmi1153651`; the local `rch` wrapper was terminated after the timeout
    window. Because no release-perf after benchmark existed, the candidate did
    not prove score >=2.0 and no code was kept. `bd-htsob` remains open with
    notes for a later retry after the rch build lane is clean.
13. Blocked for current in-flight closeout: `bd-a384r` still needs wall-clock
    release-perf quantification, but the scoped `ffs-cli` release-perf rch
    closeout attempt on `vmi1153651` entered a long silent link/build phase and
    was terminated as a repeat of the known cold rebuild storm. No read-path
    code was touched for `bd-a384r` in this pass.
14. Completed/no code kept for pass 9: `bd-a80jk` used the btrfs item-parser
    profile artifact to target `parse_inode_refs` after the xattr parser bead
    was already closed. Fresh before/after rch Criterion ran on the same worker
    (`vmi1153651`). The direct fixed-header decode candidate improved the mean
    from 47.389 ns to 46.571 ns (-1.73%), but the confidence intervals
    overlapped (`[46.032, 48.951]` vs `[45.225, 48.394]`), so the score was
    below threshold and the code was reverted manually. The evidence artifact
    is `tests/artifacts/perf/2026-06-02_plumfern_btrfs_inode_ref.md`.
15. Completed for pass 11: `bd-6m4bt` kept one safe-Rust scan lever. The
    profile-backed target was the scalar 4 KiB all-zero scan pattern used by
    ext4 directory tail-padding checks. Same-binary Criterion on rch worker
    `vmi1227854` measured scalar all-zero at 2.0966 us mean and production
    `ffs_types::all_zero_bytes` at 145.80 ns mean (14.38x faster). The
    late-nonzero case measured scalar at 2.1221 us and the production helper at
    154.05 ns (13.78x faster). Score: Impact 4 x Confidence 5 / Effort 2 =
    10.0, so the lever is kept. Isomorphism: ordering preserved because the
    parser walks the same offsets and only replaces the boolean zero-suffix
    predicate; tie-breaking/FP/RNG are N/A; error classes are unchanged.
    Golden sha256 manifests passed. Artifact:
    `tests/artifacts/perf/2026-06-02_plumfern_ffs-safe-simd-scans/README.md`.
16. Completed for pass 12: `bd-xmh5g.1` kept one sharded-cache routing lever.
    The production change was already in `cc2fb42f`: `ShardedArcCache` caches
    a mask for power-of-two shard counts and uses `block & mask`, while
    non-power-of-two shard counts still use the original modulo path. The
    accepted direct same-binary rch Criterion A/B on `vmi1149989` measured old
    runtime modulo routing at 2.3974 ns mean and production mask routing at
    530.46 ps mean (4.52x faster). Earlier 64-thread after evidence also
    improved the sharded hot-read row from 51.459 ms to 15.545 ms, with
    control-normalized ratio improving from 0.515 to 0.209. Score: Impact 5 x
    Confidence 3 / Effort 1 = 15.0, so the lever is kept. Isomorphism:
    ordering preserved because `x & (n - 1) == x % n` for power-of-two `n`;
    non-power-of-two routing falls back to modulo; tie-breaking/FP/RNG are
    unchanged or N/A. Golden workload-report sha256 passed:
    `3f8b444b755bbaf74b7287be6a70840d68bcb974b7483a1e948faf3f8294ac74`.
    Artifact: `tests/artifacts/perf/2026-06-02_plumfern_sharded_cache_routing.md`.
17. Completed for pass 13: `bd-xmh5g.11` kept one allocator bitmap scan lever.
    `bitmap_find_contiguous_linear` now skips full 64-bit words and extends
    all-free 64-bit runs before falling back to the existing byte/bit path for
    mixed and tail regions. Baseline rch Criterion on `vmi1293453`:
    `find_contiguous/plain_32_O(n)` mean 51.065 ns [49.242, 53.505]. Same-worker
    after before the clippy extraction: 36.600 ns [35.404, 37.937]. Final
    same-binary A/B on `vmi1149989`: old byte scan 55.729 ns [51.551, 59.902]
    vs word-at-a-time 34.666 ns [33.170, 36.217]. Score: Impact 4 x Confidence
    5 / Effort 2 = 10.0, so the lever is kept. Isomorphism: ordering and
    earliest-run tie-break are preserved by the new property test matching the
    old wraparound search; floating-point and RNG are N/A. Remote proof passed:
    `cargo check -p ffs-alloc --all-targets`,
    `cargo test -p ffs-alloc bitmap_find_contiguous -- --nocapture`, and
    `cargo clippy -p ffs-alloc --all-targets -- -D warnings`. Local fmt,
    `git diff --check`, conformance golden sha256, fixture golden sha256, and
    the existing find-contiguous golden trace sha256 all passed.
18. Rejected/no code kept for pass 14: `bd-xmh5g.12` tested a safe-Rust u128
    widening lever for `bitmap_largest_free_run` after fresh rch Criterion on
    `vmi1153651` ranked `largest_free_run/fragmented_mixed_bytes` as the slowest
    remaining ffs-alloc bitmap row (`[2.2668 us, 2.3374 us, 2.4563 us]`). The
    candidate was isomorphic under largest-run tests, naive proptests, and a
    same-binary agreement assert, but same-binary A/B recorded in `5ae34b24`
    measured old u64 scan 1.7365 us vs wide u128 scan 1.9894 us on the
    fragmented workload. This resume reproduced the rejection signal with rch
    Criterion on `vmi1156319`: production row `3.0472 us`, old u64 scan
    `2.3504 us`, wide u128 scan `3.1542 us`. The performance lever was rejected
    and no code changes remain in this worktree.

## Completed Passes
1. `bd-pyxaz`: rejected/no-change helper-level parser read attempt.
2. `bd-l4lxw`: kept `ffs-block` contiguous vectored reads plus `ffs-repair`
   scrub batching. Result: pread-family hot calls collapsed from 2051 to 35,
   p95 improved from 18.3 ms to 16.45 ms on `fsck -f --json`.
3. `bd-thkm1`: kept `OpenFs`/MVCC contiguous-read propagation plus harness
   direct-read metadata coalescing. Result: 15 s direct-read throughput improved
   from 2,394,739 to 2,968,867 iterations (+23.98%) with checksum 16877
   preserved.
4. `bd-r8zw8`: rejected/no-change retry after the `bd-l4lxw` compile blocker was
   resolved. The same-worker Criterion control showed the candidate did not
   improve the `ext4_group_desc` parser target, so the final diff kept no parser
   changes.
5. `bd-srh8m`: kept a read-only `OpenFs` ext4 group descriptor cache with MVCC
   and writable-path bypasses. Result: 15 s direct-read throughput improved from
   2,968,867 to 4,302,375 iterations (+44.92%) with checksum 16877 preserved;
   2 s syscall profile improved from 47,483 to 37,963 `pread64` calls while
   `preadv` stayed one per iteration.
6. `bd-7l5f0`: kept a read-only `OpenFs` ext4 inode-table block cache with MVCC
   and writable-path bypasses. Result: 15 s direct-read throughput improved from
   4,302,375 to 7,301,188 iterations (+69.70%); one-iteration checksum remained
   16877, and 2 s syscall profile improved from 37,963 to 9 `pread64` calls
   while `preadv` stayed one per iteration.
7. `bd-m7adx`: generated fresh `ffs-ondisk` casefold work after the ready perf
   queue drained. Baseline on `vmi1293453` ranked
   `ext4_casefold_key_long_utf8` as the hot target at 6.0021 us mean
   [5.8735, 6.1375]. One lever was attempted: route valid UTF-8 names without
   special sharp-s expansion through `str::to_lowercase`. The after run on
   `vmi1153651` worsened the target to 10.825 us [10.627, 11.059] and worsened
   every other row, so the optimization was rejected and no runtime fast path
   was kept. The attempt exposed a behavior trap in Rust final-sigma
   lowercasing, so the only source change kept is
   `ext4_casefold_key_capital_sigma_folds_like_lowercase_sigma`.
8. `bd-htsob`: blocked/no code kept. Fresh profile confirmed a small checkpoint
   hotspot, but the only candidate could not be release-perf benchmarked because
   the rch after-build stalled; the candidate was reverted.
9. `bd-htsob`: rejected/no code kept after a later clean bench lane proved the
   candidate regressed instructions/cycles.
10. `bd-a80jk`: rejected/no code kept. Same-worker rch Criterion showed only a
   -1.73% mean shift for direct fixed-header `parse_inode_refs` loads, with
   overlapping intervals, so the lever did not meet the score threshold.
11. `bd-6m4bt`: kept a safe broadword/SWAR all-zero scan helper and routed ext4
   directory tail-padding scans through it. Result: same-binary rch Criterion
   improved 4 KiB all-zero scans from 2.0966 us to 145.80 ns and late-nonzero
   scans from 2.1221 us to 154.05 ns, with parser tests and golden SHA proof
   preserved.
12. `bd-xmh5g.1`: kept power-of-two shard-mask routing for `ShardedArcCache`.
   Result: direct runtime modulo route 2.3974 ns -> production mask route
   530.46 ps (4.52x), with modulo fallback proof, focused routing test,
   `ffs-block` check/clippy/fmt, and workload-report SHA preserved.
13. `bd-xmh5g.11`: kept a safe 64-bit word path for contiguous bitmap allocation
   scans. Result: same-binary old byte scan 55.729 ns -> final word-at-a-time
   34.666 ns, with focused wraparound/tie-break property coverage, remote
   `ffs-alloc` check/test/clippy, local fmt, and golden SHA proof preserved.
14. `bd-xmh5g.12`: rejected/no code kept for the u128 largest-free-run widening
    lever. Same-binary A/B showed old u64 scan 1.7365 us vs wide u128 scan
    1.9894 us in the recorded tracker closeout, and this resume reproduced the
    regression on `vmi1156319` with old u64 scan 2.3504 us vs wide u128 scan
    3.1542 us.

## Earlier Blockers
1. `bd-r8zw8`: no code kept. Baseline before the first candidate:
   `ext4_inode_parse` `[27.642 ns, 28.375 ns, 29.305 ns]`,
   `ext4_group_desc_32byte` `[7.1102 ns, 7.2404 ns, 7.3826 ns]`,
   `ext4_group_desc_64byte` `[7.6581 ns, 7.7427 ns, 7.8383 ns]`,
   `btrfs_superblock_parse_region` `[44.129 ns, 45.489 ns, 46.913 ns]`
   on `vmi1152480`. Focused tests/check/clippy passed for the candidate, but
   post-change benchmark failed before measurement due the unrelated
   `ffs-block` compile error above.
