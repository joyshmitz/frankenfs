# V1.2 Test Waivers

This document is consumed by `scripts/e2e/ffs_v12_program_gate_e2e.sh`. If all scenarios pass while this file is non-empty, the manifest reports `PASS-WITH-WAIVERS` instead of silently reporting a clean `PROCEED`.

| Failure | Scope | Sign-off | Tracking bead | Program-gate effect |
|---|---|---|---|---|
| `proptest_empty_bitmap` | Historical workspace-test failure admitted by `bd-m5wf.10` closeout; must be re-run under the V1.2 program gate before release. | `bd-ckivp` gate owner | `bd-ckivp` until split into a narrower bug bead | Blocks clean `PROCEED`; allowed only as `PASS-WITH-WAIVERS`. |
| `btrfs_write_fallocate` | Historical workspace-test failure admitted by `bd-m5wf.10` closeout; must be fixed or split into a narrower bug bead before release. | `bd-ckivp` gate owner | `bd-ckivp` until split into a narrower bug bead | Blocks clean `PROCEED`; allowed only as `PASS-WITH-WAIVERS`. |

No waiver may hide a failing scenario in `program_gate_manifest.json`. A failed or timed-out scenario still forces `NO-PROCEED`.
