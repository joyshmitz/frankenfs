# V1.2 Test Waivers

This document is consumed by `scripts/e2e/ffs_v12_program_gate_e2e.sh`. If all scenarios pass while the `## Active Waivers` table has one or more data rows, the manifest reports `PASS-WITH-WAIVERS` instead of silently reporting a clean `PROCEED`.

Historical rows must stay outside `## Active Waivers`. They are audit context only and do not affect the release recommendation.

## Active Waivers

| Failure | Scope | Sign-off | Tracking bead | Program-gate effect |
|---|---|---|---|---|

No active V1.2 waivers remain as of 2026-05-10.

## Resolved Waivers (Historical)

| Failure | Scope | Sign-off | Tracking bead | Program-gate effect |
|---|---|---|---|---|
| `proptest_empty_bitmap` | Historical workspace-test failure admitted by `bd-m5wf.10` closeout; re-run by audit pass `2026-05-10T03-20-31Z`. | `bd-ckivp` gate owner | `bd-xm4i6` verification | Resolved; does not affect the program-gate recommendation. |
| `btrfs_write_fallocate` | Historical workspace-test failure admitted by `bd-m5wf.10` closeout; re-run by audit pass `2026-05-10T03-20-31Z`. | `bd-ckivp` gate owner | `bd-xm4i6` verification | Resolved; does not affect the program-gate recommendation. |

No waiver may hide a failing scenario in `program_gate_manifest.json`. A failed or timed-out scenario still forces `NO-PROCEED`.
