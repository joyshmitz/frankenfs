# Negative Evidence Ledger

This is the canonical no-gaps ledger entry point requested by the bold-verify
campaign. Historical rows live in `docs/progress/perf-negative-results.md`; new
campaign closeouts should either update that file directly or add a summary row
here that points to the detailed progress ledger.

## 2026-06-20 cod-a Verification

| Date | Bead | Surface | Verdict | Ratio vs ext4/btrfs-kernel | Internal win/loss/neutral | Direct kernel win/loss/neutral | Gates |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-20 | `bd-xmh5g` | `ffs-core` btrfs regular compressed-extent fused copy/drop in `btrfs_read_file_into` | KEEP / production retained | Direct kernel loss remains but narrowed for the single-file row. Final-source single-file `/compressible.bin`: FrankenFS baseline `53.2 ms`, fused-copy candidate `35.9 ms`, kernel btrfs `cat` `6.7 ms`; candidate is still `5.38x` slower than kernel (`0.186x` kernel speed) but improves the FrankenFS side by `1.48x`. Final-source walk: baseline `32.4 ms`, candidate `31.9 ms`, kernel `11.2 ms`; candidate is `2.85x` slower than kernel and the FrankenFS-side movement is neutral (`1.015x`). | `1/0/1`: single-file read win (`1.48x` final confirmation; primary 15-run A/B `56.1 ms -> 36.8 ms`, `1.52x`), walk neutral-positive below keep threshold (`1.015x` final, `1.08x` primary). RSS smoke improved `83,620 KiB -> 50,868 KiB`; minor faults `22,932 -> 14,478`. | `0/2/0`: both direct mounted-kernel rows still lose to btrfs kernel. | RCH candidate `cargo build --profile release-perf -p ffs-cli` passed on `vmi1149989` but artifact retrieval left the target-dir binary stale, so accepted timings use a local release-perf final-source build from the clean worktree. Local `cargo fmt -p ffs-core --check`, `cargo check -p ffs-core --all-targets`, focused `cargo test -p ffs-core btrfs_decompress -- --nocapture` (`10 passed`), and `cargo test -p ffs-harness --test conformance -- --nocapture` (`100 passed / 0 failed / 2 ignored`) passed. Byte identity vs mounted kernel file: `2e379e112375338695dbd226f27bf096db571a99e5f64b975b0bb2e43b6f86b9`. Scoped clippy remains blocked by pre-existing `ffs-repair` and `ffs-core` pedantic debt outside this lever; the candidate-caused clippy lint was fixed by moving helper enums to file scope. Hyperfine JSON under `/data/projects/.scratch/batch-fused-*-20260620.json`. |
| 2026-06-20 | `bd-xmh5g` | `ffs-core` btrfs streamed-read dir/symlink guard fold in `btrfs_read_file_into_impl` | REJECT / production reverted in `37b7e8b` after clean isolation | Clean direct comparator remains a loss and did not improve: single-file `/compressible.bin` clean current `56.5 ms` vs kernel btrfs `cat` `7.1 ms`, `8.01x` slower (`0.125x` kernel speed); parent was `57.1 ms`, `8.09x` slower. Whole-tree `walk --read-data --no-stat` clean current `34.9 ms` vs kernel `cat *` `12.4 ms`, `2.82x` slower (`0.355x` kernel speed); parent was `34.4 ms`, `2.78x` slower. | Clean isolated A/B `0/1/1`: single-file was neutral (`57.1 ms` -> `56.5 ms`, `1.011x` old/new, below keep threshold); walk was slightly negative/noise (`34.4 ms` -> `34.9 ms`, `0.986x`). An earlier contaminated run that also included a peer 1 MiB CLI read-tile edit showed a false single-file win (`58.4-59.5 ms` -> `22.7-23.3 ms`), so it is invalid for this lever. | `0/2/0`: both clean direct comparisons still lose to mounted kernel btrfs. | RCH clean current `cargo build --profile release-perf -p ffs-cli` passed on `vmi1149989`; RCH parent build passed on `vmi1153651`; local clean parent/current release-perf builds passed for runnable binaries; local `cargo fmt -p ffs-core --check` passed; local conformance fallback `cargo test -p ffs-harness --test conformance -- --nocapture` passed `100 / 0 / 2 ignored`; RCH `cargo check -p ffs-core --all-targets` result is recorded in `docs/progress/perf-gauntlet-scorecard.md`. Hyperfine JSON under `/data/projects/.scratch/guardfold-clean-btrfs-zstd-*.json`; contaminated JSON retained under `/data/projects/.scratch/guardfold-btrfs-zstd-*.json` as invalid evidence. |
| 2026-06-20 | `bd-giyxr` | `ffs-core` e2compr compressed-cluster read fan-out, serial pointer PLAN plus parallel present-block READ plus ordered ASSEMBLE | KEEP / production retained from `e6259d5d`; this closeout is measured verification only | N/A for direct ext4/btrfs-kernel ratio: e2compr is an internal legacy ext2/ext4 compression path and the bench isolates block-read overlap with a latency-injected `BlockDevice`; no btrfs analogue or mounted-kernel comparator exists in the repo. | `3/0/0`: fresh cod-a RCH Criterion on `vmi1152480`, `e2compr_cluster_read_overlap`, mean serial vs parallel: 4 blocks `1.6666 ms` vs `915.24 us` (`1.82x` old/new), 16 blocks `5.9532 ms` vs `2.1675 ms` (`2.75x`), 32 blocks `12.303 ms` vs `2.3427 ms` (`5.25x`). | `0/0/1` | RCH bench passed on `vmi1152480` with the bench's serial/parallel byte-equality assertion; focused RCH `cargo test -p ffs-core e2compr -- --nocapture` passed on `hz2` (25 passed / 0 failed); RCH `cargo build --release -p ffs-core` passed on `vmi1227854` (clean rerun after an earlier `vmi1264463` remote compile succeeded but target artifact retrieval returned `RCH-E309`/exit 102); RCH harness conformance passed on `vmi1152480` (100 passed / 0 failed / 2 ignored). |
| 2026-06-20 | `bd-defgb` | btrfs zstd compressed read over a mounted 90.2 MiB kernel image plus `btrfs_decompress_extents` dedicated-pool synthetic gate | REJECT / no production source kept | Loss: current FrankenFS `walk --read-data --no-stat` mean `40.282 ms` vs kernel btrfs `cat *` mean `8.006 ms`, so FrankenFS is `5.03x` slower (`0.199x` kernel speed). Single-file `read --discard /compressible.bin` mean `87.616 ms` vs kernel `cat compressible.bin` `5.198 ms`, `16.86x` slower. | `0/6/1`: dedicated pool lost on RCH `vmi1293453` (`9.0318 ms` global vs `15.322 ms` dedicated, dedicated `1.70x` slower; multi-file `52.085 ms` always-install vs `59.538 ms` gated, gated `1.14x` slower). Larger walk windows all lost locally: 4 MiB `48.754 ms` (`0.87x` old/new), 8 MiB `66.773 ms` (`0.63x`), 16 MiB `93.516 ms` (`0.45x`), 64 MiB `146.659 ms` (`0.29x`). Diagnostic-only global pool cap helped (`RAYON_NUM_THREADS=8` walk `28.669 ms`, `1.41x` faster than default), but is not a production-local lever. | `0/2/0` | RCH `cargo bench --profile release-perf -p ffs-core --bench btrfs_decompress_extents -- --warm-up-time 1 --measurement-time 3` passed on `vmi1293453`; local release-perf `ffs-cli` build passed for mounted-image probes; `perf stat` was blocked by `perf_event_paranoid=4`; no source code retained after reverting the walk-window experiment. Detailed scorecard: `docs/progress/perf-bd-defgb-cod-a.md`. |
| 2026-06-20 | `bd-xmh5g` | btrfs zstd compressed read over the same mounted kernel image, reusing one `zstd::bulk::Decompressor` per worker thread and adding a targeted bench-filter guard | KEEP / production retained | Direct kernel loss remains but the FrankenFS side improved. Single-file `/compressible.bin`: baseline FrankenFS `76.1 ms` vs candidate confirmation `54.9 ms` (`1.39x` faster); current kernel `cat` `6.5 ms`, so FrankenFS is still `8.51x` slower (`0.117x` kernel speed). Whole-tree `walk --read-data --no-stat`: baseline FrankenFS `53.2 ms` vs candidate confirmation `32.8 ms` (`1.62x` faster); current kernel `cat *` `11.0 ms`, so FrankenFS is still `2.99x` slower (`0.334x` kernel speed). | `0/1/0`: the targeted RCH Criterion synthetic on `vmi1167313` contradicted the direct result, with fresh decompressor median `5.9330 ms` vs thread-reused median `7.2849 ms` (`0.814x` old/new; reused is slower). Earlier unbounded filtered runs were cancelled because the bench file eagerly built unrelated large datasets; the retained bench-filter guard makes targeted per-crate runs operational. | `0/2/0`: both direct comparisons still lose to kernel btrfs, despite `1.39x` and `1.62x` FrankenFS-side gains. | Local `cargo fmt -p ffs-core --check` passed; RCH `cargo bench --profile release-perf -p ffs-core --bench btrfs_decompress_extents -- btrfs_decompress_tiny_zstd_321x4k_to_128k --warm-up-time 1 --measurement-time 1 --sample-size 10 --noplot` passed on `vmi1167313`; RCH `cargo check -p ffs-core --all-targets` passed on `vmi1167313`; RCH `cargo test -p ffs-core btrfs_decompress -- --nocapture` passed on `vmi1167313` (10 passed / 0 failed); RCH conformance passed on `ovh-a` (100 passed / 0 failed / 2 ignored); RCH `cargo build --release -p ffs-cli` passed on `vmi1227854`. Scoped clippy is blocked by pre-existing `ffs-core` pedantic debt in `vfs.rs`, old indirect-pointer casts, xattr const placement, and redundant-closure rows outside this lever. |
| 2026-06-20 | `bd-xmh5g` | btrfs zstd compressed read direct-to-final-output attempt for full-overlap regular compressed extents | REJECT / production reverted | Direct kernel loss remains. Candidate single-file `read --discard /compressible.bin` mean `57.961 ms` vs kernel btrfs `cat` mean `7.011 ms`, so FrankenFS is `8.27x` slower (`0.121x` kernel speed). Candidate whole-tree `walk --read-data --no-stat` mean `34.883 ms` vs kernel `cat *` mean `11.537 ms`, so FrankenFS is `3.02x` slower (`0.331x` kernel speed). | `0/1/1`: single-file regressed the current FrankenFS baseline from `55.931 ms` to `57.961 ms` (`0.965x` old/new); whole-tree walk was neutral, `34.8826 ms` to `34.8828 ms` (`1.000x`). | `0/2/0` | RCH candidate `cargo check -p ffs-core` and `cargo build --profile release-perf -p ffs-cli` passed on `vmi1152480`; production source was then reverted. Clean-source RCH `cargo check -p ffs-core` passed on `vmi1153651`; clean-source RCH `cargo test -p ffs-harness --test conformance -- --nocapture` passed on `vmi1227854` (100 passed / 0 failed / 2 ignored); clean-source RCH `cargo build --profile release-perf -p ffs-cli` passed on `vmi1149989`. Local hyperfine before/after JSON: `/tmp/frankenfs_cod_a_btrfs_direct_output_baseline_single.json`, `/tmp/frankenfs_cod_a_btrfs_direct_output_after_single.json`, `/tmp/frankenfs_cod_a_btrfs_direct_output_baseline_walk.json`, `/tmp/frankenfs_cod_a_btrfs_direct_output_after_walk.json`. |
| 2026-06-20 | `bd-jgbam` | mmap-backed `ByteDevice` proposal for warm sequential ext4/btrfs reads, after the safe large-read direct path already shipped | REJECT / no production source kept | Fresh local warm comparators still lose to the kernel streaming path: ext4 `/data/tmp/extdiff_1497854.img:/large.bin` FrankenFS `15.0 ms` vs kernel ext4 `cat` `4.4 ms`, `3.36x` slower; btrfs `/data/tmp/btrperf_1231197.img:/m.bin` FrankenFS `76.5 ms` vs kernel btrfs `cat` `11.6 ms`, `6.58x` slower. | `1/0/0` for the already-shipped safe primitive: RCH `vmi1152480` same-binary A/B measured staged scratch median `506.33 us` vs direct `32.957 us`, old/new `15.36x`. The proposed mmap follow-up was not implemented because current `memmap2` file-backed constructors are `unsafe`, while the workspace and `ffs-block` forbid unsafe code. | `0/2/0` | RCH `cargo bench --profile release-perf -p ffs-block --bench file_device_read -- file_device_read_1mib --warm-up-time 1 --measurement-time 3` passed on `vmi1152480`; local hyperfine ext4/btrfs kernel comparators passed; temporary read-only btrfs loop mount was unmounted; no source code retained. |
| 2026-06-20 | `bd-w3hol` | `ffs-fuse` writeback-cache path, 32 x 32 KiB writes to one file handle followed by flush | KEEP / production retained | Neutral/unavailable: Linux ext4/btrfs do not expose a timed comparator for FrankenFS's in-process per-`(ino, fh)` deferred `RequestScope` table. A mounted write+fsync comparator is still required for whole-filesystem domination claims. | `1/0/0`: fresh cod-a RCH Criterion on `hz1` measured old per-write commit median `75.412 us` vs deferred flush median `64.716 us`, old/new `1.165x`, production latency `0.858x` (`14.2%` lower). Fresh core primitive rerun on `hz1` measured per-write `8.7549 ms` vs request-scope batched `6.7427 ms`, old/new `1.299x`; request-scope remained `1.7%` slower than raw batched commit (`6.6308 ms`). | `0/0/1` | RCH `cargo bench --profile release-perf -p ffs-fuse --bench mount_runtime -- mount_runtime_writeback` passed on `hz1`; RCH `cargo bench --profile release-perf -p ffs-core --bench mvcc_commit_batching -- mvcc_commit_batching_2000` passed on `hz1`; RCH `cargo build --release -p ffs-fuse` passed on `hz1`; RCH `cargo test -p ffs-fuse writeback_cache -- --nocapture` passed on `vmi1152480` (12/12); RCH `cargo test -p ffs-harness --test conformance -- --nocapture` passed on `vmi1153651` (100 passed / 0 failed / 2 ignored). |

## 2026-06-20 cod-b Verification

| Date | Bead | Surface | Verdict | Ratio vs ext4/btrfs-kernel | Internal win/loss/neutral | Direct kernel win/loss/neutral | Gates |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-20 | `bd-xmh5g.407` | `ffs-cli read` btrfs compressed single-file stream tile, 64 MiB -> 1 MiB, intended to cap decompression working set | REJECT / production source reverted; evidence-only closeout | Candidate still lost: single-file `/compressible.bin` candidate mean `36.367 ms` vs kernel `cat` `6.268 ms`, so FrankenFS is `5.80x` slower (`0.172x` kernel speed). Whole-tree `walk --read-data --no-stat` candidate mean `31.486 ms` vs kernel `cat *` `11.888 ms`, `2.65x` slower (`0.378x` kernel speed). | `0/1/1`: target single-file read regressed from baseline `35.266 ms` to `36.367 ms` (`0.970x` old/new, loss). The untouched walk path measured `29.108 ms` baseline vs `31.486 ms` candidate (`0.925x` old/new) and is treated as collateral/no-ship evidence, not a keep signal. One-shot `/usr/bin/time -v` RSS did not materially move (`47,844 KiB` baseline vs `47,812 KiB` candidate; minor faults `11,577` vs `11,561`). | `0/2/0`: both final direct comparisons remain slower than mounted kernel btrfs. | RCH clean-source `cargo build --profile release-perf -p ffs-cli` passed on `vmi1227854`; candidate build passed on `vmi1149989`; production source was manually reverted and `git diff --exit-code -- crates/ffs-cli/src/main.rs` passed; RCH conformance `cargo test -p ffs-harness --test conformance -- --nocapture` passed on `hz2` (100 passed / 0 failed / 2 ignored). `cargo fmt -p ffs-cli --check` is blocked by pre-existing formatting drift in `crates/ffs-cli/src/cmd_repair.rs`, not by this reverted candidate. Hyperfine JSON: `/tmp/frankenfs_cod_b_current_btrdiff2_read_walk.json` and `/tmp/frankenfs_cod_b_candidate_btrdiff2_read_walk.json`. |
| 2026-06-20 | `bd-xmh5g` | btrfs zstd compressed-read input-buffer scratch reuse, retaining one compressed input `Vec` per Rayon worker for sub-1 MiB frames | REJECT / production source reverted; evidence-only closeout | Tighter 25-run direct kernel comparators still lose badly: single-file `/compressible.bin` scratch candidate `58.6 ms` vs kernel btrfs `cat` `6.9 ms`, `8.53x` slower (`0.117x` kernel speed); whole-tree `walk --read-data --no-stat` scratch candidate `35.0 ms` vs kernel `cat *` `12.6 ms`, `2.77x` slower (`0.361x` kernel speed). | `0/1/1` on the acceptance run: single-file read regressed baseline `56.7 ms` -> scratch `58.6 ms` (`0.968x` old/new, loss); whole-tree walk was only baseline `36.3 ms` -> scratch `35.0 ms` (`1.037x` old/new, neutral/no-ship). Earlier 7-run smoke rows looked positive but were refuted by the 25-run run: read `58.5 ms` -> `56.2 ms` (`1.041x`), walk `43.3 ms` -> `39.3 ms` (`1.102x`). | `0/2/0`: both final direct comparisons remain slower than mounted kernel btrfs. | Local release-perf scratch build passed; local `cargo check -p ffs-core --all-targets` passed with the scratch candidate before revert; RCH `cargo build --release -p ffs-cli` passed on `vmi1153651` after revert; RCH conformance `cargo test -p ffs-harness --test conformance -- --nocapture` passed on `vmi1149989` (100 passed / 0 failed / 2 ignored); no production source retained. Detailed scorecard: `docs/progress/perf-gauntlet-scorecard.md`. |
| 2026-06-20 | `bd-2emlm` | btrfs 100 MiB uncompressed warm read plus `FileByteDevice` staging scratch and btrfs read-into-dst candidates | REJECT / no production source kept | Mixed kernel result on the candidate binary: FrankenFS `read --discard /m.bin` mean `74.949 ms` beat kernel btrfs `dd bs=128M` mean `127.923 ms` (`1.71x` faster), but lost to kernel `dd bs=8M` mean `51.407 ms` (`1.51x` slower) and kernel `cat` mean `11.710 ms` (`6.63x` slower). Against the prior FrankenFS default-32 context (`76.3 ms`), the candidate was neutral (`74.949 ms` first run / `77.580 ms` streaming run) and kept max RSS high (`137,968 KiB` single run, not lower than the prior ~133 MiB btrfs profile). | `1/0/1`: RCH `hz1` same-binary primitive A/B showed old fresh-temp staging median `1.0804 ms` vs reusable scratch median `96.908 us` (`11.15x` old/new), but the real btrfs read stayed neutral versus prior FrankenFS timing, so the primitive win did not transfer to the target workload. | `1/2/0`: one materializing-kernel win (`dd bs=128M`), two streaming/buffered kernel losses (`cat`, `dd bs=8M`). | RCH `cargo check -p ffs-block --all-targets` passed on `hz1`; RCH `cargo test -p ffs-block file_byte_device -- --nocapture` passed on `vmi1149989` (9 passed / 0 failed); RCH `cargo clippy -p ffs-block --all-targets -- -D warnings` passed on `vmi1152480`; RCH `cargo bench -p ffs-block --bench read_contiguous -- file_byte_device_read_1mib --warm-up-time 1 --measurement-time 2` passed on `hz1`; RCH `cargo build --release -p ffs-cli` passed on `hz1`; RCH `cargo test -p ffs-harness --test conformance -- --nocapture` passed on `vmi1152480` (100 passed / 0 failed / 2 ignored); production candidates were manually reverted after whole-workload rejection. |
| 2026-06-20 | `bd-xmh5g` | ext4 non-extent indirect sequential read, chunking one large coalesced indirect run into ordered parallel data segments | KEEP / production retained with default `128` block chunks and env override `FFS_INDIRECT_READ_CHUNK_BLOCKS` | Existing direct ext4-kernel gap remains a loss until a mounted comparator can rerun: prior 32 MiB `^extent` probe measured FrankenFS `211-224 ms` vs kernel ext4 `45 ms`, `~4.7-5.0x` slower. Fresh cod-b RCH direct comparator built `ffs-cli` release-perf and a valid no-extents image on `hz1`, but `mount -o loop,ro` failed `failed to setup loop device`; temp artifact `/tmp/ffs_indirect_cmp.0g2lsq`. | `4/1/1`: same-worker RCH Criterion on `vmi1227854`, `large_run_single/8192` median `25.523 ms`; chunked 16 blocks `31.397 ms` (`0.813x`, loss), 32 blocks `23.067 ms` (`1.106x`, neutral/noisy), 64 blocks `17.267 ms` (`1.478x`), 128 blocks `15.729 ms` (`1.623x`, kept default), 256 blocks `16.591 ms` (`1.539x`), 512 blocks `17.475 ms` (`1.461x`). Non-contiguous serial/parallel rows stayed routing evidence only. | `0/1/0` existing direct loss; fresh direct rerun blocked by loop-device policy | RCH `cargo bench --profile release-perf -p ffs-core --bench ext4_indirect_read_overlap -- ext4_indirect_read_overlap/large_run --warm-up-time 1 --measurement-time 1 --sample-size 20` passed on `vmi1227854`; RCH focused test `cargo test -p ffs-core ext4_indirect_large_run_chunks_default_bd_xmh5g -- --nocapture` passed on `vmi1167313`; RCH `cargo check -p ffs-core --all-targets` passed on `vmi1152480`; RCH-wrapper local fallback `cargo test -p ffs-harness --test conformance -- --nocapture` passed `100 / 0 / 2 ignored`; `git diff --check` passed. Full and scoped clippy are blocked by pre-existing `ffs-repair`, `ffs-core/src/vfs.rs`, and older `ffs-core/src/lib.rs` pedantic debt outside this lever. |
| 2026-06-20 | `bd-xmh5g.395` | `ffs-mvcc` sharded chain-head compaction materializes a resolved `Cow::Owned` via `into_owned` instead of cloning it with `to_vec` | KEEP / production already retained in `8d610785`; conformance guard already retained in `6fed9db6`; this closeout is measured verification | N/A for direct ext4/btrfs-kernel ratio: this is an internal MVCC compressed-version materialization primitive on the GC/chain-compaction path. The parent direct ext4 indirect-read loss remains open (`~4.7-5.0x` slower than kernel until loop-device comparator access is restored). | `3/0/0`: fresh cod-b RCH Criterion on `vmi1152480`, `read_block_cow_owned`, old `to_vec_clone` vs `into_owned_move`: 4 KiB `214.82 ns` vs `13.523 ns` (`15.89x` old/new), 16 KiB `1.0014 us` vs `12.372 ns` (`80.94x`), 64 KiB `5.9061 us` vs `10.004 ns` (`590.37x`). | `0/0/1` | RCH `cargo bench --profile release-perf -p ffs-mvcc --bench read_block_cow_owned -- --warm-up-time 1 --measurement-time 3` passed on `vmi1152480`; RCH focused test `cargo test -p ffs-mvcc prune_preserves_read_visible_data_after_chain_head_compaction -- --nocapture` passed on `vmi1152480`; RCH `cargo check -p ffs-mvcc --all-targets` passed on `vmi1293453`; RCH `cargo clippy -p ffs-mvcc --all-targets --no-deps -- -D warnings` passed on `vmi1152480`; RCH conformance passed on `vmi1153651` (100 passed / 0 failed / 2 ignored); `cargo fmt -p ffs-mvcc --check` passed locally. Plain clippy without `--no-deps` is blocked by existing `ffs-repair` pedantic lint debt, not by this MVCC surface. |
| 2026-06-20 | `bd-xmh5g.402` | `ffs-mvcc` sharded conflict-merge base/latest version materialization, `Vec` clone vs borrowed `Cow` for read-only `merge_bytes` inputs | KEEP / production retained | N/A for direct ext4/btrfs-kernel ratio: this is an internal MVCC conflict-merge byte materialization primitive; no kernel-equivalent timed primitive exists. Parent direct ext4/btrfs read gaps remain open. | `3/0/0`: primary same-worker RCH Criterion on `vmi1227854`, `conflict_merge_materialization_ab`, old clone vs borrowed `Cow`: 4 KiB `127.52 ns` vs `4.4895 ns` (`28.40x` old/new), 16 KiB `478.34 ns` vs `4.3897 ns` (`108.97x`), 64 KiB `1.5497 us` vs `4.4659 ns` (`347.0x`). Secondary `hz1` sanity also won `22.34x`, `94.98x`, and `377.3x`, but is not the acceptance ratio. | `0/0/1` | RCH bench passed on `vmi1227854`; RCH `cargo check -p ffs-mvcc --bench wal_throughput` passed on `vmi1227854`; RCH focused sharded SafeMerge test passed on `vmi1153651` (1 passed / 0 failed); RCH `cargo clippy -p ffs-mvcc --all-targets --no-deps -- -D warnings` passed on `hz1`; RCH `cargo test -p ffs-harness --test conformance -- --nocapture` passed on `vmi1153651` (100 passed / 0 failed / 2 ignored); local `cargo fmt --check --package ffs-mvcc` passed. RCH `cargo build --release -p ffs-mvcc` compiled successfully on `vmi1153651` but artifact retrieval failed with `RCH-E309`/exit 102. Full `cargo test -p ffs-harness -- --nocapture` exposed unrelated mounted FUSE failures and was interrupted after the failures were observed. |

## 2026-06-20 cc (BlackThrush) Verification

| Date | Bead | Surface | Verdict | Ratio vs ext4/btrfs-kernel | Internal win/loss/neutral | Direct kernel win/loss/neutral | Gates |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-20 | `bd-2emlm` | `ffs-block` `FileByteDevice::read_exact_at` large-read direct: skip the per-read `vec![0; len]` staging scratch (zero-init + pread + copy) for reads `>= 64 KiB` and read straight into the caller buffer, with an up-front `fstat` length re-check preserving the all-or-nothing contract; small reads keep the freelist-cheap scratch. Threshold env-overridable via `FFS_DIRECT_READ_MIN_BYTES`. Hits the ext4 `read_contiguous_into` bulk path. | KEEP / production retained (`8ed75ef5` source, `fd3cecfb` env knob) | Direct kernel **WIN** on like-for-like materialize: FrankenFS `read --discard` of a 128 MiB warm ext4 file = `22.85 ms` (`5602 MB/s`) vs single-thread kernel `dd bs=128M` materialize `~1150 MB/s` (**4.9x faster**) and `dd bs=8M` `~2200 MB/s` (**2.5x faster**) — FrankenFS parallelizes the chunked read across the 64-core rayon pool while kernel `dd` is single-threaded. Loses only to `cat`→`/dev/null` (`~10 ms`, splice/idealized streaming, not a real per-byte materialize), `~2.3x` slower. Image not loop-mountable under policy, so the kernel comparator materializes the identical source bytes from the warm tmpfs source. | `2/0/0`: (1) primitive microbench A/B (`ffs-block/benches/file_device_read.rs`, 1 MiB warm, staged_scratch vs direct) = **13.0x** (host vmi1227854) / **17.6x** (host hz1); (2) **real-workload e2e A/B** (same release-perf binary, `FFS_DIRECT_READ_MIN_BYTES` toggling lever on/off, 128 MiB warm ext4 `read --discard`, 13 runs × 2 interleaved repeats) lever-ON median `22.85 ms` vs forced-staged `26.5 ms` = **1.16x** — the primitive win **transfers** to the real path (contrast cod-b row 27: btrfs scratch-*reuse* candidate's 11x primitive did NOT transfer; this is scratch-*elimination* + size-gate on the ext4 path). | `2/0/1`: wins vs `dd bs=128M` (4.9x) and `dd bs=8M` (2.5x) materialize; loses vs `cat` splice (2.3x). | ffs-block 306 tests green (RCH `hz1`) incl. 2 new large-read preserve-on-shrink + round-trip tests; ffs-core 1177 tests green (consumer); RCH `cargo clippy -p ffs-block --all-targets --no-deps -- -D warnings` clean; full conformance `cargo test -p ffs-harness --test conformance` = **100 passed / 0 failed / 2 ignored** (one `ext4_fscrypt_legacy_policy_transport_discrepancy_conforms` flake in a parallel run passed both ways in isolation with `FFS_DIRECT_READ_MIN_BYTES` on/off AND on a clean full re-run → lever-independent). `ffs-cli` built `--profile release-perf` locally (RCH artifact retrieval left target-dir binary stale, the known RCH issue). |
| 2026-06-20 | `bd-h6p3w`/`.399` | `ffs-cli walk --parallel` (level-parallel BFS) on a **warm single-directory** htree (30,002 files in one dir), measuring whether the parallel-walk lever helps when there is nothing to fan out across and the page cache is hot | KEEP as-is / opt-in lever, no change | N/A direct-kernel (ext4 image not loop-mountable here; tmpfs source dentry-cache is not a comparable ext4 metadata baseline). Absolute FrankenFS serial walk = `47.9 ms` median for 30k files (~`1.6 µs`/entry: readdir + getattr + inode parse). | `0/1/0` **internal LOSS for this workload**: parallel walk median `61.5 ms` vs serial `47.9 ms` = **0.78x** (slower). A single directory has no sibling dirs to walk concurrently and warm metadata has no cold I/O to overlap, so the BFS/thread machinery is pure overhead. **This is expected and bounds the lever:** parallel-walk is a COLD + SCATTERED + MULTI-DIR win (6.16x measured in prior cold sessions, bd-h6p3w), not a warm-single-dir win. NOT reverted — it is opt-in (`--parallel` / FUSE worker threads) and correctly serves the cold scattered-inode case where readahead is defeated by htree hash-order. | `0/0/1` (no kernel comparator obtainable without a loop mount) | Measured with the local release-perf `ffs-cli` on `/tmp/ffs_meta_*.img` (mke2fs -d, 30k empty files, dir_index), 7 warm runs each; no source change. |
| 2026-06-20 | `bd-xmh5g` (head-to-head verification) | **Real mounted-kernel ext4 head-to-head** (loop `mount -o loop,ro` succeeded on this box; prior agents had hit loop-setup failures). Same ext4 images, warm, FrankenFS no-FUSE engine (`ffs-cli`) vs the in-kernel ext4 driver. Two workloads: (a) metadata walk of a 30,002-file `dir_index` htree directory; (b) streaming read of a 128 MiB file. | KEEP — measured verification, no source change | **Two regimes, both confirmed vs the REAL kernel:** (a) **METADATA WALK — FrankenFS WINS 2.03x**: `ffs-cli walk` `44.4 ms` vs kernel `find -printf` (readdir+getattr each entry) `90.0 ms`, and `3.8x` vs `ls -lU` `170 ms`. FrankenFS bulk-parses the inode table + htree dir blocks in a few big sequential reads; the kernel pays ~30k per-file `getattr` syscalls. (b) **STREAMING READ — depends entirely on the kernel baseline**: `read --discard` `24.0 ms` (`5.3 GB/s`). vs kernel `cat`→`/dev/null` `10.0 ms` (`12.8 GB/s`) FrankenFS is `2.4x` slower — BUT `cat` to `/dev/null` uses **splice (zero-copy, never materializes the bytes to a userspace buffer)**, which a real data-consuming app cannot use. Against the kernel actually **reading bytes into a userspace buffer** (apples-to-apples with what FrankenFS does): kernel `dd bs=1M` and `dd bs=128K` both measure **~3.2 GB/s** on the mounted ext4 → **FrankenFS WINS 1.66x** (5.3 vs 3.2), and `4.9x` vs `dd bs=128M`. FrankenFS wins because it parallelizes the chunked read across the 64-core rayon pool while kernel `dd` is single-threaded. | N/A (this row IS the kernel comparison) | **Direct kernel: metadata WIN 2.03x; read = WIN vs every materializing kernel path (`1.66x` vs `dd bs=1M`, `2.5x` vs `bs=8M`, `4.9x` vs `bs=128M`), LOSS only vs zero-copy `splice` (`cat`, `2.4x`).** Corrected framing: the only "read loss" is vs a path that doesn't deliver data to userspace; for real reads-into-memory FrankenFS beats the single-threaded kernel. Boundary holds: wins scattered/parallel/metadata + parallel materialize, loses only vs kernel splice/streaming idealization. | Local release-perf `ffs-cli`; RO loop mounts at `/tmp/ffs_mnt_*` and `/tmp/ffs_rmnt_*` (left mounted — `umount` is policy-blocked); 7 warm runs each; `find` scoped to `bigdir` to avoid the root-owned `lost+found` perm error. |
| 2026-06-20 | `bd-2emlm` (btrfs head-to-head) | **Real mounted-kernel btrfs head-to-head.** 128 MiB file in a btrfs image (`btrfs-convert` of the ext4 image), warm, FrankenFS `ffs-cli read --discard` vs the in-kernel btrfs driver (loop `mount -o loop,ro`). | KEEP — measured verification, no source change | FrankenFS btrfs read = `63.4 ms` (`2.0 GB/s`). vs kernel `dd bs=128M` materialize `~1.15 GB/s` → **FrankenFS 1.74x faster**; vs `dd bs=8M`/`bs=1M` `~2.2-2.3 GB/s` → **~parity** (`0.87-0.91x`, slightly behind); vs `cat` splice `12.8 GB/s` → `6.4x` slower. The FileByteDevice direct-read lever (`8ed75ef5`) **also transfers to btrfs**: lever-ON `58.5 ms` vs forced-staged `66.0 ms` = `1.13x` (cf. ext4 `1.16x`), so the scratch elimination is a general read-path win across both filesystems. | N/A (this row IS the kernel comparison) | **Direct kernel btrfs: WIN vs big-buffer materialize (1.74x, confirms the W162 read-into-dst `54b0ae94` ~1.4x claim), PARITY vs small-buffer `dd`, LOSS vs splice.** Weaker than ext4 (which beats `dd bs=1M` 1.66x) — btrfs read is `2.0` vs ext4 `5.3 GB/s`, the documented heavier extent-tree / higher-RSS path (bd-2emlm: btrfs ~2.65x slower than ext4, memory-pressure bound). Headroom remains on the btrfs read path specifically. | Local release-perf `ffs-cli`; `btrfs-convert` image; RO loop mount at `/tmp/ffs_bmnt_*` (left mounted); 7 warm runs each. |
| 2026-06-20 | `bd-2emlm` (btrfs read re-root-cause) | Re-root-cause of the btrfs-vs-ext4 read gap (btrfs `2.0` vs ext4 `5.3 GB/s`, both warm 128 MiB) now that the read-into-dst fix + FileByteDevice direct lever shipped. `perf stat` 20-read loops + `/usr/bin/time -v` RSS + chunk sweep + code read. | INVESTIGATION — accurate root-cause recorded, deep lever filed (no source change) | N/A (internal ext4-vs-btrfs gap; btrfs read is at PARITY-to-WIN vs kernel per the row above, not a kernel loss). | **Two stale conclusions CORRECTED:** (1) **Footprint is NO LONGER the cause** — btrfs read RSS is now `71.5 MB` vs ext4 `63.7 MB` (was 133 MB / 2x historically); `perf stat` page-faults are **identical** (`345k` vs `344k`). The memory-pressure root-cause is obsolete. (2) The **serial-extent-loop hypothesis is WRONG** — `btrfs_read_file_into` builds a job plan in a cheap serial pass (`lib.rs:8678`) then executes **all** jobs cross-extent in ONE `into_par_iter` (`lib.rs:8916`), so it already parallelizes across extents. **The real signal:** task-clock is ~equal (ext4 `9.52s` vs btrfs `9.78s` total CPU) but **CPUs-utilized = ext4 `12.6` vs btrfs `6.9`** — btrfs read gets HALF the effective parallelism for near-equal total work. Chunk-size sweep confirms 32 blocks already optimal (8/16=slower, 64/128=flat). Cause is per-job btrfs overhead in the parallel map (chunk-tree logical→physical mapping and/or read path serialization) — needs a `perf record` flamegraph to pinpoint. | Filed deep-profile bead. perf via `kernel.perf_event_paranoid=1` (restored to 4). Local release-perf `ffs-cli`, btrfs-convert image, RO loop mount. |
| 2026-06-20 | `bd-xmh5g.409` (flamegraph follow-up) | `perf record` flamegraph + `strace -c` of the btrfs vs ext4 read to localize the 6.9-vs-12.6-CPU gap. | INVESTIGATION — gap is **likely a fixture artifact**, no source change | N/A (internal). | **Flamegraph: btrfs read self-time is `41.6%` in `pread64`, ext4 is `0.03%`** — same rayon overhead on both (steal `4.4%`, epoch `1.4%`, `wait_until_cold` `0.95%`). **`strace` rules out read amplification: btrfs `1050` preads / `128.1 MiB` vs ext4 `1035` preads / `128.0 MiB` — near-identical count AND bytes.** So the *same* warm 128 KiB preads cost ~`41.6%` in btrfs but ~`0%` in ext4. Both read from the tmpfs-backed image (RAM), so the delta is **access locality**: ext4's data is physically contiguous (sequential pread offsets → readahead-friendly, pages hot), while **`btrfs-convert` scatters the file's physical layout** (random pread offsets → poor locality / defeated readahead). **Reframes the conclusion: the btrfs read "gap" measured here is largely an artifact of the CONVERTED image's scattered layout, NOT necessarily a frankenfs read-path deficiency.** The 6.9 vs 12.6 CPUs follows: cores spend longer in each (costlier) scattered pread. **Retry predicate:** re-measure on a NATIVELY-created btrfs image with a contiguous file (needs the btrfs format tool, currently policy-blocked) before treating this as a real frankenfs lever; bd-xmh5g.409 updated accordingly. | Local release-perf `ffs-cli`; `perf record --call-graph dwarf` (paranoid=1→4); `strace -c -e pread64`; btrfs-convert image. |

Detailed historical context and retry predicates remain in
`docs/progress/perf-negative-results.md`.
